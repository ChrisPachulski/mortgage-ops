---
phase: 16
plan: 04
type: execute
wave: 3
depends_on: [16-01, 16-02, 16-03]
files_modified:
  - tests/test_property_analysis.py
  - tests/fixtures/property_analysis/condo_with_hoa_seattle.json
  - tests/fixtures/property_analysis/sfh_conforming_king_county.json
  - tests/fixtures/property_analysis/sfh_jumbo_bay_area.json
autonomous: true
requirements: [REF-09, REF-10]
must_haves:
  truths:
    - "All 7 literal-string assertions of `PMI-RATE-ESTIMATED-0.0075` in `tests/test_property_analysis.py` (verified L481, L1101 (docstring), L1102 (docstring), L1116 (substring), L1120 (literal), L1299 (docstring), L1319 (docstring), L1326 (literal)) are REWRITTEN to substring/prefix matches against the new MGIC family — never the exact retired literal"
    - "`condo_with_hoa_seattle.json` is RE-ANCHORED: PMI rate values in cells' computed PITI/DTI are recomputed using the new YAML's MGIC rate (NOT a string find/replace — the math changes because the YAML rate differs from the placeholder 0.0075), and the literal `PMI-RATE-ESTIMATED-0.0075` / `PMI-RATE-ESTIMATED` strings (5 occurrences at L6 notes, L8 _meta citation, L80 eligible_reasons, L96 eligible_reasons, L126 top-level `warnings` array) are replaced with the new parameterized tag (e.g., `PMI-RATE-ESTIMATED-MGIC-90-95-740-759` for cell eligible_reasons; `PMI-RATE-ESTIMATED-MGIC` for the top-level warnings aggregator label)"
    - "`sfh_conforming_king_county.json` and `sfh_jumbo_bay_area.json` have their top-level `warnings` array entries updated from `PMI-RATE-ESTIMATED` to `PMI-RATE-ESTIMATED-MGIC` (the new aggregator label per Plan 16-02 Task 2)"
    - "The two warnings-aggregator tests (`test_conv_pmi_warning_surfaces` at L475 and `test_analyze_warnings_dedup_pmi_estimated` at L1099) assert the new tag pattern: `any(r.startswith('PMI-RATE-ESTIMATED-MGIC-') for r in cell.eligible_reasons)` AND `w == 'PMI-RATE-ESTIMATED-MGIC'` for the warning label (matching what Plan 16-02 Task 2 emits)"
    - "After this plan: `grep -rc 'PMI-RATE-ESTIMATED-0.0075' lib/ tests/` returns 0 (the literal is search-and-destroyed across source, tests, and fixtures — closing D-16-PMI-03's 'reason-code retirement' decision)"
    - "After this plan: `uv run pytest tests/test_property_analysis.py tests/test_property_verdict.py -x` exits 0 (full Phase 14 regression GREEN — ROADMAP SC #4 'lib/rules/_loader.py:_check_staleness warns when effective: > 12 months old — no loader changes' indirectly verified by the test suite re-running without loader edits)"
    - "After this plan: `uv run pytest tests/ -x` exits 0 (full project regression GREEN — all Phase 14 + Phase 16 fixtures synchronized; Phase 13 + Phase 15 untouched per scope)"
    - "Citation-coverage meta-test still green after re-anchoring: `uv run pytest tests/test_rules/test_citation_coverage.py -x` exits 0 (no fixture renames invalidate the `pmi_*.json` / `insurance_*.json` glob)"
  artifacts:
    - path: "tests/test_property_analysis.py"
      provides: "Phase 14 regression suite with assertion patterns matching the new tag families"
      contains: "PMI-RATE-ESTIMATED-MGIC"
    - path: "tests/fixtures/property_analysis/condo_with_hoa_seattle.json"
      provides: "Re-anchored hand-calc golden values using the new YAML's MGIC rate"
      contains: "PMI-RATE-ESTIMATED-MGIC"
    - path: "tests/fixtures/property_analysis/sfh_conforming_king_county.json"
      provides: "Warnings array updated for the new aggregator label"
      contains: "PMI-RATE-ESTIMATED-MGIC"
    - path: "tests/fixtures/property_analysis/sfh_jumbo_bay_area.json"
      provides: "Warnings array updated for the new aggregator label"
      contains: "PMI-RATE-ESTIMATED-MGIC"
  key_links:
    - from: "tests/test_property_analysis.py"
      to: "lib/property_analysis.py (Phase 14 + 16 wire-in)"
      via: "import analyze + assertions against the new tag families"
      pattern: "PMI-RATE-ESTIMATED-MGIC"
    - from: "tests/fixtures/property_analysis/condo_with_hoa_seattle.json"
      to: "data/reference/property-analysis-heuristics.yml (PMI YAML shipped by 16-01)"
      via: "re-anchored hand-calc PMI rate value"
      pattern: "PMI-RATE-ESTIMATED-MGIC"
---

<objective>
Search-and-destroy the retired literal `PMI-RATE-ESTIMATED-0.0075` across `tests/test_property_analysis.py` (7 hits) and 3 fixture JSON files. Re-anchor `condo_with_hoa_seattle.json` to the new YAML's actual MGIC rate (because the PITI/DTI math changes when the rate differs from the placeholder 75bps). Restore Phase 14 regression suite to GREEN.

Purpose: Close D-16-PMI-03 (retire the placeholder literal) AND complete the Phase 14 regression invariant ("after wire-in, the full Phase 14 test suite still passes"). This is the final plan in Phase 16; after this lands, every D-16-* decision and every RESEARCH correction is closed in code.

Output: 1 modified test file + 3 modified fixture JSONs. No new files. No edits to `lib/`. No edits to YAMLs or `lib/rules/*.py`.

This plan executes after Plan 16-03 completes (which itself sequenced after 16-02 due to shared `lib/property_analysis.py` edits — see 16-03 `depends_on`). The aggregator label that this plan's test assertions must match (`PMI-RATE-ESTIMATED-MGIC`) is shipped by Plan 16-02 Task 2; the INSURANCE-ESTIMATED-NAIC tag-emit is shipped by Plan 16-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-reference-data/16-CONTEXT.md
@.planning/phases/16-reference-data/16-RESEARCH.md
@.planning/phases/16-reference-data/16-PATTERNS.md
@.planning/phases/16-reference-data/16-01-PLAN.md
@.planning/phases/16-reference-data/16-01-SUMMARY.md
@.planning/phases/16-reference-data/16-02-PLAN.md
@.planning/phases/16-reference-data/16-02-SUMMARY.md
@.planning/phases/16-reference-data/16-03-PLAN.md
@.planning/phases/16-reference-data/16-03-SUMMARY.md
@CLAUDE.md

# Source-of-truth files (post-Wave-1 state)
@lib/property_analysis.py
@lib/rules/pmi.py
@data/reference/property-analysis-heuristics.yml

# Files being edited
@tests/test_property_analysis.py
@tests/fixtures/property_analysis/condo_with_hoa_seattle.json
@tests/fixtures/property_analysis/sfh_conforming_king_county.json
@tests/fixtures/property_analysis/sfh_jumbo_bay_area.json
</context>

<interfaces>
Grep-confirmed 12 hit sites of `PMI-RATE-ESTIMATED-0.0075` and `PMI-RATE-ESTIMATED` (as of 2026-05-22, pre-edit baseline):

**tests/test_property_analysis.py (7 hits):**
- L476 docstring + L481 assertion (`"PMI-RATE-ESTIMATED-0.0075" in cell.eligible_reasons`)
- L1101-1102 docstring (mentions `PMI-RATE-ESTIMATED-0.0075` + `PMI-RATE-ESTIMATED` warning literal)
- L1116 substring check (`any("PMI-RATE-ESTIMATED" in r for r in c.eligible_reasons)`)
- L1120 literal-equals (`pmi_warnings = [w for w in report.warnings if w == "PMI-RATE-ESTIMATED"]`)
- L1299 docstring + L1319 docstring (W-1 single-literal context)
- L1326 assertion (`"PMI-RATE-ESTIMATED-0.0075" in conv30_cell.eligible_reasons`)

**tests/fixtures/property_analysis/condo_with_hoa_seattle.json (5 hits — verified by `grep -n "PMI-RATE-ESTIMATED" tests/fixtures/property_analysis/condo_with_hoa_seattle.json`):**
- L6 notes (citation prose mentions the 0.0075 placeholder + the resulting PITI numbers)
- L8 _meta citation (references the 0.0075 soft signal)
- L80 eligible_reasons (`["PMI-RATE-ESTIMATED-0.0075"]`)
- L96 eligible_reasons (same literal)
- L126 top-level `warnings` array (`["PMI-RATE-ESTIMATED"]`) — structurally identical to `sfh_conforming_king_county.json:130` and `sfh_jumbo_bay_area.json:142` (the AnalysisReport-level warnings aggregator output)

**tests/fixtures/property_analysis/sfh_conforming_king_county.json (1 hit):**
- L130 warnings array (`["PMI-RATE-ESTIMATED"]`)

**tests/fixtures/property_analysis/sfh_jumbo_bay_area.json (1 hit):**
- L142 warnings array (same)

From Plan 16-02's _REPORT_WARNING_PREFIXES (shipped):
- The new warning labels are `"PMI-RATE-ESTIMATED-MGIC"`, `"PMI-RATE-CAPPED-MGIC"`, `"INSURANCE-ESTIMATED-NAIC"`.

From Plan 16-01's PMI YAML (shipped — values pinned at MGIC archive time):
- The actual MGIC rate for the household's FICO × LTV band determines the new PITI math in condo_with_hoa_seattle.json.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite the 7 literal-string assertions in tests/test_property_analysis.py</name>

  <files>tests/test_property_analysis.py</files>

  <read_first>
    - tests/test_property_analysis.py (offset 470, limit 20 — test_conv_pmi_warning_surfaces at L475-481, the single-line literal assertion)
    - tests/test_property_analysis.py (offset 1095, limit 35 — test_analyze_warnings_dedup_pmi_estimated at L1099-1122, docstring + substring check + literal-equals check)
    - tests/test_property_analysis.py (offset 1290, limit 45 — the W-1 verdict test at L1299-1326, docstring + final assertion)
    - lib/property_analysis.py (offset 1495, limit 25 — confirm the post-16-02 aggregator label is `PMI-RATE-ESTIMATED-MGIC`)
    - lib/rules/pmi.py (FULL — confirm the in-band reason tag pattern is `PMI-RATE-ESTIMATED-MGIC-{ltv_band}-{fico_band}` so assertions match)
    - .planning/phases/16-reference-data/16-02-PLAN.md (confirm `_REPORT_WARNING_PREFIXES` declares `PMI-RATE-ESTIMATED-MGIC` and `PMI-RATE-CAPPED-MGIC`)
    - .planning/phases/16-reference-data/16-02-SUMMARY.md (read post-implementation line numbers — they may have drifted)
    - .planning/phases/16-reference-data/16-RESEARCH.md (Edit Site 7 — verbatim find/replace pattern: `assert any(r.startswith("PMI-RATE-ESTIMATED-MGIC-") for r in cell.eligible_reasons)`)
  </read_first>

  <behavior>
    - After this task: `grep -c "PMI-RATE-ESTIMATED-0.0075" tests/test_property_analysis.py` returns 0.
    - `test_conv_pmi_warning_surfaces` (L475-481): assertion changes from `"PMI-RATE-ESTIMATED-0.0075" in cell.eligible_reasons` to `any(r.startswith("PMI-RATE-ESTIMATED-MGIC-") for r in cell.eligible_reasons)`. Docstring at L476 updated to mention the new tag family.
    - `test_analyze_warnings_dedup_pmi_estimated` (L1099-1121): docstring mentions of `PMI-RATE-ESTIMATED-0.0075` and `PMI-RATE-ESTIMATED` (the literal warning) updated to `PMI-RATE-ESTIMATED-MGIC-*` (cell tag) and `PMI-RATE-ESTIMATED-MGIC` (warning label). L1116 substring check stays as substring (`"PMI-RATE-ESTIMATED" in r` still matches the new tag — leave the broad substring alone OR tighten to `"PMI-RATE-ESTIMATED-MGIC" in r` for clarity). L1120 literal-equals check changes from `w == "PMI-RATE-ESTIMATED"` to `w == "PMI-RATE-ESTIMATED-MGIC"` to match the new aggregator label.
    - W-1 verdict test (L1299-1326): docstring mentions of `PMI-RATE-ESTIMATED-0.0075` (L1299, L1319) updated to reference the MGIC family; final assertion at L1326 changes from `"PMI-RATE-ESTIMATED-0.0075" in conv30_cell.eligible_reasons` to `any(r.startswith("PMI-RATE-ESTIMATED-MGIC-") for r in conv30_cell.eligible_reasons)`.
    - All other tests in `test_property_analysis.py` (~600 tests) are NOT modified. The only assertions touched are the 7 grep-confirmed hits.
    - After this task (combined with Task 2 below): `uv run pytest tests/test_property_analysis.py -x` exits 0.
  </behavior>

  <action>
    Use `grep -n "PMI-RATE-ESTIMATED" tests/test_property_analysis.py` to enumerate every hit. Cross-reference against the expected 7 sites (line numbers may drift slightly):
    - L476, L481 (test_conv_pmi_warning_surfaces docstring + assertion)
    - L1101, L1102 (test_analyze_warnings_dedup_pmi_estimated docstring)
    - L1116 (substring check inside the test)
    - L1120 (literal-equals warning-label check)
    - L1299, L1319 (W-1 verdict test docstring)
    - L1326 (W-1 verdict test final assertion)

    Apply the following targeted edits:

    EDIT 1 — `test_conv_pmi_warning_surfaces` docstring (L476):
    Change `"""Conv30 at 5% DP tags eligible_reasons with PMI-RATE-ESTIMATED-0.0075."""` to:
    `"""Conv30 at 5% DP tags eligible_reasons with PMI-RATE-ESTIMATED-MGIC-{ltv_band}-{fico_band}."""`

    EDIT 2 — `test_conv_pmi_warning_surfaces` assertion (L481):
    Change `assert "PMI-RATE-ESTIMATED-0.0075" in cell.eligible_reasons` to:
    `assert any(r.startswith("PMI-RATE-ESTIMATED-MGIC-") for r in cell.eligible_reasons)`

    EDIT 3 — `test_analyze_warnings_dedup_pmi_estimated` docstring (L1100-L1103):
    Find the docstring describing the test purpose. Replace mentions of:
    - `PMI-RATE-ESTIMATED-0.0075` → `PMI-RATE-ESTIMATED-MGIC-{ltv_band}-{fico_band}`
    - `'PMI-RATE-ESTIMATED'` (the warning literal) → `'PMI-RATE-ESTIMATED-MGIC'`

    EDIT 4 — `test_analyze_warnings_dedup_pmi_estimated` substring check at L1116:
    OPTIONAL tightening — `"PMI-RATE-ESTIMATED" in r` still matches the new tag (it's a strict substring of `PMI-RATE-ESTIMATED-MGIC-*`), so it can stay as-is. For clarity, tighten to `"PMI-RATE-ESTIMATED-MGIC" in r`. Either is acceptable; pick tightening for grep-discoverability.

    EDIT 5 — `test_analyze_warnings_dedup_pmi_estimated` warning-label equals check at L1120:
    Change `pmi_warnings = [w for w in report.warnings if w == "PMI-RATE-ESTIMATED"]` to:
    `pmi_warnings = [w for w in report.warnings if w == "PMI-RATE-ESTIMATED-MGIC"]`
    (Matches the new aggregator label per Plan 16-02 Task 2.)

    EDIT 6 — W-1 verdict test docstring (L1299, L1319):
    Replace `PMI-RATE-ESTIMATED-0.0075 soft signal` (L1299) with `PMI-RATE-ESTIMATED-MGIC-* soft signal`. Replace L1319's similar wording.

    EDIT 7 — W-1 verdict test final assertion (L1326):
    Change `assert "PMI-RATE-ESTIMATED-0.0075" in conv30_cell.eligible_reasons` to:
    `assert any(r.startswith("PMI-RATE-ESTIMATED-MGIC-") for r in conv30_cell.eligible_reasons)`

    DO NOT modify any other test in this file. DO NOT modify imports. DO NOT modify the helper fixtures `_make_clean_household` / `_make_clean_listing` / `_make_clean_profile` — they're already compatible (the household has a FICO + state_fips that will route correctly through the new lookup).

    Optional sanity verification while editing: use `grep -n "PMI-RATE-ESTIMATED-0.0075" tests/test_property_analysis.py` after each edit pass to confirm hit count decreases monotonically toward 0.
  </action>

  <verify>
    <automated>uv run pytest tests/test_property_analysis.py::test_conv_pmi_warning_surfaces tests/test_property_analysis.py::test_analyze_warnings_dedup_pmi_estimated -x</automated>
  </verify>

  <acceptance_criteria>
    - Retired literal gone from test file: `grep -c "PMI-RATE-ESTIMATED-0.0075" tests/test_property_analysis.py` returns 0
    - New tag prefix present in assertions: `grep -c "PMI-RATE-ESTIMATED-MGIC-" tests/test_property_analysis.py` returns ≥ 2 (the 2 prefix-assertions)
    - New warning label present in equals check: `grep -c "w == \"PMI-RATE-ESTIMATED-MGIC\"" tests/test_property_analysis.py` returns 1
    - Targeted tests pass: `uv run pytest tests/test_property_analysis.py::test_conv_pmi_warning_surfaces tests/test_property_analysis.py::test_analyze_warnings_dedup_pmi_estimated -x` exits 0
    - Full file still passes EXCEPT for any fixture-pinned tests (Task 2 closes those): `uv run pytest tests/test_property_analysis.py -x --ignore-glob='*fixture*' 2>&1 | tail -5` shows the fixture-snapshot test as the remaining failure (closed by Task 2)
  </acceptance_criteria>

  <done>
    All 7 grep-confirmed hits in `tests/test_property_analysis.py` rewritten to match the new tag pattern; the two warnings-aggregator tests + the W-1 verdict test pass against the post-Wave-1 source; `grep -c "PMI-RATE-ESTIMATED-0.0075" tests/test_property_analysis.py` returns 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-anchor the 3 Phase 14 fixture JSONs to the new MGIC rates</name>

  <files>
    tests/fixtures/property_analysis/condo_with_hoa_seattle.json,
    tests/fixtures/property_analysis/sfh_conforming_king_county.json,
    tests/fixtures/property_analysis/sfh_jumbo_bay_area.json
  </files>

  <read_first>
    - tests/fixtures/property_analysis/condo_with_hoa_seattle.json (FULL — the 5-hit fixture; the L6 notes + L8 _meta citation + L80/L96 eligible_reasons + L126 top-level warnings array all need attention, AND the hand-calc PITI/DTI numbers downstream of the PMI rate change need recomputation)
    - tests/fixtures/property_analysis/sfh_conforming_king_county.json (FULL — single-hit fixture; only L130 warnings array needs updating)
    - tests/fixtures/property_analysis/sfh_jumbo_bay_area.json (FULL — single-hit fixture; only L142 warnings array needs updating)
    - data/reference/property-analysis-heuristics.yml (FULL — the actual MGIC rate values shipped in 16-01; needed for re-anchoring condo_with_hoa_seattle.json's hand-calc)
    - .planning/phases/16-reference-data/16-01-SUMMARY.md (the post-Plan-16-01 record of which exact MGIC rate values shipped in the YAML — needed to compute the new PMI dollar amounts)
    - .planning/phases/16-reference-data/16-RESEARCH.md (Edit Site 6 — verbatim per-fixture action)
    - .planning/phases/16-reference-data/16-PATTERNS.md (lib/property_analysis.py Edit Site 6 — same per-fixture action)
    - tests/test_property_analysis.py (the test loading these fixtures — confirm via grep how they're loaded so the executor knows which fields are pinned)
  </read_first>

  <behavior>
    - `condo_with_hoa_seattle.json` (5 hits, verified by `grep -n "PMI-RATE-ESTIMATED" tests/fixtures/property_analysis/condo_with_hoa_seattle.json` → L6, L8, L80, L96, L126):
      - L80 + L96 `eligible_reasons` arrays change from `["PMI-RATE-ESTIMATED-0.0075"]` to the new parameterized tag matching the household's actual FICO band × the cell's LTV band (e.g., for FICO 740 × LTV 95: `["PMI-RATE-ESTIMATED-MGIC-90-95-740-759"]`). The exact tag is derived from the YAML's `fico_band_label` + `ltv_band_label` for the matching row.
      - L126 top-level `"warnings": ["PMI-RATE-ESTIMATED"]` array changes to `"warnings": ["PMI-RATE-ESTIMATED-MGIC"]` (the new aggregator label per Plan 16-02 Task 2). This is structurally identical to the L130/L142 edits in the conforming/jumbo fixtures — the AnalysisReport-level warnings aggregator output.
      - L6 `notes` prose mentions the old PMI rate (0.0075) and the resulting PITI ($4075.91) — these are HAND-CALC numbers that change when the YAML rate differs. Re-derive: `monthly_mi = base_loan_amount * new_rate / 12`, then `PITI = monthly_pi + monthly_tax + monthly_insurance + monthly_hoa + monthly_mi`, then update the cascade trace (stressed DTI = (new_PITI + obligations) / (income × 0.70) — verify the cascade still hits Level 3 income-shock breach, OR the verdict flips). If the new YAML rate is materially different (e.g., 0.0050 vs 0.0075), the cascade Level 3 breach may NOT fire, and the verdict could flip from WATCH to GO. **THE EXECUTOR MUST RE-HAND-CALC AND CHECK CASCADE BRANCH STILL HOLDS** — if it doesn't hold, follow the locked-default decision tree in Step 4 below.
      - L8 `_meta.citation` prose updated to drop the `0.0075` reference; replace with the new tag pattern.
      - All PMI-driven downstream values (monthly_mi, PITI, DTI, stressed_DTI) recomputed with `Decimal` math (CLAUDE.md money discipline: hand-calc against quoted-string YAML values).
    - `sfh_conforming_king_county.json`:
      - L130 `warnings` array changes from `["PMI-RATE-ESTIMATED"]` to `["PMI-RATE-ESTIMATED-MGIC"]` (the new aggregator label per Plan 16-02 Task 2).
      - No other changes needed (the fixture's hand-calc PITI numbers are not pinned to the PMI rate — verify via grep that no eligible_reasons array in this fixture mentions the retired literal; the grep at the top of this plan confirms only the warnings array carries the substring).
    - `sfh_jumbo_bay_area.json`:
      - L142 `warnings` array changes from `["PMI-RATE-ESTIMATED"]` to `["PMI-RATE-ESTIMATED-MGIC"]`.
      - No other changes needed (same rationale as sfh_conforming).
    - After Task 1 + Task 2: `uv run pytest tests/test_property_analysis.py tests/test_property_verdict.py -x` exits 0.
  </behavior>

  <action>
    PER-FIXTURE EDIT PROCEDURE:

    **A. `tests/fixtures/property_analysis/condo_with_hoa_seattle.json`** (DEEP re-anchor, 5 hits — verified by `grep -n "PMI-RATE-ESTIMATED" tests/fixtures/property_analysis/condo_with_hoa_seattle.json` returning L6, L8, L80, L96, L126):

    Enumerate the 5 hits up front to make the search-and-destroy gate trivial to verify:
    - L6: `notes` prose ("0.0075" + hand-calc PITI numbers)
    - L8: `_meta.citation` prose (mentions the 0.0075 soft signal)
    - L80: Conv30 cell `eligible_reasons: ["PMI-RATE-ESTIMATED-0.0075"]`
    - L96: Conv15 cell `eligible_reasons: ["PMI-RATE-ESTIMATED-0.0075"]`
    - L126: top-level `warnings: ["PMI-RATE-ESTIMATED"]` — the AnalysisReport-level aggregator output (structurally identical to L130/L142 in the other two fixtures)

    Step 1 — Read 16-01-SUMMARY.md to learn the actual MGIC rate value for the FICO × LTV band that the condo fixture exercises. The fixture's household has a specific FICO (read via `_make_clean_household` in `tests/test_property_analysis.py` — likely FICO 740 or 745, exercising the 740-759 band). The cells at L80 and L96 are Conv30 + Conv15 at 5% DP (LTV = 95%) — they exercise the LTV 90-95 row.

    Step 2 — Derive the new PMI dollar amounts:
    - `monthly_mi_new = quantize_cents(base_loan_amount * new_annual_rate / Decimal("12"))`
    - If, for example, `base_loan_amount = Decimal("451250")` and `new_annual_rate = Decimal("0.0080")` (compared to the old `0.0075`), then `monthly_mi_new = quantize_cents(451250 * 0.0080 / 12) = $300.83` (vs the old $282.03).

    Step 3 — Recompute PITI for each affected cell:
    - Conv30 cell at L80: `PITI_new = monthly_pi + monthly_tax + monthly_insurance + monthly_hoa + monthly_mi_new`. Update the `monthly_mi`, `monthly_piti`, and any downstream DTI fields in the cell.
    - Conv15 cell at L96: same procedure (different monthly_pi, same monthly_mi formula).

    Step 4 — Recompute the cascade trace in `notes` (L6) AND check the verdict invariant:
    - The original L6 prose claims `Conv30 stressed DTI = (4075.91 + 300) / (9500 × 0.70) = 0.658032 > 0.50 → VERDICT_WATCH_STRESS_INCOME_FAIL`.
    - Recompute with the new PITI: `stressed_DTI_new = (PITI_new + obligations) / (income × 0.70)`.
    - **If `stressed_DTI_new > 0.50`** → Level 3 breach STILL FIRES → verdict stays WATCH → fixture re-anchoring succeeds. PROCEED to Step 5.
    - **If `stressed_DTI_new ≤ 0.50`** → Level 3 breach DOES NOT FIRE → verdict flips to GO → the fixture's design intent (WATCH-via-income-shock) breaks. **DEFAULT (Option A) — DO THIS UNLESS A and B are both infeasible:** Pick a different DP cell (e.g., move from 5% DP up to 3% DP) that raises LTV, raises PMI rate, and re-pins the breach. Update the cell's DP, base_loan_amount, LTV, PMI band labels (e.g., LTV 95-97 row instead of 90-95), and all downstream PITI/DTI numbers. The WATCH-via-income-shock design intent is preserved. The new eligible_reasons tag for the chosen cell becomes `PMI-RATE-ESTIMATED-MGIC-95-97-{fico_band}` (or whichever LTV band the chosen DP lands in).
    - **If Option A fails** (no DP cell preserves the breach against the new YAML rate): **Option B** — adjust the household's monthly obligations upward (e.g., add a synthetic $200/mo obligation) OR adjust income downward by ~5% to re-pin the breach. Document the synthetic adjustment in L6 notes. This is a less-clean fix because it perturbs the household profile, but it preserves the fixture's design intent without requiring a full redesign.
    - **Option C (escalate to planner)** — Document the verdict flip + update downstream tests. **DO NOT take this option without returning to planning.** A WATCH→GO flip in this fixture cascades into the W-1 verdict test (test_property_analysis.py:1299-1326) and possibly into test_property_verdict.py assertions. Enumerating those downstream test updates is a planner-level scope question, NOT an executor decision. If both Options A and B are infeasible, STOP and emit `## PHASE INCONCLUSIVE: condo cascade flip — re-plan required` to surface the decision to the planner.

    Step 5 — Update L80 eligible_reasons:
    ```json
    "eligible_reasons": ["PMI-RATE-ESTIMATED-MGIC-90-95-740-759"]
    ```
    (Exact band labels depend on the YAML row that matches the cell's actual FICO and LTV. Read the YAML to confirm the labels. If Step 4 forced Option A — different DP cell — the LTV band label changes accordingly, e.g., `95-97`.)

    Step 6 — Update L96 eligible_reasons identically (same band labels — same household FICO, same 95% LTV at 5% DP for Conv15; or whatever LTV the Option A pivot selected).

    Step 7 — Update L8 `_meta.citation` prose: drop "PMI-RATE-ESTIMATED-0.0075"; replace with "PMI-RATE-ESTIMATED-MGIC-* soft signal".

    Step 8 — Update L6 `notes` prose: drop the `0.0075` PMI rate value and the resulting PITI numbers; substitute the new values + the new tag family name + the recomputed cascade.

    Step 9 — Update L126 top-level `warnings` array:
    ```json
    "warnings": ["PMI-RATE-ESTIMATED-MGIC"]
    ```
    (Replaces `["PMI-RATE-ESTIMATED"]`. This is the AnalysisReport-level aggregator output emitted by Plan 16-02 Task 2's `_REPORT_WARNING_PREFIXES` loop. Structurally identical to the L130 edit in sfh_conforming_king_county.json and L142 edit in sfh_jumbo_bay_area.json.)

    **B. `tests/fixtures/property_analysis/sfh_conforming_king_county.json`** (shallow update):

    Single edit at L130:
    ```json
    "warnings": ["PMI-RATE-ESTIMATED-MGIC"]
    ```
    (Replaces `["PMI-RATE-ESTIMATED"]`.)

    No other changes. The fixture's PMI cells don't carry pinned eligible_reasons that mention the rate (confirmed by the original grep at the top of this plan). Verify via `grep "PMI-RATE-ESTIMATED" tests/fixtures/property_analysis/sfh_conforming_king_county.json` returns only the L130 hit.

    **C. `tests/fixtures/property_analysis/sfh_jumbo_bay_area.json`** (shallow update):

    Same as B but for L142:
    ```json
    "warnings": ["PMI-RATE-ESTIMATED-MGIC"]
    ```

    No other changes. Same grep-verification.

    PRE-COMMIT CHECK: `grep -rc "PMI-RATE-ESTIMATED-0.0075" lib/ tests/ data/` should return 0 across the entire codebase, AND `grep -l "PMI-RATE-ESTIMATED" tests/fixtures/property_analysis/` should return no files containing the bare `"PMI-RATE-ESTIMATED"` aggregator label (all 3 fixtures must carry the new `-MGIC` suffix). If any hits remain, identify them BEFORE committing — for `condo_with_hoa_seattle.json` specifically, re-run `grep -n "PMI-RATE-ESTIMATED" tests/fixtures/property_analysis/condo_with_hoa_seattle.json` and verify all 5 baseline hits (L6, L8, L80, L96, L126) have been replaced.

    Run `uv run pytest tests/test_property_analysis.py tests/test_property_verdict.py -x` and ensure it exits 0. If any test fails due to a fixture-pinned numeric value being wrong, re-derive the math; do not silence the test.
  </action>

  <verify>
    <automated>uv run pytest tests/test_property_analysis.py tests/test_property_verdict.py tests/test_rules/ -x</automated>
  </verify>

  <acceptance_criteria>
    - Codebase-wide search-and-destroy complete: `grep -rc "PMI-RATE-ESTIMATED-0.0075" lib/ tests/ data/` returns 0
    - Old warning label gone from all 3 fixtures: `grep -l "PMI-RATE-ESTIMATED" tests/fixtures/property_analysis/` lists no file containing the bare `"PMI-RATE-ESTIMATED"` (i.e., every hit now carries the `-MGIC` or `-MGIC-{band}-{band}` suffix). Equivalent check: `grep -rE '"PMI-RATE-ESTIMATED"' tests/fixtures/property_analysis/ | wc -l` returns 0.
    - New tag pattern present in condo fixture: `grep -c "PMI-RATE-ESTIMATED-MGIC-" tests/fixtures/property_analysis/condo_with_hoa_seattle.json` returns ≥ 2 (the 2 eligible_reasons arrays at L80 + L96)
    - New aggregator label present in condo fixture top-level warnings: `grep -c "\"PMI-RATE-ESTIMATED-MGIC\"" tests/fixtures/property_analysis/condo_with_hoa_seattle.json` returns ≥ 1 (the L126 warnings array)
    - New warning label present in conforming + jumbo fixtures: `grep -c "PMI-RATE-ESTIMATED-MGIC" tests/fixtures/property_analysis/sfh_conforming_king_county.json` returns 1; same for sfh_jumbo_bay_area.json
    - JSON well-formed: `uv run python -c "import json; [json.loads(open(f).read()) for f in ('tests/fixtures/property_analysis/condo_with_hoa_seattle.json', 'tests/fixtures/property_analysis/sfh_conforming_king_county.json', 'tests/fixtures/property_analysis/sfh_jumbo_bay_area.json')]"` exits 0
    - All targeted tests pass: `uv run pytest tests/test_property_analysis.py tests/test_property_verdict.py -x` exits 0
    - Full Phase 16 rules suite still passes: `uv run pytest tests/test_rules/ -x` exits 0
    - Full project suite passes: `uv run pytest tests/ -x` exits 0
    - mypy clean on the changed files (none — only JSON + test edits in this plan; mypy not applicable)
    - If the executor took Option A or Option B from Step 4: 16-04-SUMMARY.md documents the decision (which DP cell pivoted to, OR which household-profile adjustment was applied). If Option C was triggered, the executor STOPPED and surfaced `## PHASE INCONCLUSIVE: condo cascade flip — re-plan required` instead of silently flipping the verdict.
  </acceptance_criteria>

  <done>
    All 3 Phase 14 fixtures updated; the retired literal `PMI-RATE-ESTIMATED-0.0075` no longer appears anywhere in the repo; all 5 condo_with_hoa_seattle.json hit sites (L6, L8, L80, L96, L126) replaced — including the top-level warnings aggregator at L126; `condo_with_hoa_seattle.json` re-anchored against the new YAML's actual MGIC rate (or the cascade-flip is documented in SUMMARY with the chosen Option A/B mitigation; Option C escalates back to planner instead of executor-side scope expansion); `uv run pytest tests/ -x` exits 0; ROADMAP Phase 16 SC #1, #2, #3, #5 all closed (SC #4 was a "no-changes-needed" criterion — `_loader.py` was untouched throughout Phase 16).
  </done>
</task>

</tasks>

<verification>
After both tasks complete (this plan is the final Phase 16 milestone):

```bash
# 1. Retired literal nuked everywhere
grep -rc "PMI-RATE-ESTIMATED-0.0075" lib/ tests/ data/ 2>/dev/null

# 2. Bare retired warning label nuked from all 3 fixtures
grep -rE '"PMI-RATE-ESTIMATED"' tests/fixtures/property_analysis/ | wc -l   # must be 0

# 3. Full project suite green
uv run pytest tests/ -x

# 4. Citation-coverage meta-test still green (no fixture renames broke the glob)
uv run pytest tests/test_rules/test_citation_coverage.py -x

# 5. mypy strict on the wired-in file
uv run mypy --strict lib/property_analysis.py
```

All 5 must succeed. After this plan, every Phase 16 deliverable is shipped:
- REF-09 closed (PMI YAML + predicate + tests + wire-in + fixture re-anchor)
- REF-10 closed (insurance YAML + predicate + tests + wire-in + fixture re-anchor)
- ROADMAP SC #1, #2, #3, #5 closed
- ROADMAP SC #4 (no loader changes) trivially satisfied (Phase 16 never touched `_loader.py`)
- Every D-16-* decision implemented
- Every RESEARCH correction honored
</verification>

<success_criteria>
- All 7 literal-string assertions in `tests/test_property_analysis.py` rewritten to the prefix-match pattern `r.startswith("PMI-RATE-ESTIMATED-MGIC-")` (cell tag) OR exact-equals `w == "PMI-RATE-ESTIMATED-MGIC"` (warning label)
- `condo_with_hoa_seattle.json` re-anchored across all 5 hit sites (L6, L8, L80, L96, L126): `eligible_reasons` arrays at L80 + L96 carry the new parameterized tag; top-level `warnings` array at L126 carries the new aggregator label `"PMI-RATE-ESTIMATED-MGIC"`; `notes` (L6) + `_meta.citation` (L8) prose updated; all PMI-dependent dollar values (monthly_mi, PITI, DTI, stressed_DTI) recomputed against the new YAML rate; cascade-branch invariant preserved per Step 4 default (Option A: pick a different DP cell) OR Option B (adjust household profile) — with the choice documented in 16-04-SUMMARY.md. Option C (verdict flip + downstream test updates) requires returning to planning and is NOT taken executor-side.
- `sfh_conforming_king_county.json` and `sfh_jumbo_bay_area.json` warnings arrays updated to `["PMI-RATE-ESTIMATED-MGIC"]`
- `grep -rc "PMI-RATE-ESTIMATED-0.0075" lib/ tests/ data/` returns 0 (codebase-wide search-and-destroy complete)
- `grep -rE '"PMI-RATE-ESTIMATED"' tests/fixtures/property_analysis/` returns 0 (the bare aggregator label is gone from every fixture; all hits carry the `-MGIC` suffix)
- `uv run pytest tests/ -x` exits 0
- `uv run pytest tests/test_rules/test_citation_coverage.py -x` exits 0
- mypy clean on `lib/property_analysis.py`
- D-16-PMI-03 reason-code retirement closed in code (retired literal gone; new patterns active)
- Every Phase 16 ROADMAP SC verifiable via shipped tests
</success_criteria>

<output>
After completion, create `.planning/phases/16-reference-data/16-04-SUMMARY.md` documenting:
- Final verification: `grep -rc "PMI-RATE-ESTIMATED-0.0075" lib/ tests/ data/` returns 0 AND `grep -rE '"PMI-RATE-ESTIMATED"' tests/fixtures/property_analysis/` returns 0
- Full test count: `uv run pytest tests/ --collect-only -q | tail -1` — record both the count and the green-suite size
- The exact new tag string used in `condo_with_hoa_seattle.json` (depends on the household's FICO band and the cell's LTV — `PMI-RATE-ESTIMATED-MGIC-{ltv_band}-{fico_band}`)
- All 5 condo fixture hit sites enumerated with their pre/post values: L6 (notes prose), L8 (_meta.citation), L80 (Conv30 eligible_reasons), L96 (Conv15 eligible_reasons), L126 (top-level warnings)
- The new MGIC rate value applied to condo_with_hoa_seattle.json + the recomputed PITI/DTI/cascade chain
- Step 4 cascade-branch decision: which option (A: different DP cell, B: profile adjustment) was taken AND why; OR if neither was feasible, the planner-escalation marker that was surfaced.
- Phase 16 retrospective bullets for `.planning/RETROSPECTIVE.md`:
  - What worked: 4-plan decomposition isolated the 4 surfaces (YAML+predicate, PMI wire-in, insurance wire-in, search-and-destroy) cleanly
  - What broke: any unexpected cascade flip in condo_with_hoa_seattle.json
  - Pattern established: ship YAML + predicate first (Wave 0); wire in each call site in its own plan (Wave 1 + Wave 2, sequential because they share a file); search-and-destroy retired strings last (Wave 3)
- Phase 16 closed; v1.1 milestone progress now at 66.7% (4 of 6 phases complete; Phases 17 + 18 remain)
</output>
