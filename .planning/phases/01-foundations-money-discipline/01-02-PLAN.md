---
phase: 01-foundations-money-discipline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - DATA_CONTRACT.md
  - config/household.example.yml
  - config/profile.example.yml
autonomous: true
requirements:
  - FND-07
must_haves:
  truths:
    - "DATA_CONTRACT.md declares all four layers (User / System / Data / Reference) with explicit path lists"
    - "config/household.example.yml exists as a redacted skeleton with no real PII"
    - "config/profile.example.yml exists as a redacted skeleton with no real PII"
    - "DATA_CONTRACT.md cites scripts/hooks/block-user-layer.py as the enforcement mechanism (forward reference to Plan 06)"
  artifacts:
    - path: DATA_CONTRACT.md
      provides: "Four-layer data contract; the human-readable spec the Plan 06 hook enforces"
      contains: "## User Layer"
      min_lines: 50
    - path: config/household.example.yml
      provides: "Schema skeleton for User Layer household.yml (real file gitignored, never committed)"
      contains: "household:"
    - path: config/profile.example.yml
      provides: "Schema skeleton for User Layer profile.yml"
      contains: "profile:"
  key_links:
    - from: DATA_CONTRACT.md
      to: scripts/hooks/block-user-layer.py
      via: "User Layer enforcement citation"
      pattern: "block-user-layer\\.py"
    - from: DATA_CONTRACT.md
      to: .gitignore
      via: "User Layer paths must match .gitignore entries (Plan 06)"
      pattern: "config/household\\.yml"
---

<objective>
Author the human-readable data contract (DATA_CONTRACT.md) and the two committed example skeletons for the User Layer (`config/household.example.yml`, `config/profile.example.yml`). Implements FND-07. Ships the spec that Plan 06 enforces via `scripts/hooks/block-user-layer.py` and `.gitignore`.

Purpose: Establish the User / System / Data / Reference layer taxonomy as the project's canonical naming and the explicit list of paths that belong to each layer. Phase 1 must commit the spec before Plan 06 commits the enforcement (otherwise the hook has nothing authoritative to cite).

Output: DATA_CONTRACT.md (the four-layer markdown spec) + config/household.example.yml + config/profile.example.yml (committed redacted skeletons).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/cujo253/Documents/mortgage-ops/CLAUDE.md
@/Users/cujo253/Documents/mortgage-ops/.planning/REQUIREMENTS.md
@/Users/cujo253/Documents/mortgage-ops/.planning/phases/01-foundations-money-discipline/01-RESEARCH.md
@/Users/cujo253/Documents/mortgage-ops/.planning/phases/01-foundations-money-discipline/01-PATTERNS.md
@/Users/cujo253/Documents/career-ops/DATA_CONTRACT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write DATA_CONTRACT.md with four-layer taxonomy</name>
  <files>DATA_CONTRACT.md</files>
  <read_first>
    - /Users/cujo253/Documents/mortgage-ops/.planning/phases/01-foundations-money-discipline/01-RESEARCH.md (Pattern 8 lines 742-799 — full DATA_CONTRACT outline; CLAUDE.md Data Contract section lines 66-71 cited at lines 33)
    - /Users/cujo253/Documents/mortgage-ops/.planning/phases/01-foundations-money-discipline/01-PATTERNS.md (DATA_CONTRACT.md row lines 242-263; Convention 6 line 379)
    - /Users/cujo253/Documents/career-ops/DATA_CONTRACT.md (sibling soft analog — read for shape only; do NOT copy career-ops content like cv.md / portals.yml)
    - /Users/cujo253/Documents/mortgage-ops/CLAUDE.md (Data Contract conventions block)
  </read_first>
  <action>
Create `/Users/cujo253/Documents/mortgage-ops/DATA_CONTRACT.md` with EXACTLY this content (this is the authoritative Phase 1 spec, lifted from RESEARCH.md Pattern 8 lines 748-799 and adapted to mortgage-ops's four-layer split):

```markdown
# Data Contract

This document defines which files belong to the **User Layer** (read-only from system code), the **System Layer** (auto-updatable code & instructions), the **Data Layer** (generated artifacts), and the **Reference Layer** (committed regulatory data, manually refreshed).

The contract is enforced by:

1. `.gitignore` — blocks `git add` for User Layer and Data Layer paths.
2. `scripts/hooks/block-user-layer.py` (pre-commit hook, see `.pre-commit-config.yaml`) — refuses any commit that stages a User Layer path, including `git add -f` bypasses of `.gitignore`.
3. CI re-runs the same hook (server-side belt-and-suspenders for `--no-verify` bypasses).

## User Layer (NEVER auto-updated; gitignored)

These files contain personal data, customizations, and computed user-private state. **No system process — pre-commit hooks, CI, scripts, or future Claude skills — may write to a User Layer path.** Runtime enforcement is each script's responsibility; commit-time enforcement is the pre-commit hook + `.gitignore`.

| Path | Purpose |
|------|---------|
| `config/household.yml` | Household income, applicants, joint-applicant credit scores, monthly debts, location |
| `config/profile.yml` | User identity, preferences (mortgage display conventions, default loan term, etc.) |
| `modes/_profile.md` | (Phase 10) user-specific narrative overrides for the Claude skill |
| `data/mortgage-ops.duckdb` | (Phase 9) computed scenarios + reports |
| `data/mortgage-ops.duckdb-wal` | (Phase 9) DuckDB write-ahead log sidecar |
| `data/mortgage-ops.duckdb-shm` | (Phase 9) DuckDB shared-memory sidecar |
| `reports/*.md` | (Phase 10+) generated reports — except `reports/.gitkeep`, which seams the directory |

**Rule:** User Layer files are *read* by system code (e.g., `lib/affordability.py` will read `config/household.yml` in Phase 4) but are *never written* by system code. Annual data refresh is a manual user action (edit `config/household.yml` by hand).

## System Layer (auto-updatable; committed)

| Path | Purpose |
|------|---------|
| `lib/**` | Python calc engine (Phases 1–8) |
| `scripts/**` | CLI helpers (Phase 3+) and tooling hooks (Phase 1) |
| `tests/**` | Test suite + fixtures |
| `pyproject.toml` / `uv.lock` / `.python-version` | Build + deps |
| `.github/workflows/**` | CI |
| `.pre-commit-config.yaml` | Hook config |
| `CLAUDE.md` / `DATA_CONTRACT.md` / `README.md` | Project docs |
| `config/household.example.yml` / `config/profile.example.yml` | Schema skeletons (no real values) |
| `orchestration/**` | (Phase 9) Node DuckDB writer + lockfile |
| `.claude/skills/mortgage-ops/**` | (Phase 10) skill bundle (excluding `modes/_profile.md`) |
| `.claude/agents/**` | (Phase 11) subagent definitions |
| `evals/**` | (Phase 12) eval harness |

System Layer files are auto-updatable by Claude / scripts / CI. Commits to these paths are the normal flow.

## Data Layer (generated; gitignored)

| Path | Purpose |
|------|---------|
| `data/mortgage-ops.duckdb` | (Phase 9) single-file persistence — also a User Layer file by virtue of containing user-private scenarios |
| `data/market/*.parquet` | (Phase 12) FRED rate cache |
| `reports/{###}-{slug}-{YYYY-MM-DD}.md` | (Phase 10+) generated reports |

**Rule:** Data Layer files are regenerated from System Layer code + User Layer inputs + Reference Layer YAMLs. They are never hand-edited; an out-of-band edit will be overwritten by the next regeneration. The DuckDB file is also enumerated in the User Layer because it contains user-private numeric state — both layer rules apply (gitignored AND blocker rejects `git add -f`).

## Reference Layer (committed; manually refreshed annually)

| Path | Purpose |
|------|---------|
| `data/reference/conforming-limits-{YEAR}.yml` | (Phase 2) FHFA baseline + ceiling + per-county lookup |
| `data/reference/fha-limits-{YEAR}.yml` | (Phase 2) FHA floor/ceiling + per-county lookup |
| `data/reference/fha-mip-rates.yml` | (Phase 2) FHA UFMIP + annual MIP per term/LTV/loan-amount tier |
| `data/reference/va-funding-fees.yml` | (Phase 2) VA funding fee tables |
| `data/reference/va-residual-income.yml` | (Phase 2) VA residual income geographic × family-size table |
| `data/reference/usda-income-limits.yml` | (Phase 2) USDA 115%-of-area-median thresholds |
| `data/reference/irs-pub936.yml` | (Phase 2) IRS Pub 936 mortgage interest deduction caps |
| `data/known-loans.yml` | (Phase 9) product catalog |

**Rule:** Reference Layer files are committed and human-readable; each must include `source:` (URL of the regulatory page) and `effective:` (ISO-8601 date). Annual refresh = YAML edit + commit, never code change. A startup-time staleness check (Phase 2) warns when `effective:` is > 12 months old.

## Layer Cross-References

- The User Layer paths in this document must match the `USER_LAYER_PATTERNS` and `USER_LAYER_GLOB_DIRS` tuples in `scripts/hooks/block-user-layer.py` exactly. Both lists are kept in sync by editing this file and the hook source in the same commit.
- The User Layer paths must also be enumerated in `.gitignore`. The seam files (`reports/.gitkeep`, `data/reference/.gitkeep`) are explicitly whitelisted (`!path/.gitkeep`) so the seam directories remain tracked.
- Phase 1 commits this contract and the empty `data/reference/` directory (with `.gitkeep`); Reference Layer YAML files are added in Phase 2.
```

This is FND-07's deliverable: a four-layer User / System / Data / Reference declaration that the rest of the project (and especially Plan 06's enforcement) can cite. The path lists in the User Layer table MUST match the patterns in Plan 06's `block-user-layer.py` exactly (Convention 6, PATTERNS.md line 379) — Plan 06 reads from this file when building its `USER_LAYER_PATTERNS` tuple.
  </action>
  <verify>
    <automated>test -f DATA_CONTRACT.md && grep -q '^# Data Contract$' DATA_CONTRACT.md && grep -q '^## User Layer' DATA_CONTRACT.md && grep -q '^## System Layer' DATA_CONTRACT.md && grep -q '^## Data Layer' DATA_CONTRACT.md && grep -q '^## Reference Layer' DATA_CONTRACT.md && grep -q '`config/household.yml`' DATA_CONTRACT.md && grep -q '`config/profile.yml`' DATA_CONTRACT.md && grep -q 'block-user-layer\.py' DATA_CONTRACT.md && [ "$(wc -l < DATA_CONTRACT.md)" -ge 50 ]</automated>
  </verify>
  <acceptance_criteria>
- `DATA_CONTRACT.md` exists at repo root
- Contains exactly four `## User Layer`, `## System Layer`, `## Data Layer`, `## Reference Layer` H2 headings (verified by `grep -c '^## .* Layer'` returning at least 4)
- Lists `config/household.yml` and `config/profile.yml` and `modes/_profile.md` and `data/mortgage-ops.duckdb` under the User Layer table (each as a distinct backticked path)
- References the enforcement mechanism `scripts/hooks/block-user-layer.py` by name (forward reference to Plan 06)
- Document is at least 50 lines (this is non-trivial spec content, not a stub)
- No real PII anywhere in the file (no real names, addresses, account numbers, SSN-like patterns)
  </acceptance_criteria>
  <done>DATA_CONTRACT.md committed; the spec Plan 06 will enforce is in place; the four-layer taxonomy is locked.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write config/household.example.yml + config/profile.example.yml redacted skeletons</name>
  <files>config/household.example.yml, config/profile.example.yml</files>
  <read_first>
    - /Users/cujo253/Documents/mortgage-ops/.planning/phases/01-foundations-money-discipline/01-RESEARCH.md (project structure lines 219-220 + Open Question lines around config example; A3 lines 1188-1189 — Phase 1 commits a stub, Phase 4 fleshes out)
    - /Users/cujo253/Documents/mortgage-ops/.planning/phases/01-foundations-money-discipline/01-PATTERNS.md (config/household.example.yml row lines 290-294; Convention 7 line 380)
    - /Users/cujo253/Documents/mortgage-ops/.planning/REQUIREMENTS.md (AFFD-09 — Phase 4 will document full schema; FND-07/FND-08 — Phase 1 commits the example, gitignores the live file)
    - /Users/cujo253/Documents/mortgage-ops/DATA_CONTRACT.md (just-written; the live file paths must match)
  </read_first>
  <action>
Create two redacted skeleton YAML files. These are committed (in System Layer); the live `config/household.yml` and `config/profile.yml` are User Layer (never committed). The redacted skeletons document the schema shape only — every value is an obvious placeholder, no real PII.

**1. `config/household.example.yml`** — write EXACTLY:

```yaml
# config/household.example.yml
#
# COMMITTED SKELETON — copy to config/household.yml and fill in your real values.
# config/household.yml is in the User Layer (per DATA_CONTRACT.md): gitignored
# and never auto-updated by any system process.
#
# Phase 4 (Affordability) consumes this schema. Phase 1 ships only this redacted
# example so DATA_CONTRACT.md can cite a real path. Fields here are placeholders;
# Phase 4 (AFFD-09) will document the full schema with units and constraints.

household:
  # Location — used by Phase 2 rules predicates (loan_type classification needs county).
  location:
    state: "WA"
    county: "King"            # Must match a county in data/reference/conforming-limits-*.yml (Phase 2)
    zip: "00000"

  # Joint applicants — Phase 4 supports two-income households.
  applicants:
    - name: "Applicant A"     # Display name only, not a legal identifier
      gross_monthly_income: "0.00"   # Decimal string — see CLAUDE.md money discipline
      credit_score: 0                # FICO 300-850; Phase 4 picks the lower-mid score per Fannie/Freddie convention
    - name: "Applicant B"
      gross_monthly_income: "0.00"
      credit_score: 0

  # Monthly debts that count toward back-end DTI.
  monthly_debts:
    auto: "0.00"
    student_loans: "0.00"
    credit_cards: "0.00"
    other: "0.00"

  # Optional: existing housing payment (rent/own) for affordability comparison.
  current_housing_payment: "0.00"
```

**2. `config/profile.example.yml`** — write EXACTLY:

```yaml
# config/profile.example.yml
#
# COMMITTED SKELETON — copy to config/profile.yml and fill in your real values.
# config/profile.yml is in the User Layer (per DATA_CONTRACT.md): gitignored
# and never auto-updated by any system process.
#
# Phase 10 (Claude Skill Frontend) consumes this schema. Phase 1 ships only this
# redacted example so DATA_CONTRACT.md can cite a real path.

profile:
  # Display preferences for generated reports and skill output.
  display:
    money_format: "USD"         # Reserved; Phase 10 uses this
    rate_format: "percent"      # "percent" => 6.5%, "decimal" => 0.065
    decimal_places_money: 2
    decimal_places_rate: 4

  # Default assumptions for "what if" runs when the user does not specify.
  defaults:
    term_months: 360            # 30-year default
    rate_source: "FRED:MORTGAGE30US"  # Phase 12 FRED MCP (LIVE-01)
    extra_principal_monthly: "0.00"

  # Per-mode behavior overrides (Phase 10).
  modes:
    affordability:
      max_back_end_dti: "0.43"
    refinance:
      assumed_holding_period_months: 60
```

These files are **System Layer** (committed, schema reference). The live `config/household.yml` and `config/profile.yml` are **User Layer** (gitignored in Plan 06 + blocked by the hook in Plan 06). PATTERNS.md Convention 7 (line 380): "Any user-layer YAML ships a committed `.example.yml` skeleton with redacted/dummy values; the real file is gitignored and never committed." Per A3 (RESEARCH.md line 1188-1189), Phase 4 (AFFD-09) will flesh out the schema; Phase 1 only ships the seam.

Critical: every numeric value is a placeholder zero or sentinel. No real income, no real ZIP, no real names, no real credit scores. The file should be obviously a template, not actual user data.
  </action>
  <verify>
    <automated>test -f config/household.example.yml && test -f config/profile.example.yml && grep -q '^household:$' config/household.example.yml && grep -q '^  applicants:$' config/household.example.yml && grep -q '^profile:$' config/profile.example.yml && grep -q 'COMMITTED SKELETON' config/household.example.yml && grep -q 'COMMITTED SKELETON' config/profile.example.yml && uv run python -c "import yaml; yaml.safe_load(open('config/household.example.yml')); yaml.safe_load(open('config/profile.example.yml')); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
- `config/household.example.yml` exists, is valid YAML (`yaml.safe_load` succeeds), and contains top-level keys `household:` with sub-keys `location:`, `applicants:` (a list), `monthly_debts:`, and `current_housing_payment`
- `config/profile.example.yml` exists, is valid YAML, and contains top-level key `profile:` with sub-keys `display:`, `defaults:`, and `modes:`
- Both files start with a comment block containing the literal string `COMMITTED SKELETON` and reference DATA_CONTRACT.md
- All money values in both files are Decimal-safe strings (e.g., `"0.00"`), not floats or ints — verifies the money-discipline convention even in placeholders
- No real PII: no real names (only `"Applicant A"`/`"Applicant B"`), no real ZIP codes (`"00000"` placeholder), no real credit scores (`0` placeholder), no SSN-like patterns
- `config/household.yml` and `config/profile.yml` (the live files) DO NOT exist at this point — only the `.example.yml` skeletons
  </acceptance_criteria>
  <done>Two committed example skeletons exist; users can `cp config/household.example.yml config/household.yml` and edit; the live files will be gitignored + hook-blocked in Plan 06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Layer ↔ System Layer | The DATA_CONTRACT.md spec defines the boundary; commits crossing it (e.g., committing a real `config/household.yml`) are forbidden |
| Committed example ↔ live user file | `config/*.example.yml` is committed; `config/*.yml` is User Layer; mistaking one for the other leaks PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-05 | I (Information Disclosure) | config/household.example.yml | medium | mitigate | All numeric placeholders are `"0.00"` strings or sentinel values like `"00000"` ZIP / `0` credit_score; no real PII; obvious template comments at file head; Plan 06's `.gitignore` excludes the live `config/household.yml` so a future user-edited copy cannot be accidentally committed |
| T-1-06 | I (Information Disclosure) | config/profile.example.yml | low | mitigate | Same approach as household: redacted comments, no real preferences, only schema shape; live `config/profile.yml` gitignored in Plan 06 |
| T-1-07 | T (Tampering) | DATA_CONTRACT.md drift vs hook source | medium | mitigate | Convention 6 (PATTERNS.md line 379): User Layer paths in DATA_CONTRACT.md MUST match `USER_LAYER_PATTERNS` in Plan 06's `block-user-layer.py`. Plan 06's task explicitly reads DATA_CONTRACT.md before authoring the hook. Drift would fail Plan 06's acceptance criteria |
| T-1-08 | I (Information Disclosure) | DATA_CONTRACT.md itself | low | accept | Spec is meant to be public-readable; lists path conventions but no actual user data. Risk: zero |
</threat_model>

<verification>
Both files commit and the Wave-1 phase gate continues to pass:

```bash
uv run ruff check . && uv run ruff format --check . && uv run mypy --strict . && uv run pytest
```

(YAML files are not in scope for ruff/mypy; they are validated only by `yaml.safe_load` in the verify command.)
</verification>

<success_criteria>
- `DATA_CONTRACT.md` exists at repo root with all four layer sections (User / System / Data / Reference)
- DATA_CONTRACT.md cites `scripts/hooks/block-user-layer.py` as the enforcement mechanism (forward reference Plan 06 will satisfy)
- DATA_CONTRACT.md path lists are internally consistent — every path mentioned in a layer table is unambiguously assigned to that layer
- `config/household.example.yml` and `config/profile.example.yml` exist as committed redacted skeletons
- Both example YAMLs parse via `yaml.safe_load` and contain no real PII
- The phase-gate command (`uv run ruff check . && uv run ruff format --check . && uv run mypy --strict . && uv run pytest`) still exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-money-discipline/01-02-SUMMARY.md` documenting:
- The exact User Layer path list shipped in DATA_CONTRACT.md (must match Plan 06's hook source verbatim — call out the synchronization requirement)
- That `config/household.example.yml` and `config/profile.example.yml` are System Layer (committed); the live `config/household.yml` and `config/profile.yml` are User Layer (Plan 06 will gitignore + block)
- Forward references for Plan 06 to satisfy: gitignore entries, hook patterns, CI re-run of the hook
</output>
