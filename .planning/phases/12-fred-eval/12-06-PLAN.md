---
phase: 12-fred-eval
plan: 06
type: execute
wave: 6
depends_on: [12-05]
files_modified:
  - evals/expected/evaluate-01.json
  - evals/expected/evaluate-02.json
  - evals/expected/evaluate-03.json
  - evals/expected/compare-01.json
  - evals/expected/compare-02.json
  - evals/expected/compare-03.json
  - evals/expected/refinance-01.json
  - evals/expected/refinance-02.json
  - evals/expected/refinance-03.json
  - evals/expected/affordability-01.json
  - evals/expected/affordability-02.json
  - evals/expected/affordability-03.json
  - evals/expected/stress-01.json
  - evals/expected/stress-02.json
  - evals/expected/stress-03.json
  - evals/expected/amortize-01.json
  - evals/expected/amortize-02.json
  - evals/expected/amortize-03.json
  - evals/expected/arm-01.json
  - evals/expected/arm-02.json
  - evals/expected/arm-03.json
  - evals/expected/live-rate-injection-01.json
  - tests/test_evals_runner.py
autonomous: true
requirements: [EVAL-02]
tags: [phase-12, wave-6, eval-oracles, paired-expected, sc-3-sc-4-close]
must_haves:
  truths:
    - "evals/expected/ contains exactly 22 JSON files (one per prompt, paired by stem)"
    - "13 oracles are anchored (numeric_status='anchored' + non-empty expected_numbers); 9 are skipped (numeric_status='skip')"
    - "live-rate-injection-01.json pins to fixture cache value (6.50 per Plan 12-05 fixture)"
    - "Every oracle has schema_version + id + mode + expected_route_keywords + expected_scripts + expected_numbers + v1_frozen_at"
    - "Running `python -m evals.runner` exits 0 (numeric_match_rate >= 0.95) on the v1 set: 13/(13+0) = 100%"
    - "Citation-coverage meta-test asserts every expected_number with provenance='stdout' has a source_script field pointing at an existing .claude/skills/mortgage-ops/scripts/*.py"
  artifacts:
    - path: "evals/expected/"
      provides: "22 oracle JSON files (1:1 paired with evals/prompts/*.md)"
    - path: "evals/expected/live-rate-injection-01.json"
      provides: "Anchors to fixture cache MORTGAGE30US=6.50 with provenance: static"
      contains: "static"
  key_links:
    - from: "evals/expected/{id}.json"
      to: "evals/prompts/{id}.md"
      via: "id-stem 1:1 match (Plan 12-04 runner enforces)"
      pattern: "id-stem"
    - from: "evals/expected/*.json provenance:stdout entries"
      to: ".claude/skills/mortgage-ops/scripts/*.py"
      via: "source_script field — verified by citation-coverage meta-test"
      pattern: "source_script"
---

<objective>
Ship paired oracle JSON files for all 22 prompts from Plan 12-05. Each oracle is the authoritative machine-readable expected output: numbers, route keywords, script invocations. Together with Plan 12-04's runner + Plan 12-05's prompts, this closes EVAL-02 + EVAL-03 + EVAL-04 + SC-3 + SC-4 + SC-5 at the end-to-end layer.

Purpose: lock the gate. After this plan ships, `python -m evals.runner` returns exit 0 (13/(13+0) = 100% ≥ 95%) on the v1 set, and any future regression that drops an anchored prompt out of PASS bucket flips the gate to FAIL.

Output: 22 oracle JSON files + flip the last xfail in `tests/test_evals_runner.py` + 1 new citation-coverage meta-test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fred-eval/12-CONTEXT.md
@.planning/phases/12-fred-eval/12-RESEARCH.md
@.planning/phases/12-fred-eval/12-PATTERNS.md
@.planning/phases/12-fred-eval/12-05-SUMMARY.md
@CLAUDE.md
@evals/prompts/
@evals/runner.py
@evals/metrics.py

<interfaces>
Oracle JSON shape (PINNED by RESEARCH §Pattern 4 lines 338-368 + D-12-SC4-01 skip extension):

ANCHORED oracle:
```json
{
  "schema_version": 1,
  "id": "evaluate-01",
  "mode": "evaluate",
  "numeric_status": "anchored",
  "expected_scripts": [
    {"script": "amortize.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [
    {"label": "monthly_pi", "value": "1264.14", "tolerance": "0.005",
     "source_script": "amortize.py", "provenance": "stdout"}
  ],
  "expected_route_keywords": ["evaluate", "amortize.py"],
  "v1_frozen_at": "2026-05-10"
}
```

TBD oracle:
```json
{
  "schema_version": 1,
  "id": "refinance-02",
  "mode": "refinance",
  "numeric_status": "skip",
  "defer_until_phase": "13.0",
  "expected_scripts": [
    {"script": "refi_npv.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [],
  "expected_route_keywords": ["refinance", "refi_npv.py"],
  "v1_frozen_at": "2026-05-10"
}
```

Rules:
- `id` field MUST equal the filename stem.
- `numeric_status` is one of "anchored" | "skip".
- `value` and `tolerance` are JSON STRINGS (D-19 money discipline; runner Decimal-parses).
- For anchored oracles, `expected_numbers` MUST mirror the prompt frontmatter byte-for-byte (label + value + tolerance + source_script + provenance).
- `expected_scripts` mirrors the prompt frontmatter.
- `expected_route_keywords` mirrors the prompt frontmatter.
- `v1_frozen_at: "2026-05-10"` — uniform date across all 22 oracles for v1 ship (per RESEARCH §Pattern 4 line 361 idiom).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship 22 paired oracle JSON files (13 anchored + 9 skipped) mirroring Plan 12-05 prompt frontmatter</name>
  <files>
    evals/expected/evaluate-01.json,
    evals/expected/evaluate-02.json,
    evals/expected/evaluate-03.json,
    evals/expected/compare-01.json,
    evals/expected/compare-02.json,
    evals/expected/compare-03.json,
    evals/expected/refinance-01.json,
    evals/expected/refinance-02.json,
    evals/expected/refinance-03.json,
    evals/expected/affordability-01.json,
    evals/expected/affordability-02.json,
    evals/expected/affordability-03.json,
    evals/expected/stress-01.json,
    evals/expected/stress-02.json,
    evals/expected/stress-03.json,
    evals/expected/amortize-01.json,
    evals/expected/amortize-02.json,
    evals/expected/amortize-03.json,
    evals/expected/arm-01.json,
    evals/expected/arm-02.json,
    evals/expected/arm-03.json,
    evals/expected/live-rate-injection-01.json
  </files>
  <read_first>
    evals/prompts/*.md (ALL 22 — each oracle mirrors its paired prompt's frontmatter),
    .planning/phases/12-fred-eval/12-PATTERNS.md lines 421-454 (PINNED oracle shape),
    .planning/phases/12-fred-eval/12-RESEARCH.md §Pattern 4 lines 338-368 (oracle conventions)
  </read_first>
  <action>
For each of the 22 prompts under `evals/prompts/`, create the paired oracle JSON file under `evals/expected/`. The oracle is a JSON mirror of the prompt's frontmatter PLUS the `schema_version`, `numeric_status`, and `v1_frozen_at` fields.

**Convention: every oracle uses `v1_frozen_at: "2026-05-10"`.**

**Generate-from-prompt rule:** for each prompt file `evals/prompts/{id}.md`:
1. Parse its frontmatter via python-frontmatter.
2. If `numeric_status: skip` is present in the frontmatter → oracle's `numeric_status` is `"skip"` and `expected_numbers` is `[]`.
3. Otherwise → oracle's `numeric_status` is `"anchored"` and `expected_numbers` mirrors the prompt's `expected_numbers` byte-for-byte.
4. `id` field MUST equal `{id}` (filename stem).
5. Write each oracle as pretty-printed JSON (`json.dumps(..., indent=2)`).

Use the Write tool for each file. Below are the 22 expected oracle contents:

**File 1: `evals/expected/evaluate-01.json`** (anchored):
```json
{
  "schema_version": 1,
  "id": "evaluate-01",
  "mode": "evaluate",
  "numeric_status": "anchored",
  "expected_scripts": [
    {"script": "amortize.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [
    {"label": "monthly_pi", "value": "1264.14", "tolerance": "0.005",
     "source_script": "amortize.py", "provenance": "stdout"}
  ],
  "expected_route_keywords": ["evaluate", "amortize.py"],
  "v1_frozen_at": "2026-05-10"
}
```

**File 2: `evals/expected/evaluate-02.json`** (anchored):
```json
{
  "schema_version": 1,
  "id": "evaluate-02",
  "mode": "evaluate",
  "numeric_status": "anchored",
  "expected_scripts": [
    {"script": "amortize.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [
    {"label": "monthly_pi", "value": "2528.27", "tolerance": "0.005",
     "source_script": "amortize.py", "provenance": "stdout"}
  ],
  "expected_route_keywords": ["evaluate", "amortize.py"],
  "v1_frozen_at": "2026-05-10"
}
```

**File 3: `evals/expected/evaluate-03.json`** (skip):
```json
{
  "schema_version": 1,
  "id": "evaluate-03",
  "mode": "evaluate",
  "numeric_status": "skip",
  "defer_until_phase": "13.0",
  "expected_scripts": [
    {"script": "apr_reg_z.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [],
  "expected_route_keywords": ["evaluate", "apr_reg_z.py"],
  "v1_frozen_at": "2026-05-10"
}
```

**File 4: `evals/expected/compare-01.json`** (anchored):
```json
{
  "schema_version": 1,
  "id": "compare-01",
  "mode": "compare",
  "numeric_status": "anchored",
  "expected_scripts": [
    {"script": "amortize.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [
    {"label": "offer_a_monthly_pi", "value": "2528.27", "tolerance": "0.005",
     "source_script": "amortize.py", "provenance": "stdout"},
    {"label": "offer_b_monthly_pi", "value": "1797.66", "tolerance": "0.005",
     "source_script": "amortize.py", "provenance": "stdout"}
  ],
  "expected_route_keywords": ["compare", "amortize.py"],
  "v1_frozen_at": "2026-05-10"
}
```

**File 5: `evals/expected/compare-02.json`** (anchored):
```json
{
  "schema_version": 1,
  "id": "compare-02",
  "mode": "compare",
  "numeric_status": "anchored",
  "expected_scripts": [
    {"script": "amortize.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [
    {"label": "offer_a_monthly_pi", "value": "761.78", "tolerance": "0.005",
     "source_script": "amortize.py", "provenance": "stdout"},
    {"label": "offer_b_monthly_pi", "value": "1264.14", "tolerance": "0.005",
     "source_script": "amortize.py", "provenance": "stdout"}
  ],
  "expected_route_keywords": ["compare", "amortize.py"],
  "v1_frozen_at": "2026-05-10"
}
```

**File 6: `evals/expected/compare-03.json`** (skip):
```json
{
  "schema_version": 1,
  "id": "compare-03",
  "mode": "compare",
  "numeric_status": "skip",
  "defer_until_phase": "13.0",
  "expected_scripts": [
    {"script": "refi_npv.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [],
  "expected_route_keywords": ["compare", "refi_npv.py", "amortize.py"],
  "v1_frozen_at": "2026-05-10"
}
```

**File 7: `evals/expected/refinance-01.json`** (anchored — Phase 6 fixture):
```json
{
  "schema_version": 1,
  "id": "refinance-01",
  "mode": "refinance",
  "numeric_status": "anchored",
  "expected_scripts": [
    {"script": "refi_npv.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [
    {"label": "npv", "value": "60705.48", "tolerance": "0.01",
     "source_script": "refi_npv.py", "provenance": "stdout"}
  ],
  "expected_route_keywords": ["refinance", "refi_npv.py"],
  "v1_frozen_at": "2026-05-10"
}
```

**File 8: `evals/expected/refinance-02.json`** (skip):
```json
{
  "schema_version": 1,
  "id": "refinance-02",
  "mode": "refinance",
  "numeric_status": "skip",
  "defer_until_phase": "13.0",
  "expected_scripts": [
    {"script": "refi_npv.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [],
  "expected_route_keywords": ["refinance", "refi_npv.py"],
  "v1_frozen_at": "2026-05-10"
}
```

**File 9: `evals/expected/refinance-03.json`** (skip):
```json
{
  "schema_version": 1,
  "id": "refinance-03",
  "mode": "refinance",
  "numeric_status": "skip",
  "defer_until_phase": "13.0",
  "expected_scripts": [
    {"script": "refi_npv.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [],
  "expected_route_keywords": ["refinance", "refi_npv.py"],
  "v1_frozen_at": "2026-05-10"
}
```

**File 10: `evals/expected/affordability-01.json`** (anchored):
```json
{
  "schema_version": 1,
  "id": "affordability-01",
  "mode": "affordability",
  "numeric_status": "anchored",
  "expected_scripts": [
    {"script": "affordability.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [
    {"label": "monthly_pi", "value": "2528.27", "tolerance": "0.005",
     "source_script": "affordability.py", "provenance": "stdout"}
  ],
  "expected_route_keywords": ["affordability", "affordability.py"],
  "v1_frozen_at": "2026-05-10"
}
```

**File 11: `evals/expected/affordability-02.json`** (anchored — Phase 4 reverse round-trip):
```json
{
  "schema_version": 1,
  "id": "affordability-02",
  "mode": "affordability",
  "numeric_status": "anchored",
  "expected_scripts": [
    {"script": "affordability.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [
    {"label": "max_loan_amount", "value": "646322.54", "tolerance": "0.01",
     "source_script": "affordability.py", "provenance": "stdout"}
  ],
  "expected_route_keywords": ["affordability", "affordability.py"],
  "v1_frozen_at": "2026-05-10"
}
```

**File 12: `evals/expected/affordability-03.json`** (skip):
```json
{
  "schema_version": 1,
  "id": "affordability-03",
  "mode": "affordability",
  "numeric_status": "skip",
  "defer_until_phase": "13.0",
  "expected_scripts": [
    {"script": "affordability.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [],
  "expected_route_keywords": ["affordability", "affordability.py", "blocked_by"],
  "v1_frozen_at": "2026-05-10"
}
```

**File 13: `evals/expected/stress-01.json`** (anchored):
```json
{
  "schema_version": 1,
  "id": "stress-01",
  "mode": "stress",
  "numeric_status": "anchored",
  "expected_scripts": [
    {"script": "stress_test.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [
    {"label": "monthly_pi_at_6_5pct", "value": "2528.27", "tolerance": "0.005",
     "source_script": "stress_test.py", "provenance": "stdout"}
  ],
  "expected_route_keywords": ["stress", "stress_test.py"],
  "v1_frozen_at": "2026-05-10"
}
```

**File 14: `evals/expected/stress-02.json`** (skip):
```json
{
  "schema_version": 1,
  "id": "stress-02",
  "mode": "stress",
  "numeric_status": "skip",
  "defer_until_phase": "13.0",
  "expected_scripts": [
    {"script": "stress_test.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [],
  "expected_route_keywords": ["stress", "stress_test.py", "stress-test-agent"],
  "v1_frozen_at": "2026-05-10"
}
```

**File 15: `evals/expected/stress-03.json`** (skip):
```json
{
  "schema_version": 1,
  "id": "stress-03",
  "mode": "stress",
  "numeric_status": "skip",
  "defer_until_phase": "13.0",
  "expected_scripts": [
    {"script": "stress_test.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [],
  "expected_route_keywords": ["stress", "stress_test.py"],
  "v1_frozen_at": "2026-05-10"
}
```

**File 16: `evals/expected/amortize-01.json`** (anchored):
```json
{
  "schema_version": 1,
  "id": "amortize-01",
  "mode": "amortize",
  "numeric_status": "anchored",
  "expected_scripts": [
    {"script": "amortize.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [
    {"label": "monthly_pi", "value": "1264.14", "tolerance": "0.005",
     "source_script": "amortize.py", "provenance": "stdout"}
  ],
  "expected_route_keywords": ["amortize", "amortize.py"],
  "v1_frozen_at": "2026-05-10"
}
```

**File 17: `evals/expected/amortize-02.json`** (anchored):
```json
{
  "schema_version": 1,
  "id": "amortize-02",
  "mode": "amortize",
  "numeric_status": "anchored",
  "expected_scripts": [
    {"script": "amortize.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [
    {"label": "monthly_pi", "value": "1797.66", "tolerance": "0.005",
     "source_script": "amortize.py", "provenance": "stdout"}
  ],
  "expected_route_keywords": ["amortize", "amortize.py"],
  "v1_frozen_at": "2026-05-10"
}
```

**File 18: `evals/expected/amortize-03.json`** (anchored):
```json
{
  "schema_version": 1,
  "id": "amortize-03",
  "mode": "amortize",
  "numeric_status": "anchored",
  "expected_scripts": [
    {"script": "amortize.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [
    {"label": "monthly_pi", "value": "761.78", "tolerance": "0.005",
     "source_script": "amortize.py", "provenance": "stdout"}
  ],
  "expected_route_keywords": ["amortize", "amortize.py"],
  "v1_frozen_at": "2026-05-10"
}
```

**File 19: `evals/expected/arm-01.json`** (anchored):
```json
{
  "schema_version": 1,
  "id": "arm-01",
  "mode": "arm",
  "numeric_status": "anchored",
  "expected_scripts": [
    {"script": "arm_simulate.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [
    {"label": "initial_period_monthly_pi", "value": "2398.20", "tolerance": "0.01",
     "source_script": "arm_simulate.py", "provenance": "stdout"}
  ],
  "expected_route_keywords": ["arm", "arm_simulate.py"],
  "v1_frozen_at": "2026-05-10"
}
```

**File 20: `evals/expected/arm-02.json`** (skip):
```json
{
  "schema_version": 1,
  "id": "arm-02",
  "mode": "arm",
  "numeric_status": "skip",
  "defer_until_phase": "13.0",
  "expected_scripts": [
    {"script": "arm_simulate.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [],
  "expected_route_keywords": ["arm", "arm_simulate.py"],
  "v1_frozen_at": "2026-05-10"
}
```

**File 21: `evals/expected/arm-03.json`** (skip):
```json
{
  "schema_version": 1,
  "id": "arm-03",
  "mode": "arm",
  "numeric_status": "skip",
  "defer_until_phase": "13.0",
  "expected_scripts": [
    {"script": "arm_simulate.py", "args_must_include": ["--input"]}
  ],
  "expected_numbers": [],
  "expected_route_keywords": ["arm", "arm_simulate.py"],
  "v1_frozen_at": "2026-05-10"
}
```

**File 22: `evals/expected/live-rate-injection-01.json`** (anchored — static provenance):
```json
{
  "schema_version": 1,
  "id": "live-rate-injection-01",
  "mode": "evaluate",
  "numeric_status": "anchored",
  "expected_scripts": [],
  "expected_numbers": [
    {"label": "current_30yr_rate", "value": "6.50", "tolerance": "0.01",
     "source_script": "fixture_cache", "provenance": "static"}
  ],
  "expected_route_keywords": ["data/cache/fred_MORTGAGE30US.json", "6.50"],
  "v1_frozen_at": "2026-05-10"
}
```
  </action>
  <verify>
    <automated>
      test "$(ls evals/expected/*.json 2&gt;/dev/null | wc -l | tr -d ' ')" -eq 22 \
        &amp;&amp; uv run python -c 'import json; from pathlib import Path; orcs = list(Path("evals/expected").glob("*.json")); assert len(orcs) == 22; anchored = sum(1 for p in orcs if json.loads(p.read_text())["numeric_status"] == "anchored"); skipped = sum(1 for p in orcs if json.loads(p.read_text())["numeric_status"] == "skip"); assert anchored == 13, f"expected 13 anchored, got {anchored}"; assert skipped == 9, f"expected 9 skipped, got {skipped}"; print(f"anchored={anchored} skipped={skipped}")' \
        &amp;&amp; uv run python -c 'import json; from pathlib import Path; [json.loads(p.read_text()) for p in Path("evals/expected").glob("*.json")]' \
        &amp;&amp; uv run python -c 'import json; from pathlib import Path; ids_files = {p.stem for p in Path("evals/prompts").glob("*.md") if p.stem != ".gitkeep"}; ids_oracles = {p.stem for p in Path("evals/expected").glob("*.json")}; assert ids_files == ids_oracles, f"mismatch: only-prompts={ids_files - ids_oracles}, only-oracles={ids_oracles - ids_files}"; print("1:1 paired")'
    </automated>
  </verify>
  <done>
    22 oracle JSON files exist; 13 have `numeric_status: "anchored"` with non-empty `expected_numbers`; 9 have `numeric_status: "skip"` with empty `expected_numbers` + `defer_until_phase: "13.0"`; every prompt has a paired oracle (stem-matched 1:1); all 22 files parse cleanly as JSON; `live-rate-injection-01.json` has `provenance: "static"` for the rate entry.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip the final xfail + add citation-coverage meta-test + run end-to-end gate</name>
  <files>
    tests/test_evals_runner.py,
    tests/test_evals_coverage.py
  </files>
  <read_first>
    tests/test_evals_runner.py (the one remaining xfail: test_every_prompt_has_paired_oracle),
    evals/expected/ (now full of 22 oracles),
    evals/runner.py (run_all + main entrypoint behavior),
    .planning/phases/12-fred-eval/12-04-SUMMARY.md (gate semantics)
  </read_first>
  <behavior>
    - Final xfail in tests/test_evals_runner.py (`test_every_prompt_has_paired_oracle`) FLIPPED.
    - New test file `tests/test_evals_coverage.py` ships green with 3 citation-coverage tests:
      1. Every anchored expected_number with `provenance: stdout` has a `source_script` that exists at `.claude/skills/mortgage-ops/scripts/{source_script}` (file presence check).
      2. Every prompt's `mode` frontmatter matches its oracle's `mode` field.
      3. Running `evals.runner.run_all(PROMPTS_DIR)` produces a HarnessReport with `numeric_pass_count=13`, `numeric_fail_count=0`, `numeric_skip_count=9`, `numeric_match_rate=1.0` → gate PASSES.
  </behavior>
  <action>
**1. Flip the final xfail in `tests/test_evals_runner.py`** — remove `@pytest.mark.xfail(...)` from `test_every_prompt_has_paired_oracle`. After this, the module has 0 xfails.

Update module docstring: "Phase 12 Wave-6 complete: 22 prompts + 22 oracles + three-bucket gate end-to-end."

**2. Create `tests/test_evals_coverage.py`** — new file. 3 citation-coverage + end-to-end gate tests (no xfails):

```python
"""Phase 12 Wave-6 citation-coverage + end-to-end gate tests.

These three tests close ROADMAP SC-3 + SC-4 + SC-5 at the integration layer:

- test_every_stdout_provenance_has_existing_source_script — SC-3 cross-check that every
  anchored expected_number with provenance="stdout" cites a script that EXISTS in the
  skill bundle. Defends against oracle drift after Phase 10+ script-relocations.
- test_prompt_mode_matches_oracle_mode — schema consistency: prompt mode field MUST
  equal oracle mode field (Plan 12-05 + Plan 12-06 author them in parallel; this
  test pins the contract).
- test_runner_gate_passes_on_v1_set — SC-4 + D-12-SC4-01 end-to-end: `evals.runner.run_all`
  on the shipped 22-prompt set produces 13 pass / 0 fail / 9 skip, gate at 100% ≥ 95%.
"""

from __future__ import annotations

import json
from pathlib import Path

import frontmatter
import pytest

from evals.metrics import NumericScore
from evals.runner import EXPECTED_DIR, PROMPTS_DIR, run_all

SKILL_SCRIPTS_DIR: Path = (
    Path(__file__).resolve().parent.parent
    / ".claude" / "skills" / "mortgage-ops" / "scripts"
)


def _all_oracles() -> list[dict]:
    return [json.loads(p.read_text()) for p in sorted(EXPECTED_DIR.glob("*.json"))]


def test_every_stdout_provenance_has_existing_source_script() -> None:
    """SC-3: every anchored expected_number with provenance='stdout' MUST cite a script
    that exists under .claude/skills/mortgage-ops/scripts/. Defends against oracle drift."""
    failures: list[str] = []
    for oracle in _all_oracles():
        if oracle.get("numeric_status") != "anchored":
            continue
        for entry in oracle.get("expected_numbers", []) or []:
            if entry.get("provenance") != "stdout":
                continue
            src = entry.get("source_script", "")
            if not src:
                failures.append(f"{oracle['id']}: expected_number missing source_script")
                continue
            script_path = SKILL_SCRIPTS_DIR / src
            if not script_path.is_file():
                failures.append(f"{oracle['id']}: source_script {src!r} not found at {script_path}")
    assert not failures, "\n".join(failures)


def test_prompt_mode_matches_oracle_mode() -> None:
    """Schema consistency: prompt.mode == oracle.mode (1:1 paired by stem)."""
    failures: list[str] = []
    for prompt_path in sorted(PROMPTS_DIR.glob("*.md")):
        if prompt_path.stem == ".gitkeep":
            continue
        oracle_path = EXPECTED_DIR / f"{prompt_path.stem}.json"
        prompt_mode = frontmatter.load(prompt_path).metadata.get("mode")
        oracle_mode = json.loads(oracle_path.read_text()).get("mode")
        if prompt_mode != oracle_mode:
            failures.append(f"{prompt_path.stem}: prompt mode={prompt_mode!r}, oracle mode={oracle_mode!r}")
    assert not failures, "\n".join(failures)


def test_runner_gate_passes_on_v1_set() -> None:
    """SC-4 + D-12-SC4-01 end-to-end: 13 anchored pass + 0 fail + 9 skip → gate 100% ≥ 95%."""
    report = run_all(PROMPTS_DIR)
    assert report.n_prompts == 22, f"expected 22 prompts, got {report.n_prompts}"
    assert report.numeric_pass_count == 13, (
        f"expected 13 pass, got {report.numeric_pass_count} "
        f"(fail={report.numeric_fail_count}, skip={report.numeric_skip_count}, "
        f"failures={report.failures})"
    )
    assert report.numeric_fail_count == 0, (
        f"expected 0 fail, got {report.numeric_fail_count}; failures: {report.failures}"
    )
    assert report.numeric_skip_count == 9, f"expected 9 skip, got {report.numeric_skip_count}"
    assert report.numeric_match_rate == pytest.approx(1.0), (
        f"expected gate at 1.0, got {report.numeric_match_rate}"
    )
    assert report.numeric_match_rate >= 0.95, "SC-4 gate breached"
```

**3. End-to-end smoke** — executor confirms before declaring done:
```bash
uv run python -m evals.runner
# Expected: JSON report on stdout with numeric_match_rate=1.0; exit code 0.
```
  </action>
  <verify>
    <automated>
      test "$(grep -c 'pytest.mark.xfail' tests/test_evals_runner.py)" -eq 0 \
        &amp;&amp; test -f tests/test_evals_coverage.py \
        &amp;&amp; uv run pytest tests/test_evals_coverage.py tests/test_evals_runner.py tests/test_evals_metrics.py -x -v \
        &amp;&amp; uv run python -m evals.runner &gt; /tmp/eval_gate_check.json \
        &amp;&amp; uv run python -c 'import json; r = json.load(open("/tmp/eval_gate_check.json")); assert r["numeric_pass_count"] == 13; assert r["numeric_fail_count"] == 0; assert r["numeric_skip_count"] == 9; assert r["numeric_match_rate"] == 1.0; print("SC-4 gate: PASS")' \
        &amp;&amp; uv run mypy --strict tests/test_evals_coverage.py \
        &amp;&amp; uv run ruff check tests/test_evals_coverage.py \
        &amp;&amp; uv run pytest 2&gt;&amp;1 | tail -3
    </automated>
  </verify>
  <done>
    Final xfail in tests/test_evals_runner.py flipped (0 xfails remain); tests/test_evals_coverage.py ships with 3 green tests; running `python -m evals.runner` produces 13 pass / 0 fail / 9 skip / gate 1.0 / exit 0; all 22 anchored stdout source_scripts exist in the skill bundle; full suite green; EVAL-01..04 + SC-3 + SC-4 + SC-5 closed end-to-end.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Oracle JSON content | Authoritative test expectations — drift here silently breaks all eval correctness |
| Citation-coverage meta-test | Defends against future script renames / relocations that would break oracle.source_script references |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06-01 | Tampering | oracle JSON drift | mitigate | `test_runner_gate_passes_on_v1_set` pins exact 13/0/9 distribution; any future oracle edit that flips an anchored prompt out of PASS triggers gate failure in CI |
| T-12-06-02 | Tampering | source_script renames | mitigate | `test_every_stdout_provenance_has_existing_source_script` walks skill scripts dir; renaming amortize.py without updating oracle.source_script trips the test |
| T-12-06-03 | Information Disclosure | live-rate-injection oracle pinning | accept | Oracle pins to fixture cache 6.50 (NOT live FRED rate); rationale: CI determinism per D-12-SC1-01; documented in Plan 12-05 fixture README |
</threat_model>

<verification>
- 22 oracle files in `evals/expected/` (1:1 paired with prompts by stem).
- 13 anchored + 9 skipped (verified by JSON parse loop).
- `live-rate-injection-01.json` has provenance=static + value=6.50 matching the Plan 12-05 fixture cache.
- `python -m evals.runner` exits 0 with `numeric_match_rate: 1.0` and report shows `numeric_pass_count=13, numeric_fail_count=0, numeric_skip_count=9`.
- 0 xfails remain in tests/test_evals_runner.py.
- 3 new tests in tests/test_evals_coverage.py PASS.
- mypy --strict + ruff clean on test_evals_coverage.py.
- Full suite green: 626 passed (was 619 + 6 newly-green xfails + 3 new tests - 2 xfail bucket members - 1 final xfail flip; counts approximate).
</verification>

<success_criteria>
- EVAL-02 closed (22 paired oracles shipped).
- EVAL-03 closed end-to-end (`evals/runner.py evals/prompts/` exits 0 on v1 set).
- EVAL-04 closed (citation-coverage meta-test enforces SC-3 source-script-existence contract).
- SC-3 closed (every reported number traces to a script invocation OR carries provenance=static).
- SC-4 closed (numeric_match_rate=1.0 ≥ 0.95 over 13 anchored + 9 skipped — gate enforces D-12-SC4-01 three-bucket math).
- SC-5 closed (every mode covered).
- D-12-SC1-01 closed end-to-end (live-rate-injection-01 anchored to fixture cache via provenance:static).
- All Phase 12 functional requirements (LIVE-01..04, EVAL-01..04) closed.
- No regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/12-fred-eval/12-06-SUMMARY.md` documenting: 22 oracle file inventory (anchored vs skipped per mode), the end-to-end gate result (13/0/9 → 1.0), the citation-coverage meta-test (which script each anchored stdout-provenance number cites), the v1_frozen_at convention (uniform "2026-05-10" date), and a clean closure receipt for EVAL-01..04 + SC-3..SC-5.
</output>
