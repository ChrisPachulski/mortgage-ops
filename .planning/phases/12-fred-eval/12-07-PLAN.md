---
phase: 12-fred-eval
plan: 07
type: execute
wave: 7
depends_on: [12-06]
files_modified:
  - .github/workflows/ci.yml
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: []
tags: [phase-12, wave-7, ci-wiring, requirements-update, roadmap-finalize]
must_haves:
  truths:
    - "CI workflow runs evals/runner.py as a gate step after Pytest"
    - "REQUIREMENTS.md LIVE-01 wording updated per D-12-LIVE01-01 (HTTP wrapper canonical + MCP optional secondary)"
    - "REQUIREMENTS.md LIVE-02 wording updated per D-12-LIVE02-01 (Pattern A prose-only cache citation)"
    - "ROADMAP.md Phase 12 marked complete with SC-1..SC-5 closure verified by tests"
    - "REQUIREMENTS.md Traceability table updates LIVE-01..04 + EVAL-01..04 status to Done"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "New eval gate step running after Pytest, before User-Layer guard"
      contains: "evals/runner.py"
    - path: ".planning/REQUIREMENTS.md"
      provides: "LIVE-01 + LIVE-02 wording rewrites + 8 Phase 12 IDs marked Done"
      contains: "HTTP wrapper (canonical)"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 12 status updated to Complete with SC verification receipt"
      contains: "Phase 12 Complete"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "evals/runner.py"
      via: "uv run python -m evals.runner step"
      pattern: "evals.runner"
    - from: ".planning/REQUIREMENTS.md LIVE-01"
      to: ".claude/skills/mortgage-ops/references/fred-context.md"
      via: "wording references Plan 12-08 reference doc for MCP secondary path"
      pattern: "fred-context.md|HTTP wrapper"
---

<objective>
Wire the eval harness into CI as a gate step + update REQUIREMENTS.md LIVE-01 + LIVE-02 wording per D-12-LIVE01-01 + D-12-LIVE02-01 + mark ROADMAP.md Phase 12 complete with SC-1..SC-5 closure receipts.

Purpose: CI integration ensures future commits cannot silently regress the eval gate. REQUIREMENTS.md wording updates ratify the planning-time decisions to live in the source-of-truth requirements doc. ROADMAP.md update closes Phase 12's status entry.

Output: ci.yml + REQUIREMENTS.md + ROADMAP.md edits. No new code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-fred-eval/12-CONTEXT.md
@.planning/phases/12-fred-eval/12-06-SUMMARY.md
@.github/workflows/ci.yml
@CLAUDE.md

<interfaces>
Current `.github/workflows/ci.yml` step sequence (lines 11-61):
  1. checkout (with full history)
  2. setup-uv
  3. uv python install 3.12
  4. uv sync --locked --dev
  5. Ruff lint
  6. Ruff format check
  7. Mypy strict
  8. Pytest
  9. User-Layer commit guard (server-side belt-and-suspenders)

Insertion point for the new eval gate step: AFTER Pytest (step 8), BEFORE User-Layer guard (step 9). The eval gate depends on test-pass (some Phase 12 tests run the runner with monkeypatched FRED) but is logically separate from pytest's `tests/` scope.

REQUIREMENTS.md current LIVE-01 wording (line 158):
  `**LIVE-01**: FRED MCP integration via stefanoamorelli/fred-mcp-server for MORTGAGE30US weekly rate`

REQUIREMENTS.md current LIVE-02 wording (line 159):
  `**LIVE-02**: SKILL.md uses inline `!\`...\`` shell injection for current rate context at invocation`

D-12-LIVE01-01 mandates LIVE-01 rewrite:
  "FRED API integration via HTTP wrapper (canonical) with optional fred-mcp-server registration documented as secondary path"

D-12-LIVE02-01 mandates LIVE-02 rewrite:
  "SKILL.md cites cache-file paths for MORTGAGE30US and MORTGAGE15US in a 'Live Mortgage Rates' section; cache refreshed by scripts/fred_cli.py with 7-day TTL"

Traceability table format (REQUIREMENTS.md lines 322-329):
  | LIVE-01 | Phase 12 | Pending |
  → update to:
  | LIVE-01 | Phase 12 | Done (12-01 + 12-08 — HTTP wrapper at .claude/skills/mortgage-ops/scripts/fred_cli.py canonical per D-12-LIVE01-01; MCP server documented as optional secondary path in references/fred-context.md) |

ROADMAP.md Phase 12 status row (line 257):
  `| 12. FRED MCP Live Rates & Eval Harness | 0/9 | Planned-BLOCKED 2026-05-02 (4 blockers ...) | - |`
  → update to:
  `| 12. FRED MCP Live Rates & Eval Harness | 9/9 | Complete — Wave 0..7 all shipped; ... | 2026-05-10 |`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add eval gate step to .github/workflows/ci.yml + update REQUIREMENTS.md wording and traceability</name>
  <files>
    .github/workflows/ci.yml,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    .github/workflows/ci.yml (full file — locate Pytest step at line 38-39),
    .planning/REQUIREMENTS.md lines 156-165 (LIVE-01..04 + EVAL-01..04 wording),
    .planning/REQUIREMENTS.md lines 322-329 (Traceability table Phase 12 rows),
    .planning/phases/12-fred-eval/12-CONTEXT.md (D-12-LIVE01-01 + D-12-LIVE02-01 verbatim wording)
  </read_first>
  <action>
**1. Edit `.github/workflows/ci.yml`** — INSERT a new step BETWEEN the existing `Pytest` step (line 38-39) and the existing `User-Layer commit guard` step (line 41+). Use the Edit tool with the exact insertion text below; preserve all surrounding YAML indentation (6 spaces for step-name; 8 spaces for run content):

```yaml
      - name: Eval gate (replay-stub mode, deterministic; D-12-SC4-01 three-bucket)
        # Phase 12 SC-4: numeric_match_rate >= 0.95 over (pass + fail), skip excluded
        # from the denominator. v1 ships 13 anchored + 9 TBD-skip → 13/(13+0) = 100%.
        # The harness exits 0 on pass, 1 on fail; CI propagates the exit code.
        run: uv run python -m evals.runner --gate 0.95
```

The resulting step sequence becomes:
  1-8. (unchanged: checkout → setup-uv → python install → sync → ruff → mypy → pytest)
  9. **Eval gate (replay-stub mode, deterministic; D-12-SC4-01 three-bucket)** ← NEW
  10. User-Layer commit guard (server-side belt-and-suspenders)

**2. Edit `.planning/REQUIREMENTS.md`** — apply 2 wording rewrites + 8 traceability row updates.

**2a. Rewrite line 158 (LIVE-01):**
```markdown
- [x] **LIVE-01**: FRED API integration via HTTP wrapper (canonical) at `.claude/skills/mortgage-ops/scripts/fred_cli.py` with optional `stefanoamorelli/fred-mcp-server` registration documented as secondary path in `.claude/skills/mortgage-ops/references/fred-context.md` (per D-12-LIVE01-01)
```

**2b. Rewrite line 159 (LIVE-02):**
```markdown
- [x] **LIVE-02**: SKILL.md cites cache-file paths for `MORTGAGE30US` and `MORTGAGE15US` in a `## Live Mortgage Rates` section; cache refreshed by `scripts/fred_cli.py` with 7-day TTL (Pattern A prose-only injection per D-12-LIVE02-01 — Anthropic `!`...`` shell-injection syntax NOT used)
```

**2c. Mark LIVE-03 + LIVE-04 + EVAL-01..04 as `[x]`** (checkbox toggle from `[ ]` to `[x]`):
  - Line 160 LIVE-03 → `[x]`
  - Line 161 LIVE-04 → `[x]`
  - Line 162 EVAL-01 → `[x]`
  - Line 163 EVAL-02 → `[x]`
  - Line 164 EVAL-03 → `[x]`
  - Line 165 EVAL-04 → `[x]`

**2d. Update traceability table rows (lines 322-329) for LIVE-01..04 + EVAL-01..04**:

Replace these 8 rows verbatim:
```
| LIVE-01 | Phase 12 | Pending |
| LIVE-02 | Phase 12 | Pending |
| LIVE-03 | Phase 12 | Pending |
| LIVE-04 | Phase 12 | Pending |
| EVAL-01 | Phase 12 | Pending |
| EVAL-02 | Phase 12 | Pending |
| EVAL-03 | Phase 12 | Pending |
| EVAL-04 | Phase 12 | Pending |
```

With:
```
| LIVE-01 | Phase 12 | Done (12-01 + 12-08 — HTTP wrapper at .claude/skills/mortgage-ops/scripts/fred_cli.py canonical per D-12-LIVE01-01; MCP server documented as optional secondary path in references/fred-context.md; 4 tests in tests/test_fred_cli.py all green; always-exit-0 envelope per Pitfall 1 + D-12-LIVE02-01 recovery contract verified by test_fred_cli_missing_api_key_returns_exit_0_with_error_envelope) |
| LIVE-02 | Phase 12 | Done (12-03 — Pattern A prose-only injection per D-12-LIVE02-01 verbatim section at SKILL.md `## Live Mortgage Rates`; 3 grep tests in tests/test_skill_md_fred.py all green; forbidden `!`...`` shell-injection syntax pinned absent by test_skill_md_does_not_use_shell_injection_syntax) |
| LIVE-03 | Phase 12 | Done (12-02 — lib/fred_cache.py 7-day TTL strict-< boundary; 4 freezegun boundary tests in tests/test_fred_cache.py all green at 6d23h59m fresh / 7d exact stale / 8d stale; orchestration/lockfile.mjs Python port via with_cache_lock + 60s stale recovery) |
| LIVE-04 | Phase 12 | Done (12-01 — MORTGAGE15US in allowlist alongside MORTGAGE30US; parametrized test_fred_cli_supports_both_series covers both series_ids) |
| EVAL-01 | Phase 12 | Done (12-05 — 22 prompts in evals/prompts/ — 21 mode-coverage (3 × 7 modes per SC-5) + 1 live-rate-injection-01 per D-12-SC1-01; 13 anchored + 9 TBD-skip per D-12-SC4-01) |
| EVAL-02 | Phase 12 | Done (12-06 — 22 paired oracles in evals/expected/ — 1:1 stem-matched with prompts; 13 anchored with provenance: stdout (or static for live-rate-injection); 9 skipped with defer_until_phase: 13.0) |
| EVAL-03 | Phase 12 | Done (12-04 + 12-06 — evals/runner.py three-bucket gate per D-12-SC4-01; python -m evals.runner on the v1 set returns numeric_match_rate=1.0 (13 pass / 0 fail / 9 skip) and exit 0; CI gate wired in .github/workflows/ci.yml per Plan 12-07) |
| EVAL-04 | Phase 12 | Done (12-04 — evals/metrics.detect_hallucinations STDOUT-only per D-12-SC3-01; score_route_match Pitfall #2b cross-check fails when numeric_output present + no subprocess invocation; provenance: static exempts IRS-Pub-936-style citations; 5 tests in tests/test_evals_metrics.py all green) |
```
  </action>
  <verify>
    <automated>
      grep -q 'Eval gate (replay-stub mode' .github/workflows/ci.yml \
        &amp;&amp; grep -q 'uv run python -m evals.runner --gate 0.95' .github/workflows/ci.yml \
        &amp;&amp; grep -q '\[x\] \*\*LIVE-01\*\*: FRED API integration via HTTP wrapper (canonical)' .planning/REQUIREMENTS.md \
        &amp;&amp; grep -q '\[x\] \*\*LIVE-02\*\*: SKILL.md cites cache-file paths' .planning/REQUIREMENTS.md \
        &amp;&amp; grep -q '\[x\] \*\*LIVE-03\*\*' .planning/REQUIREMENTS.md \
        &amp;&amp; grep -q '\[x\] \*\*LIVE-04\*\*' .planning/REQUIREMENTS.md \
        &amp;&amp; grep -q '\[x\] \*\*EVAL-01\*\*' .planning/REQUIREMENTS.md \
        &amp;&amp; grep -q '\[x\] \*\*EVAL-04\*\*' .planning/REQUIREMENTS.md \
        &amp;&amp; grep -q '| LIVE-01 | Phase 12 | Done' .planning/REQUIREMENTS.md \
        &amp;&amp; grep -q '| EVAL-03 | Phase 12 | Done' .planning/REQUIREMENTS.md \
        &amp;&amp; ! grep -q '| LIVE-01 | Phase 12 | Pending' .planning/REQUIREMENTS.md \
        &amp;&amp; ! grep -q '| EVAL-04 | Phase 12 | Pending' .planning/REQUIREMENTS.md \
        &amp;&amp; uv run python -c 'import yaml; yaml.safe_load(open(".github/workflows/ci.yml"))'
    </automated>
  </verify>
  <done>
    `.github/workflows/ci.yml` has the new eval gate step inserted BEFORE the User-Layer guard step; ci.yml still parses as valid YAML; REQUIREMENTS.md LIVE-01 + LIVE-02 wording updated per D-12-LIVE01-01 + D-12-LIVE02-01; LIVE-03..04 + EVAL-01..04 marked `[x]`; 8 traceability rows updated to `Done` with plan-pointer details. No regressions to other REQUIREMENTS.md entries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mark ROADMAP.md Phase 12 complete with SC-1..SC-5 closure receipt</name>
  <files>
    .planning/ROADMAP.md
  </files>
  <read_first>
    .planning/ROADMAP.md (full file — locate Phase 12 entry at line 28 + status row at line 257),
    .planning/phases/12-fred-eval/12-06-SUMMARY.md (closure receipts from end-to-end run)
  </read_first>
  <action>
**1. Update line 28 (Phase 12 list entry)** — toggle checkbox from `[ ]` to `[x]`:

From:
```
- [ ] **Phase 12: FRED MCP Live Rates & Eval Harness** - Live MORTGAGE30US + skill quality regression tests
```
To:
```
- [x] **Phase 12: FRED MCP Live Rates & Eval Harness** - Live MORTGAGE30US + skill quality regression tests
```

**2. Update Phase 12 Plans subsection** (under `### Phase 12:` block, currently shows `**Plans**: TBD`):

Replace `**Plans**: TBD` (one line) with a full plan list:
```markdown
**Plans:** 9/9 plans complete
- [x] 12-00-PLAN.md — Wave 0: 5 Wave-0 strict-xfail test files + evals/ tree + tests/fixtures/fred/ + freezegun + python-frontmatter dev-deps + .gitignore Phase 12 entries
- [x] 12-01-PLAN.md — Wave 1: scripts/fred_cli.py HTTP wrapper canonical per D-12-LIVE01-01; always-exit-0 envelope per Pitfall 1; LIVE-01 + LIVE-04 closed
- [x] 12-02-PLAN.md — Wave 2: lib/fred_cache.py 7-day TTL strict-< boundary + orchestration/lockfile.mjs Python port via with_cache_lock; LIVE-03 closed
- [x] 12-03-PLAN.md — Wave 3: SKILL.md `## Live Mortgage Rates` section verbatim per D-12-LIVE02-01 (Pattern A prose-only); LIVE-02 closed
- [x] 12-04-PLAN.md — Wave 4: evals/runner.py HarnessReport three-bucket gate per D-12-SC4-01 + evals/metrics.py STDOUT-only sourcing per D-12-SC3-01; EVAL-03 + EVAL-04 closed at scorer layer
- [x] 12-05-PLAN.md — Wave 5: 22 prompts (13 anchored + 9 TBD-skip) covering all 7 modes + live-rate-injection-01 per D-12-SC1-01 + 2 FRED fixture caches; EVAL-01 + SC-5 closed
- [x] 12-06-PLAN.md — Wave 6: 22 paired oracle JSONs + citation-coverage meta-test + end-to-end gate (13/0/9 → 1.0); EVAL-02 closed + EVAL-03 closed end-to-end + SC-3/SC-4 verified
- [x] 12-07-PLAN.md — Wave 7: CI eval gate step + REQUIREMENTS.md wording rewrites per D-12-LIVE01-01 + D-12-LIVE02-01 + ROADMAP.md Phase 12 closure
- [x] 12-08-PLAN.md — Wave 8: references/fred-context.md HTTP-canonical + MCP-optional path + .claude/agents/README.md + CLAUDE.md cross-link
```

**3. Update line 257 (Progress table Phase 12 row)**:

From:
```
| 12. FRED MCP Live Rates & Eval Harness | 0/9 | Planned-BLOCKED 2026-05-02 (4 blockers: fixture-id drift; SC-4 denominator inflation; upstream phase deps; SC-1 not verified end-to-end) | - |
```
To:
```
| 12. FRED MCP Live Rates & Eval Harness | 9/9 | Complete — Wave 0..8 all shipped; ROADMAP SC-1..SC-5 all closed; v1.0 LANDED. SC-1: live-rate-injection-01 eval anchored to fixture cache MORTGAGE30US=6.50 per D-12-SC1-01; gate passes via Pattern A prose-only injection (D-12-LIVE02-01) + skill loads cache via Read tool. SC-2: lib/fred_cache.py 7-day TTL strict-< boundary verified by 4 freezegun tests (6d23h59m fresh / 7d exact stale / 8d stale / lock-acquire) + 2 read-through integration tests; orchestration/lockfile.mjs Python port complete. SC-3: evals/metrics.detect_hallucinations STDOUT-only per D-12-SC3-01 (tightens RESEARCH §Pattern 6); citation-coverage meta-test in tests/test_evals_coverage.py asserts every anchored stdout-provenance number cites an existing .claude/skills/mortgage-ops/scripts/*.py. SC-4: numeric_match_rate = pass / (pass + fail) per D-12-SC4-01 three-bucket; v1 set = 13 pass / 0 fail / 9 skip → 1.0 ≥ 0.95; gate wired in .github/workflows/ci.yml. SC-5: 7-mode coverage verified by parametrized test_each_mode_has_at_least_one_prompt. Final test suite N passed + 0 xfailed (xfail count dropped from Wave-0 baseline of ~22 to 0 over Waves 1-6). LIVE-01..04 + EVAL-01..04 all closed; D-12-SC1-01 + D-12-SC3-01 + D-12-SC4-01 + D-12-LIVE01-01 + D-12-LIVE02-01 all honored verbatim. | 2026-05-10 |
```

(Executor: replace the literal "N" in "N passed" with the actual final passing test count from the most recent `uv run pytest` output after Plan 12-06 ran. Approximately 626; confirm by running pytest before this edit.)
  </action>
  <verify>
    <automated>
      grep -q '\[x\] \*\*Phase 12: FRED MCP Live Rates' .planning/ROADMAP.md \
        &amp;&amp; grep -q '\*\*Plans:\*\* 9/9 plans complete' .planning/ROADMAP.md \
        &amp;&amp; grep -q '| 12. FRED MCP Live Rates &amp; Eval Harness | 9/9 | Complete' .planning/ROADMAP.md \
        &amp;&amp; ! grep -q '| 12. FRED MCP Live Rates &amp; Eval Harness | 0/9' .planning/ROADMAP.md \
        &amp;&amp; grep -q '12-01-PLAN.md' .planning/ROADMAP.md \
        &amp;&amp; grep -q '12-08-PLAN.md' .planning/ROADMAP.md \
        &amp;&amp; uv run pytest 2&gt;&amp;1 | tail -3
    </automated>
  </verify>
  <done>
    ROADMAP.md Phase 12 entry checkbox toggled to `[x]`; Phase 12 plan list shows 9/9 complete with one-line per plan; Progress table row updated to "9/9 | Complete" with SC-1..SC-5 closure receipt; date "2026-05-10"; old "Planned-BLOCKED 2026-05-02" status removed. No regression to other phase entries.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow definition | Modifies the gate that future commits must pass — wrong threshold = silent regression vector |
| Planning artifacts (REQUIREMENTS, ROADMAP) | Source-of-truth requirements — stale wording vs implementation = confusion in audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07-01 | Tampering | CI eval gate threshold | mitigate | `--gate 0.95` is explicit in the YAML; any future PR that lowers it would surface in a diff review; the threshold value is also pinned in evals/runner.py SC4_GATE_THRESHOLD constant for cross-check |
| T-12-07-02 | Spoofing | requirements drift vs implementation | mitigate | Traceability table now points each requirement at the specific plan + test files that close it; future audit reads the table → tests → confirms closure |
| T-12-07-03 | Denial of Service | eval gate slow in CI | accept | replay-stub mode runs in-process (no live model, no FRED API); v1 set is 22 prompts; expected runtime < 5 seconds — comparable to existing pytest step |
</threat_model>

<verification>
- ci.yml has the new eval gate step (verified by grep).
- ci.yml still parses as valid YAML (verified by yaml.safe_load smoke test).
- REQUIREMENTS.md LIVE-01 + LIVE-02 wording updated per D-12-* locks (verified by literal grep for new prose).
- LIVE-03..04 + EVAL-01..04 marked `[x]` (8 checkbox toggles).
- Traceability table has 8 `Done (Phase 12, plan-NN, ...)` rows replacing 8 `Pending` rows.
- ROADMAP.md Phase 12 checkbox `[x]` + Plans subsection lists 9 plans + Progress table updated.
- Full suite still green (this plan touches no test files; no code change).
</verification>

<success_criteria>
- CI eval gate wired (`uv run python -m evals.runner --gate 0.95`).
- REQUIREMENTS.md updated per D-12-LIVE01-01 + D-12-LIVE02-01 verbatim wording.
- 8 Phase 12 requirement IDs marked Done in traceability table.
- ROADMAP.md Phase 12 marked Complete with SC-1..SC-5 closure receipt.
- No regression to Phase 1-11 status entries.
</success_criteria>

<output>
After completion, create `.planning/phases/12-fred-eval/12-07-SUMMARY.md` documenting: ci.yml step inserted (with full step block), REQUIREMENTS.md before/after diff for LIVE-01 + LIVE-02 wording, the 8 traceability row updates (one line each), and the ROADMAP.md Phase 12 closure receipt verbatim.
</output>
