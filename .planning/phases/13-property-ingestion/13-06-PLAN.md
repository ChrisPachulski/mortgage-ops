---
phase: 13-property-ingestion
plan: 06
type: execute
wave: 6
depends_on: [13-00, 13-01, 13-02, 13-03, 13-04, 13-05]
files_modified:
  - tests/fixtures/zillow/sfh_conforming_happy_path.html
  - tests/fixtures/zillow/condo_partial_tax_missing.html
  - tests/fixtures/zillow/blocked_perimeterx.html
  - tests/fixtures/zillow/extracted/__SHA_PLACEHOLDER_1__.json
  - tests/fixtures/zillow/extracted/__SHA_PLACEHOLDER_2__.json
  - tests/fixtures/zillow/README.md
  - tests/test_property_ingestion_integration.py
autonomous: true
requirements: []
tags: [phase-13, fixtures, integration, end-to-end, scope-test-01]
must_haves:
  truths:
    - "3 sanitized Zillow HTML fixtures exist (sfh_conforming_happy_path, condo_partial_tax_missing, blocked_perimeterx)"
    - "2 paired extracted/{sha16}.json mock-Sonnet outputs exist for the 2 happy-path fixtures"
    - "blocked_perimeterx.html contains a captcha phrase from CAPTCHA_PHRASES (e.g., 'press & hold')"
    - "Each HTML fixture has ≥5000 bytes (passes MIN_BODY_BYTES — block detection only fires for the captcha fixture via captcha-phrase signal, not body-too-short)"
    - "tests/fixtures/zillow/README.md is updated with the actual file table + sha-key explanation"
    - "tests/test_property_ingestion_integration.py exercises the full URL → CLI → DuckDB pipeline using the real fixtures"
    - "Integration test: SFH fixture + sha-keyed mock → shape-1 envelope; DuckDB write succeeds; round-trip read returns equivalent listing"
    - "Integration test: blocked fixture → shape-3 envelope; DuckDB NOT written (no listing to persist)"
    - "No PII or real addresses in committed fixtures; ZIP stays real per README sanitization recipe"
  artifacts:
    - path: "tests/fixtures/zillow/sfh_conforming_happy_path.html"
      provides: "Synthetic happy-path SFH listing — full __NEXT_DATA__ payload, all MUST-HAVEs scrapable"
      min_lines: 50
    - path: "tests/fixtures/zillow/condo_partial_tax_missing.html"
      provides: "Synthetic condo listing — has HOA, missing tax_annual; tests partial-extraction path"
      min_lines: 50
    - path: "tests/fixtures/zillow/blocked_perimeterx.html"
      provides: "Synthetic captcha page — fires shape-3 captcha_detected"
      min_lines: 30
    - path: "tests/fixtures/zillow/extracted/"
      provides: "Pre-recorded Sonnet outputs for the 2 happy-path fixtures (sha-keyed)"
      contains: ".json"
    - path: "tests/test_property_ingestion_integration.py"
      provides: "End-to-end pipeline test: URL → CLI → DuckDB → read-back"
      contains: "test_end_to_end_sfh_happy_path"
  key_links:
    - from: "tests/fixtures/zillow/sfh_conforming_happy_path.html"
      to: "tests/fixtures/zillow/extracted/{sha16}.json"
      via: "sha256(html_bytes)[:16] keying — mock_sonnet conftest fixture loads on lookup"
      pattern: "hashlib.sha256"
    - from: "tests/test_property_ingestion_integration.py"
      to: ".claude/skills/mortgage-ops/scripts/property_fetch.py"
      via: "subprocess.run with --html-from fixture path + mock_sonnet conftest fixture"
      pattern: "subprocess.run"
    - from: "tests/test_property_ingestion_integration.py"
      to: "lib.property_persistence.read_latest_for_zpid"
      via: "after CLI write, read back from DuckDB"
      pattern: "read_latest_for_zpid"
---

<objective>
Ship the seed test fixtures + end-to-end integration test that exercises the full Phase 13 pipeline:

```
Zillow URL → WebFetch HTML → property_fetch.py CLI → block-detect → Sonnet (mocked) → Pydantic → DuckDB write → read-back
```

Creates 3 sanitized HTML fixtures (1 SFH happy path, 1 condo with missing tax, 1 captcha-blocked page) and the 2 paired `extracted/{sha16}.json` mock-Sonnet outputs. Updates `tests/fixtures/zillow/README.md` to document the actual file table. Adds a new integration test file that runs the CLI as a subprocess against each fixture and asserts envelope shape + DuckDB round-trip.

This is the LAST plan in Phase 13. After this plan ships, all 7 phase requirement IDs are closed and all 5 D-13 locks are proven end-to-end.

No new requirement IDs — this plan provides FIXTURES + integration coverage that the previous plans (13-01..13-05) consume. TEST-01 ranges across Phases 13 + 17; Phase 13 ships the seed set, Phase 17 expands.

Output:
- 3 HTML fixtures in `tests/fixtures/zillow/`
- 2 JSON fixtures in `tests/fixtures/zillow/extracted/`
- Updated `tests/fixtures/zillow/README.md` (real file table + sha-key explanation)
- New `tests/test_property_ingestion_integration.py` (~5-7 end-to-end tests)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-property-ingestion/13-CONTEXT.md
@.planning/phases/13-property-ingestion/13-RESEARCH.md
@.planning/phases/13-property-ingestion/13-PATTERNS.md
@.planning/phases/13-property-ingestion/13-00-SUMMARY.md
@.planning/phases/13-property-ingestion/13-01-SUMMARY.md
@.planning/phases/13-property-ingestion/13-02-SUMMARY.md
@.planning/phases/13-property-ingestion/13-03-SUMMARY.md
@.planning/phases/13-property-ingestion/13-04-SUMMARY.md
@.planning/phases/13-property-ingestion/13-05-SUMMARY.md
@tests/fixtures/zillow/README.md
@tests/fixtures/subagent_transcripts/README.md
@lib/property_block_detector.py
@lib/property_listing.py
@lib/property_persistence.py
@.claude/skills/mortgage-ops/scripts/property_fetch.py
@tests/conftest.py

<interfaces>
<!-- sha-key formula (must match conftest.py mock_sonnet) -->

```python
import hashlib
sha16 = hashlib.sha256(html_bytes).hexdigest()[:16]
# extracted dict stored at: tests/fixtures/zillow/extracted/{sha16}.json
```

<!-- Fixture content rules -->

| Fixture | block-detect | Sonnet (mocked) | Envelope |
|---|---|---|---|
| sfh_conforming_happy_path.html | passes | returns full dict | shape-1 |
| condo_partial_tax_missing.html | passes | returns dict with tax_annual=null | shape-1 (tax stays null per D-13-MUSTHAVE-01) |
| blocked_perimeterx.html | fires "captcha_detected" | NEVER CALLED | shape-3 |

<!-- All fixtures must -->
- Be ≥ 5000 bytes (MIN_BODY_BYTES) — blocked fixture fires on captcha PHRASE not on body length
- Be free of PII (no real addresses, no real agent contact info, no real phone numbers)
- Contain a synthetic ZIP that is a real US ZIP (per README: ZIP stays real for Phase 14)
- Have the `<script id="__NEXT_DATA__">` block (except blocked_perimeterx — that's the point)
- Contain NO `Co-Authored-By` or AI-attribution strings (CLAUDE.md global rule)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 3 sanitized Zillow HTML fixtures (SFH happy, condo partial, captcha blocked)</name>
  <files>tests/fixtures/zillow/sfh_conforming_happy_path.html, tests/fixtures/zillow/condo_partial_tax_missing.html, tests/fixtures/zillow/blocked_perimeterx.html</files>
  <read_first>
    - tests/fixtures/zillow/README.md (the synthetic-only policy from Plan 13-00)
    - .planning/phases/13-property-ingestion/13-RESEARCH.md §"Test Fixture Strategy" + §"Sanitization recipe" (lines 943-976)
    - lib/property_block_detector.py CAPTCHA_PHRASES + MIN_BODY_BYTES (the fixtures must satisfy these)
  </read_first>
  <action>
    Create 3 HTML fixtures. ALL content must be synthetic (no PII, no real agent names/phones/emails, no copyrighted listing photos referenced). ZIP codes can be real (per README: Phase 14 needs them for tax lookups) — use 94110 (San Francisco), 98052 (Redmond, WA), or 60611 (Chicago) as common test ZIPs.

    **Fixture 1: `tests/fixtures/zillow/sfh_conforming_happy_path.html`**

    Required structure:
    - ≥ 5000 bytes total (MIN_BODY_BYTES boundary)
    - Contains `<script id="__NEXT_DATA__">{...}</script>` block with a realistic-shape JSON payload (Sonnet ignores this in tests — only the sha-keyed mock matters — but a real-shape blob makes the fixture useful for Phase 17 expansion and any future live-Sonnet smoke test)
    - SFH property, conforming price (~$625k), in ZIP 94110
    - Synthetic address ("123 Synthetic St, San Francisco, CA 94110") — clearly fake; do NOT use a real Zillow address
    - No agent info; no photos referenced
    - Includes Zillow-style boilerplate prose (Property Description, school ratings, etc.) for byte-padding past 5000

    Skeleton:
    ```html
    <!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="UTF-8" />
        <title>Synthetic Fixture — 123 Synthetic St | Zillow</title>
        <meta name="description" content="Synthetic test fixture — not a real listing." />
    </head>
    <body>
        <!-- Synthetic Zillow listing fixture. Phase 13 INGEST-02 + PROP-01 happy path.
             NO PII, NO real addresses, NO agent contact info. ZIP intentionally real (94110)
             so Phase 14 per-zip tax lookups behave realistically. -->
        <header><nav>Zillow synthetic-fixture nav</nav></header>
        <main>
            <h1>123 Synthetic St, San Francisco, CA 94110</h1>
            <section id="property-summary">
                <p>3 bd | 2.5 ba | 1,800 sqft | Built 1985 | Single Family</p>
                <p>Listed at $625,000 — synthetic fixture for INGEST-02 testing.</p>
            </section>
            <section id="property-description">
                <p>This is synthetic placeholder prose to push the fixture above MIN_BODY_BYTES.
                Lorem ipsum dolor sit amet, consectetur adipiscing elit. ... [pad to ~5000-7000 bytes] ...</p>
            </section>
        </main>
        <script id="__NEXT_DATA__" type="application/json">
            {
              "props": {
                "pageProps": {
                  "property": {
                    "zpid": "12345678",
                    "price": 625000,
                    "zipcode": "94110",
                    "propertyTypeDimension": "SingleFamily",
                    "bedrooms": 3,
                    "bathrooms": 2.5,
                    "livingArea": 1800,
                    "yearBuilt": 1985,
                    "taxAnnualAmount": 7800,
                    "hoaFee": null,
                    "zestimate": 640000,
                    "daysOnZillow": 12,
                    "datePostedString": "2026-04-28"
                  }
                }
              }
            }
        </script>
    </body>
    </html>
    ```

    Pad the description section with enough Lorem-ipsum-style synthetic prose to reach ≥ 5500 bytes total (comfortable margin above MIN_BODY_BYTES=5000). Use a non-distinctive `<!-- pad -->` comment with `x` characters if Lorem ipsum is unwanted. Verify file size with `wc -c tests/fixtures/zillow/sfh_conforming_happy_path.html`.

    **Fixture 2: `tests/fixtures/zillow/condo_partial_tax_missing.html`**

    Same shape as Fixture 1, with these differences:
    - `propertyTypeDimension: "Condominium"` (Sonnet will map to "condo")
    - `hoaFee: 425` (HOA scraped — condo with monthly HOA)
    - `taxAnnualAmount: null` (this is the "partial" part; D-13-MUSTHAVE-01 says tax_annual is NOT a MUST-HAVE — so the envelope is still shape-1 with tax_annual=null)
    - Different synthetic address ("456 Fixture Ln, Redmond, WA 98052")
    - ZIP 98052
    - Different ZPID: "87654321"
    - Price: 450000
    - h1: "456 Fixture Ln, Redmond, WA 98052"

    Same byte-padding strategy (≥ 5500 bytes).

    **Fixture 3: `tests/fixtures/zillow/blocked_perimeterx.html`**

    A captcha page. Required:
    - ≥ 5000 bytes (so it fails on the captcha PHRASE signal, NOT the body_too_short signal — that's a more realistic detection scenario)
    - Contains `Press & Hold to continue` text (matches CAPTCHA_PHRASES[0])
    - Does NOT contain `<script id="__NEXT_DATA__">` (no PerimeterX captcha page would; defense-in-depth — the captcha signal fires first either way)
    - No PII

    Skeleton:
    ```html
    <!DOCTYPE html>
    <html lang="en">
    <head>
        <title>Verification Required</title>
        <meta name="description" content="Synthetic captcha-block fixture for Phase 13 INGEST-01." />
    </head>
    <body>
        <!-- Synthetic PerimeterX-style captcha block fixture.
             Triggers D-13-BLOCK-01 captcha_detected signal via the "Press &amp; Hold" phrase. -->
        <div id="px-captcha">
            <h1>Press &amp; Hold to verify you are human</h1>
            <p>We noticed unusual traffic from your network. Please complete the human verification challenge below.</p>
        </div>
        <!-- Pad with synthetic prose so this is NOT a body_too_short detection.
             Captcha pages from real anti-bot CDNs are often 8-15KB; we mimic that. -->
        <div class="block-page-explanation">
            <p>Synthetic padding text ... [repeat ~5000+ bytes of placeholder prose] ...</p>
        </div>
    </body>
    </html>
    ```

    Pad to ≥ 5500 bytes. Verify `len(body) >= 5000` so `body_too_short` does NOT fire (the test is specifically that `captcha_detected` fires first per the cheap-first detection order locked in Plan 13-02).

    DO NOT copy/paste from any real Zillow page or real captcha CDN page. The strings "Press & Hold", "human verification", "unusual traffic" are PHRASES on the CAPTCHA_PHRASES list (Plan 13-02) — they're generic English phrases, not copyrighted text.

    DO NOT include any `<img>` tags, agent bios, contact phone/email patterns. Run a quick grep for PII patterns before committing:
    ```bash
    grep -EiH '\b(\d{3}[-.]?\d{3}[-.]?\d{4}|[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,})\b' tests/fixtures/zillow/*.html
    # Should return no matches.
    ```
  </action>
  <verify>
    <automated>
      test -f tests/fixtures/zillow/sfh_conforming_happy_path.html &amp;&amp;
      test -f tests/fixtures/zillow/condo_partial_tax_missing.html &amp;&amp;
      test -f tests/fixtures/zillow/blocked_perimeterx.html &amp;&amp;
      [ "$(wc -c &lt; tests/fixtures/zillow/sfh_conforming_happy_path.html)" -ge 5000 ] &amp;&amp;
      [ "$(wc -c &lt; tests/fixtures/zillow/condo_partial_tax_missing.html)" -ge 5000 ] &amp;&amp;
      [ "$(wc -c &lt; tests/fixtures/zillow/blocked_perimeterx.html)" -ge 5000 ] &amp;&amp;
      grep -q '__NEXT_DATA__' tests/fixtures/zillow/sfh_conforming_happy_path.html &amp;&amp;
      grep -q '__NEXT_DATA__' tests/fixtures/zillow/condo_partial_tax_missing.html &amp;&amp;
      ! grep -q '__NEXT_DATA__' tests/fixtures/zillow/blocked_perimeterx.html &amp;&amp;
      grep -qi 'press &amp; hold\|human verification' tests/fixtures/zillow/blocked_perimeterx.html &amp;&amp;
      ! grep -E '\b\d{3}-\d{3}-\d{4}\b' tests/fixtures/zillow/*.html &amp;&amp;
      ! grep -iE 'co-authored-by|generated by claude|generated by ai|claude code' tests/fixtures/zillow/*.html
    </automated>
  </verify>
  <acceptance_criteria>
    - All 3 HTML fixtures exist
    - Each fixture is ≥ 5000 bytes (verify with `wc -c`)
    - SFH + condo fixtures contain `<script id="__NEXT_DATA__"` block
    - Blocked fixture does NOT contain `__NEXT_DATA__`
    - Blocked fixture contains "Press & Hold" OR "human verification" OR another CAPTCHA_PHRASES entry (case-insensitive grep matches)
    - No fixture contains a 10-digit phone-number-shaped pattern (`\b\d{3}-\d{3}-\d{4}\b` returns no matches)
    - No fixture contains an email-shaped pattern (RFC-style local@domain)
    - No fixture contains `Co-Authored-By`, `generated by Claude`, `generated by AI`, `Claude Code` (case-insensitive)
    - SFH fixture has `zpid: "12345678"` in the __NEXT_DATA__ JSON
    - Condo fixture has `zpid: "87654321"` and `propertyTypeDimension: "Condominium"`
    - All addresses are synthetic (executor's eye-check; can grep for known placeholders: "Synthetic St" + "Fixture Ln" should both appear)
  </acceptance_criteria>
  <done>3 sanitized HTML fixtures committed under tests/fixtures/zillow/; SFH + condo for happy-path tests; blocked for shape-3 test; all PII-free per README sanitization recipe.</done>
</task>

<task type="auto">
  <name>Task 2: Create paired extracted/{sha16}.json mock-Sonnet outputs for the 2 happy-path fixtures</name>
  <files>tests/fixtures/zillow/extracted/__SHA_PLACEHOLDER_1__.json, tests/fixtures/zillow/extracted/__SHA_PLACEHOLDER_2__.json</files>
  <read_first>
    - tests/fixtures/zillow/sfh_conforming_happy_path.html (Task 1 output)
    - tests/fixtures/zillow/condo_partial_tax_missing.html (Task 1 output)
    - tests/conftest.py mock_sonnet fixture (the sha-key formula)
    - lib/property_listing.py (the schema the JSON must conform to after Pydantic validation)
  </read_first>
  <action>
    Compute sha-16 hex of each HTML fixture and write the matching pre-recorded extracted dict at `tests/fixtures/zillow/extracted/{sha16}.json`. These dicts simulate what Sonnet WOULD return for that HTML.

    Step 1 — compute sha for SFH fixture:
    ```bash
    SHA_SFH=$(python -c "import hashlib; print(hashlib.sha256(open('tests/fixtures/zillow/sfh_conforming_happy_path.html','rb').read()).hexdigest()[:16])")
    echo "SFH sha16: $SHA_SFH"
    ```

    Write `tests/fixtures/zillow/extracted/{$SHA_SFH}.json` with full happy-path content. The JSON keys must match what `lib/property_listing.py`'s `PropertyListing.model_validate` expects after the CLI merges audit fields (source_url, zpid, fetched_at). Money fields are JSON strings (D-19); non-money fields use raw types.

    SFH extracted JSON:
    ```json
    {
      "zpid": "12345678",
      "price": "625000.00",
      "zip": "94110",
      "property_type": "SFH",
      "beds": 3,
      "baths": "2.5",
      "sqft": 1800,
      "year_built": 1985,
      "tax_annual": "7800.00",
      "hoa_monthly": null,
      "insurance_estimate_annual": null,
      "zestimate": "640000.00",
      "days_on_market": 12,
      "list_date": "2026-04-28"
    }
    ```

    Step 2 — compute sha for condo fixture:
    ```bash
    SHA_CONDO=$(python -c "import hashlib; print(hashlib.sha256(open('tests/fixtures/zillow/condo_partial_tax_missing.html','rb').read()).hexdigest()[:16])")
    echo "Condo sha16: $SHA_CONDO"
    ```

    Condo extracted JSON (tax_annual: null is the deliberate partial — D-13-MUSTHAVE-01 says it's not a MUST-HAVE):
    ```json
    {
      "zpid": "87654321",
      "price": "450000.00",
      "zip": "98052",
      "property_type": "condo",
      "beds": 2,
      "baths": "2.0",
      "sqft": 1100,
      "year_built": 2008,
      "tax_annual": null,
      "hoa_monthly": "425.00",
      "insurance_estimate_annual": null,
      "zestimate": "460000.00",
      "days_on_market": 7,
      "list_date": "2026-05-03"
    }
    ```

    Note: blocked_perimeterx.html does NOT get an extracted file because block-detection fires BEFORE Sonnet (Plan 13-04 ordering guarantee), so the mock_sonnet fixture is never consulted for that fixture.

    Verify the JSON files validate against PropertyListing after audit-field augmentation:
    ```bash
    uv run python -c "
    import json
    from datetime import UTC, datetime
    from decimal import Decimal
    from lib.property_listing import PropertyListing, ProvenancedMoney

    for sha_file in ['<SHA_SFH>.json', '<SHA_CONDO>.json']:
        d = json.loads(open(f'tests/fixtures/zillow/extracted/{sha_file}').read())
        # Add the audit fields the CLI would add
        d['source_url'] = 'https://www.zillow.com/homedetails/x/' + d['zpid'] + '_zpid/'
        d['fetched_at'] = datetime(2026, 5, 10, 14, 30, tzinfo=UTC).isoformat().replace('+00:00', 'Z')
        # Wrap ProvenancedMoney fields (the CLI's merge step does this; for direct validation we do it here)
        for field in ['tax_annual', 'hoa_monthly', 'insurance_estimate_annual', 'zestimate']:
            v = d.get(field)
            if v is not None:
                d[field] = {'value': v, 'provenance': 'scraped'}
        PropertyListing.model_validate(d)
    print('Both extracted dicts validate.')
    "
    ```

    Document the computed sha-16 values in the SUMMARY for traceability. Filenames will be the actual hex strings (e.g., `tests/fixtures/zillow/extracted/a1b2c3d4e5f67890.json`) — the placeholders in `files_modified` of this plan's frontmatter (`__SHA_PLACEHOLDER_1__` / `__SHA_PLACEHOLDER_2__`) are intentional; the executor replaces them with actual hex digests when computed.

    IMPORTANT: The extracted JSONs are the SHAPE Sonnet would output BEFORE the CLI's `_coerce_money_to_string` and Pydantic validation. The CLI wraps ProvenancedMoney fields itself based on Sonnet's flat output. Writing the extracted JSON as a flat dict (with money as strings, ProvenancedMoney fields as flat values) matches the actual Sonnet output shape per the EXTRACTION_PROMPT in Plan 13-03.

    The CLI's merge logic (`_merge_user_provided` for user values, and post-Sonnet logic for scraped values) wraps these into ProvenancedMoney dicts. For the integration test in Task 4, the CLI's behavior with `--user-provided` empty + mock_sonnet returning these flat dicts must produce shape-1 envelopes — which means the CLI must also wrap Sonnet's scraped money fields into ProvenancedMoney dicts with `provenance="scraped"`. If Plan 13-04's `_coerce_money_to_string` doesn't do this wrapping, this plan must add a `_wrap_scraped_provenance` step to the CLI's flow (NOTE TO EXECUTOR: check Plan 13-04's implementation; if needed, add the wrapping step before Pydantic validation as a small CLI edit; document in SUMMARY).
  </action>
  <verify>
    <automated>
      ls tests/fixtures/zillow/extracted/*.json | wc -l | grep -q '^2$' &amp;&amp;
      uv run python -c "
      import hashlib, json, pathlib
      for html in ['sfh_conforming_happy_path.html', 'condo_partial_tax_missing.html']:
          p = pathlib.Path(f'tests/fixtures/zillow/{html}')
          sha = hashlib.sha256(p.read_bytes()).hexdigest()[:16]
          jpath = pathlib.Path(f'tests/fixtures/zillow/extracted/{sha}.json')
          assert jpath.exists(), f'Missing extracted JSON for {html}: expected {sha}.json'
          d = json.loads(jpath.read_text())
          assert 'price' in d
          assert 'zip' in d
          assert 'property_type' in d
          assert 'zpid' in d
      print('OK')
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - Exactly 2 JSON files in `tests/fixtures/zillow/extracted/` (one per happy-path HTML fixture)
    - File names are 16-char hex digests
    - SHA-16 of `sfh_conforming_happy_path.html` matches one of the JSON filenames
    - SHA-16 of `condo_partial_tax_missing.html` matches the other JSON filename
    - Each JSON contains the keys: zpid, price (string), zip, property_type, beds, baths (string), sqft, year_built, tax_annual, hoa_monthly, insurance_estimate_annual, zestimate, days_on_market, list_date
    - SFH JSON: property_type="SFH", price="625000.00", zip="94110", zpid="12345678", tax_annual="7800.00"
    - Condo JSON: property_type="condo", price="450000.00", zip="98052", zpid="87654321", tax_annual=null, hoa_monthly="425.00"
    - Money values are JSON strings (not numbers) per D-19 — verify with `jq` or python: `python -c "import json; d=json.load(open('tests/fixtures/zillow/extracted/<SHA>.json')); assert isinstance(d['price'], str)"`
  </acceptance_criteria>
  <done>2 sha-keyed extracted JSON fixtures committed; mock_sonnet conftest fixture can now find a match for both happy-path HTML files; SUMMARY documents the actual sha hex values for traceability.</done>
</task>

<task type="auto">
  <name>Task 3: Update tests/fixtures/zillow/README.md with the actual file table + sha-key worked example</name>
  <files>tests/fixtures/zillow/README.md</files>
  <read_first>
    - tests/fixtures/zillow/README.md (the Wave 0 scaffold from Plan 13-00)
    - tests/fixtures/zillow/sfh_conforming_happy_path.html (just-created)
    - tests/fixtures/zillow/condo_partial_tax_missing.html (just-created)
    - tests/fixtures/zillow/blocked_perimeterx.html (just-created)
    - tests/fixtures/zillow/extracted/ (the 2 sha-keyed JSONs)
  </read_first>
  <action>
    Edit `tests/fixtures/zillow/README.md`. Keep the existing 6 sections (synthetic policy, sanitization recipe, etc.). Update the Section 1 "Files" table from the Wave-0 placeholder to the actual committed file list. Add a "Worked sha-key example" subsection right after the Files table.

    Updated Section 1:

    ```markdown
    # Zillow HTML Fixtures (Phase 13 + Phase 17)

    Pinned, sanitized Zillow listing HTML for deterministic INGEST-01..04 + PROP-01..02 tests.

    ## Files

    | File | Tested SC | Covers | Body bytes |
    |------|-----------|--------|------------|
    | `sfh_conforming_happy_path.html` | SC-1, INGEST-02 | shape-1 success on SFH; all 13 fields populated | ~5.5KB |
    | `condo_partial_tax_missing.html` | INGEST-02, D-13-MUSTHAVE-01 | shape-1 with condo HOA scraped, tax_annual=null (non-blocking) | ~5.5KB |
    | `blocked_perimeterx.html` | SC-2, D-13-BLOCK-01 | shape-3 captcha_detected ("Press &amp; Hold" phrase fires) | ~5.5KB |
    | `extracted/{sha16}.json` × 2 | INGEST-02 | pre-recorded Sonnet outputs; sha keyed; loaded by `mock_sonnet` conftest fixture | varies |

    Phase 17 expansion target: 2 more fixtures (jumbo SFH, 2-unit multifamily).

    ### Worked sha-key example

    The `mock_sonnet` conftest fixture (added in Plan 13-03; see `tests/conftest.py`) keys
    pre-recorded Sonnet output JSON by `sha256(html_bytes).hexdigest()[:16]`.

    To compute the sha for an HTML fixture:

    ```bash
    python -c "import hashlib; print(hashlib.sha256(open('tests/fixtures/zillow/sfh_conforming_happy_path.html','rb').read()).hexdigest()[:16])"
    # → prints e.g. a1b2c3d4e5f67890
    ```

    Then `tests/fixtures/zillow/extracted/a1b2c3d4e5f67890.json` is the dict that
    `mock_sonnet` returns when the CLI calls `extract_listing(html_of_sfh_fixture, ...)`.

    If the HTML changes by even one byte, the sha changes → existing JSON is no longer
    matched → mock_sonnet returns None → tests fall through to shape-2 (treated as
    "Sonnet failed to extract"). When you edit a fixture, you MUST regenerate the
    matching extracted/{sha16}.json (or commit a new sha-named file and delete the
    old one).
    ```

    Keep all other sections (Why synthetic, Capture-and-sanitize, When to regenerate,
    ANTHROPIC_API_KEY scope, What NOT to put here) from the Wave-0 README untouched.

    Verify no AI-attribution in the README itself (CLAUDE.md global rule).
  </action>
  <verify>
    <automated>
      grep -q 'sfh_conforming_happy_path.html' tests/fixtures/zillow/README.md &amp;&amp;
      grep -q 'condo_partial_tax_missing.html' tests/fixtures/zillow/README.md &amp;&amp;
      grep -q 'blocked_perimeterx.html' tests/fixtures/zillow/README.md &amp;&amp;
      grep -q 'Worked sha-key example' tests/fixtures/zillow/README.md &amp;&amp;
      grep -q 'sha256(html_bytes).hexdigest()\[:16\]' tests/fixtures/zillow/README.md &amp;&amp;
      grep -q 'Why synthetic' tests/fixtures/zillow/README.md &amp;&amp;
      grep -q 'What NOT to put here' tests/fixtures/zillow/README.md &amp;&amp;
      ! grep -iE 'co-authored-by|generated by claude|generated by ai' tests/fixtures/zillow/README.md
    </automated>
  </verify>
  <acceptance_criteria>
    - README "Files" table lists all 3 HTML fixtures BY NAME + the `extracted/{sha16}.json × 2` row
    - README contains a "Worked sha-key example" subsection with a runnable `python -c` one-liner
    - README still has all 6 original sections (synthetic policy, sanitization recipe, when to regenerate, ANTHROPIC_API_KEY scope, what NOT to put here)
    - README contains NO AI-attribution strings (case-insensitive grep returns no matches)
    - README contains the verbatim phrase `sha256(html_bytes).hexdigest()[:16]` for executor copy-paste
  </acceptance_criteria>
  <done>README updated with actual fixture inventory + sha-key worked example; synthetic-only policy preserved; ready for Phase 17 to expand the fixture set.</done>
</task>

<task type="auto">
  <name>Task 4: Add end-to-end integration test (tests/test_property_ingestion_integration.py)</name>
  <files>tests/test_property_ingestion_integration.py</files>
  <read_first>
    - tests/test_property_fetch.py (Plan 13-04 subprocess-driven CLI test pattern)
    - tests/test_property_persistence.py (Plan 13-05 round-trip pattern)
    - .claude/skills/mortgage-ops/scripts/property_fetch.py (the CLI under test)
    - lib/property_persistence.py read_latest_for_zpid (round-trip read)
    - tests/conftest.py mock_sonnet fixture
    - All 3 HTML fixtures (Task 1) + 2 extracted JSONs (Task 2)
  </read_first>
  <action>
    Create `tests/test_property_ingestion_integration.py` — the end-to-end integration test for the full Phase 13 pipeline. This is NEW (not an xfail-flip).

    The integration test runs the CLI as a subprocess against each fixture and verifies the envelope shape. For the happy-path fixtures, it also asserts the DuckDB round-trip.

    NOTE on the mock_sonnet boundary: subprocess tests cannot see in-process monkeypatching, so `mock_sonnet` doesn't reach the subprocess. Plan 13-04 owns the solution — it ships the `MORTGAGE_OPS_MOCK_SONNET=1` env-var hook + `_mock_sonnet_extract(body, project_root)` helper that sha-hashes the body and reads `tests/fixtures/zillow/extracted/{sha16}.json`. This Task assumes 13-04 has shipped it; Task 4 acceptance criteria includes a grep verification that both helpers exist in `property_fetch.py`.

    Plan 13-04 also owns `_wrap_scraped_provenanced_money(extracted)` which wraps Sonnet's flat money output (`tax_annual: "7800.00"`) into the ProvenancedMoney shape (`tax_annual: {"value": "7800.00", "provenance": "scraped"}`) and sets `*_provenance="scraped"` siblings on non-money fields. Called between `extract_listing()` and `PropertyListing.model_validate(extracted)`. Same Task-4 grep verification covers this.

    **This Task 4 adds NO new edits to `property_fetch.py`** — the helpers are 13-04's responsibility. This Task's CLI surface is read-only verification + the integration test that exercises both helpers end-to-end via subprocess.

    Now write the integration test:

    ```python
    """End-to-end integration test for Phase 13 — Zillow URL → CLI → DuckDB → read-back.

    Uses the pinned synthetic fixtures from tests/fixtures/zillow/ + the env-var-driven
    mock-Sonnet hook (MORTGAGE_OPS_MOCK_SONNET=1) so we can run the subprocess pipeline
    without ANTHROPIC_API_KEY or live API calls.
    """
    from __future__ import annotations

    import hashlib
    import json
    import os
    import subprocess
    import sys
    from pathlib import Path

    import pytest

    SCRIPT_PATH: Path = (
        Path(__file__).resolve().parent.parent
        / ".claude" / "skills" / "mortgage-ops" / "scripts" / "property_fetch.py"
    )
    FIXTURES_DIR: Path = Path(__file__).resolve().parent / "fixtures" / "zillow"


    def _run_cli_with_mock_sonnet(
        url: str,
        html_from: Path,
        extra_args: list[str] | None = None,
        cwd: Path | None = None,
        db_path: Path | None = None,
    ) -> subprocess.CompletedProcess:
        """Run property_fetch.py with MORTGAGE_OPS_MOCK_SONNET=1 + optional DB override."""
        env = dict(os.environ)
        env.pop("ANTHROPIC_API_KEY", None)
        env["MORTGAGE_OPS_MOCK_SONNET"] = "1"
        if db_path is not None:
            env["MORTGAGE_OPS_DB_PATH"] = str(db_path)  # opt-in: if CLI honors this var
        cmd = [sys.executable, str(SCRIPT_PATH), url, "--html-from", str(html_from)]
        if extra_args:
            cmd.extend(extra_args)
        return subprocess.run(cmd, capture_output=True, text=True, timeout=20, env=env, cwd=cwd)


    def test_end_to_end_sfh_happy_path_shape_1(tmp_path: Path) -> None:
        """SC-1: SFH fixture → shape-1 envelope; all 13 fields populated; tax_annual scraped."""
        html_path = FIXTURES_DIR / "sfh_conforming_happy_path.html"
        url = "https://www.zillow.com/homedetails/x/12345678_zpid/"
        result = _run_cli_with_mock_sonnet(url, html_path)
        assert result.returncode == 0, f"stderr: {result.stderr}"
        env = json.loads(result.stdout)
        assert env["awaiting_user_input"] is False
        assert env["error"] is None
        assert env["listing"] is not None
        listing = env["listing"]
        assert listing["price"] == "625000.00"
        assert listing["zip"] == "94110"
        assert listing["property_type"] == "SFH"
        assert listing["zpid"] == "12345678"
        # Scraped ProvenancedMoney
        assert listing["tax_annual"] == {"value": "7800.00", "provenance": "scraped"}
        assert listing["hoa_monthly"] is None
        # Scraped sibling provenance
        assert listing["beds"] == 3
        assert listing["beds_provenance"] == "scraped"


    def test_end_to_end_condo_partial_tax_missing_shape_1(tmp_path: Path) -> None:
        """D-13-MUSTHAVE-01: condo with tax_annual=null → still shape-1 (tax is not MUST-HAVE)."""
        html_path = FIXTURES_DIR / "condo_partial_tax_missing.html"
        url = "https://www.zillow.com/homedetails/x/87654321_zpid/"
        result = _run_cli_with_mock_sonnet(url, html_path)
        assert result.returncode == 0, f"stderr: {result.stderr}"
        env = json.loads(result.stdout)
        assert env["awaiting_user_input"] is False  # tax_annual missing is NOT blocking
        assert env["listing"]["property_type"] == "condo"
        assert env["listing"]["tax_annual"] is None
        assert env["listing"]["hoa_monthly"] == {"value": "425.00", "provenance": "scraped"}


    def test_end_to_end_blocked_captcha_shape_3(tmp_path: Path) -> None:
        """SC-2: captcha fixture → shape-3; block detection fires BEFORE Sonnet (no Sonnet cost)."""
        html_path = FIXTURES_DIR / "blocked_perimeterx.html"
        url = "https://www.zillow.com/homedetails/x/12345678_zpid/"
        result = _run_cli_with_mock_sonnet(url, html_path)
        assert result.returncode == 0
        env = json.loads(result.stdout)
        assert env["listing"] is None
        assert env["error"] == "captcha_detected"
        assert env["awaiting_user_input"] is False


    def test_end_to_end_zpid_matches_extraction_for_both_url_patterns() -> None:
        """SC-4 + INGEST-04: both /homedetails/.../{zpid}_zpid/ and /b/{zpid}_zpid/ work in pipeline."""
        html_path = FIXTURES_DIR / "sfh_conforming_happy_path.html"
        # /homedetails/ pattern
        r1 = _run_cli_with_mock_sonnet(
            "https://www.zillow.com/homedetails/x/12345678_zpid/", html_path,
        )
        assert json.loads(r1.stdout)["listing"]["zpid"] == "12345678"
        # /b/ shortlink pattern (note: HTML's __NEXT_DATA__ zpid is still 12345678 — that's
        # what mock_sonnet returns; the URL-extracted zpid would override IF the CLI is
        # url-authoritative. Per Plan 13-04, extract_zpid runs and the result is assigned
        # to extracted["zpid"] AFTER mock-Sonnet result is returned, so URL wins.)
        r2 = _run_cli_with_mock_sonnet(
            "https://www.zillow.com/b/99999999_zpid/", html_path,
        )
        env = json.loads(r2.stdout)
        assert env["listing"]["zpid"] == "99999999"  # URL-extracted ZPID overrides


    def test_end_to_end_user_provided_round_trip_uses_cache(tmp_path: Path) -> None:
        """D-13-GAPFILL-01 + Q1 cache: round-2 with --user-provided uses cached extracted dict."""
        html_path = FIXTURES_DIR / "sfh_conforming_happy_path.html"
        url = "https://www.zillow.com/homedetails/x/12345678_zpid/"
        # Round 1: fetch + populate cache
        r1 = _run_cli_with_mock_sonnet(url, html_path)
        assert r1.returncode == 0
        # Round 2: --user-provided overrides price
        r2 = _run_cli_with_mock_sonnet(
            url, html_path,
            extra_args=["--user-provided", '{"price":"700000.00"}'],
        )
        env = json.loads(r2.stdout)
        # Bare price stays flat (no provenance wrapper)
        assert env["listing"]["price"] == "700000.00"


    def test_end_to_end_database_roundtrip(tmp_path: Path) -> None:
        """SC-5: write a PropertyListing via the CLI; read back via lib.property_persistence."""
        # The CLI writes to data/mortgage-ops.duckdb by default. For test isolation,
        # use a tmp_path DB by setting MORTGAGE_OPS_DB_PATH env var IF the CLI honors it.
        # If the CLI does NOT honor a DB-path override env var, this test is best-effort:
        # we let the CLI write to the default DB, then read back from there using
        # read_latest_for_zpid(zpid).
        html_path = FIXTURES_DIR / "sfh_conforming_happy_path.html"
        url = "https://www.zillow.com/homedetails/x/12345678_zpid/"

        # Use a clean tmp_path-based DB to avoid polluting the project DB
        from lib.property_persistence import write_listing, read_latest_for_zpid

        tmp_db = tmp_path / "test_integration.duckdb"
        # Build the listing dict the same way the CLI would, then write it directly via
        # the persistence layer (the CLI's best-effort persistence path writes to the
        # default DB and we're avoiding that side-effect in this test).

        # Run the CLI to get the envelope, then re-write via the API layer:
        r = _run_cli_with_mock_sonnet(url, html_path)
        env = json.loads(r.stdout)
        assert env["listing"] is not None

        # Use the envelope listing to construct a PropertyListing, then write to tmp DB
        from lib.property_listing import PropertyListing
        listing = PropertyListing.model_validate(env["listing"])
        write_listing(listing, household_hash="test-hash", db_path=tmp_db)

        # Read back
        roundtrip = read_latest_for_zpid("12345678", db_path=tmp_db)
        assert roundtrip is not None
        assert roundtrip == listing


    def test_no_ai_attribution_in_committed_fixtures() -> None:
        """CLAUDE.md global rule — fixtures must not contain any AI-attribution strings."""
        for p in FIXTURES_DIR.glob("*.html"):
            content = p.read_text().lower()
            for forbidden in ("co-authored-by", "generated by claude", "claude code", "generated by ai"):
                assert forbidden not in content, f"{p.name} contains forbidden string: {forbidden!r}"


    def test_extracted_json_sha_keys_match_html_fixtures() -> None:
        """The sha-keyed extracted JSON filenames must match sha256(html)[:16] for each
        happy-path HTML fixture. Prevents fixture drift when an HTML edit invalidates
        the matching extracted file."""
        for html_name in ("sfh_conforming_happy_path.html", "condo_partial_tax_missing.html"):
            html_path = FIXTURES_DIR / html_name
            sha = hashlib.sha256(html_path.read_bytes()).hexdigest()[:16]
            expected_json = FIXTURES_DIR / "extracted" / f"{sha}.json"
            assert expected_json.exists(), (
                f"{html_name}: sha={sha} but {expected_json} is missing. "
                "Either the HTML was edited without regenerating the extracted JSON, "
                "or the extracted JSON was committed under a stale name."
            )
    ```

    The CLI-side env-var-driven mock-Sonnet path (MORTGAGE_OPS_MOCK_SONNET=1) ships in Plan 13-04. This Task verifies it (Task 4 verify block greps for `_mock_sonnet_extract` + `MORTGAGE_OPS_MOCK_SONNET` + `_wrap_scraped_provenanced_money` in property_fetch.py). If verification fails, return to executor with note "Plan 13-04 incomplete — re-run before continuing."

    For `test_end_to_end_database_roundtrip`, the goal is to prove SC-5 (round-trip serialization). If the CLI doesn't accept a DB-path override, we re-implement the write via the API layer to avoid polluting `data/mortgage-ops.duckdb`. That's a deliberate test-isolation choice — the CLI's persistence is best-effort (Plan 13-04 wraps in try/except), so its DB write is incidental; the contract under test is the round-trip via the documented API.
  </action>
  <verify>
    <automated>
      test -f tests/test_property_ingestion_integration.py &amp;&amp;
      grep -q 'MORTGAGE_OPS_MOCK_SONNET' .claude/skills/mortgage-ops/scripts/property_fetch.py &amp;&amp;
      grep -q '_wrap_scraped_provenanced_money\|provenance.*scraped' .claude/skills/mortgage-ops/scripts/property_fetch.py &amp;&amp;
      uv run pytest tests/test_property_ingestion_integration.py -v 2>&amp;1 | tail -15 | grep -qE 'passed' &amp;&amp;
      ! uv run pytest tests/test_property_ingestion_integration.py 2>&amp;1 | grep -qE 'failed|error'
    </automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_property_ingestion_integration.py` exists with ≥ 7 tests
    - Tests cover: SFH shape-1 happy path; condo shape-1 with tax=null; blocked shape-3 captcha; both URL patterns produce correct zpid in envelope; --user-provided round-trip; full DuckDB round-trip; no-AI-attribution-in-fixtures meta-test; sha-key drift detection meta-test
    - CLI has been edited to support `MORTGAGE_OPS_MOCK_SONNET=1` env-var hook (sha-keyed fixture loader bypasses Sonnet)
    - CLI has been edited to wrap scraped money fields into ProvenancedMoney dicts AND set sibling *_provenance="scraped" for non-money fields (this enables Pydantic validation against PropertyListing)
    - `uv run pytest tests/test_property_ingestion_integration.py -v` reports all tests passed (no failed, no error; skipped on missing optional deps is OK)
    - The sha-key drift detection meta-test passes — proving the 2 extracted JSON files match the current HTML fixtures
    - The no-AI-attribution meta-test passes — proving the committed fixtures comply with CLAUDE.md global rule
  </acceptance_criteria>
  <done>End-to-end integration test exists covering all 5 SC-1..SC-5; pipeline proven to work URL → DuckDB; CLI extended with mock-Sonnet env hook + ProvenancedMoney wrapping; meta-tests guard against fixture drift and AI-attribution leakage.</done>
</task>

<task type="auto">
  <name>Task 5: Phase 13 final pass — full suite green, requirement audit, ROADMAP update</name>
  <files>.planning/phases/13-property-ingestion/13-06-SUMMARY.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - All 6 SUMMARY files for Phase 13 (13-00..13-05)
    - .planning/REQUIREMENTS.md (requirement status table)
    - .planning/ROADMAP.md (Phase 13 section)
    - .planning/STATE.md (current state)
  </read_first>
  <action>
    Run the FULL test suite and verify no regression + all Phase 13 requirements closed:

    ```bash
    uv run pytest -x 2>&amp;1 | tail -30
    ```

    Must show:
    - All Phase 13 tests passed (tests/test_property_*.py + tests/test_property_ingestion_integration.py)
    - All Phase 1-12 tests still green (no regression)
    - No `xfailed` (all xfails from Wave 0 were removed in Waves 1-5)
    - No `XPASS` (strict=True would have caught any premature flips)

    Create `.planning/phases/13-property-ingestion/13-06-SUMMARY.md` documenting:
    - 3 HTML fixtures committed + their byte sizes
    - 2 extracted JSON files committed + their sha-16 hex names
    - Task 4 verifies (via grep) that Plan 13-04 shipped the MORTGAGE_OPS_MOCK_SONNET hook + _wrap_scraped_provenanced_money helper in property_fetch.py. NO new CLI edits in this task.
    - Integration test count + coverage matrix
    - Full Phase 13 requirement closure audit:
      - INGEST-01: ✅ closed by Plan 13-04 + integration test
      - INGEST-02: ✅ closed by Plan 13-03 + integration test
      - INGEST-03: ✅ closed by Plan 13-04 + integration test
      - INGEST-04: ✅ closed by Plan 13-02 (regex) + Plan 13-04 (CLI integration) + integration test
      - PROP-01: ✅ closed by Plan 13-01
      - PROP-02: ✅ closed by Plan 13-05 + integration test (full round-trip)
      - PERS-08: ✅ closed by Plan 13-05
    - D-13 lock surface audit:
      - D-13-GAPFILL-01: 3-shape envelope proven across all plans ✅
      - D-13-MUSTHAVE-01: MUST_HAVE = (price, zip, property_type); condo-no-tax shape-1 test proves NICE-TO-HAVE non-blocking ✅
      - D-13-REANALYSIS-01: composite PK (zpid, analyzed_at) proven via µs-delta freezegun test ✅
      - D-13-MODEL-01: Sonnet 4.6 (NOT Haiku); call inside CLI subprocess; no auto-retry; failure → None → shape-2 ✅
      - D-13-BLOCK-01: 4 signals (status/length/captcha/missing_next_data); detection fires BEFORE Sonnet ✅
    - Open Questions audit:
      - Q1 (HTML cache mechanism): skill-side cache `data/cache/property-{zpid}.json` implemented in Plan 13-04 ✅
      - Q2 (messages.parse vs create): API path chosen per Probe A result (recorded in 13-00 and 13-03 SUMMARYs) ✅
      - Q3 (Python duckdb runtime dep): added in Plan 13-00 ✅
      - Q4 (household_hash content vs structural): content SHA256 of (household.yml + profile.yml + MORTGAGE30US value) ✅
    - Realistic cost note: ~$0.16/Sonnet call at Sonnet 4.6 pricing; ~$5/mo at 30 listings/mo (corrects CONTEXT.md's outdated $0.02 Haiku-era estimate)

    Update `.planning/ROADMAP.md`:
    - Find the line `- [ ] **Phase 13: Property Ingestion**` and change to `- [x] **Phase 13: Property Ingestion**`
    - Find the "Phase 13: Property Ingestion" section
    - Update any inline status markers (e.g., add `**Status:** COMPLETED ({date})` line under the Goal)

    Update `.planning/REQUIREMENTS.md`:
    - Find each of the 7 requirement rows: INGEST-01, INGEST-02, INGEST-03, INGEST-04, PROP-01, PROP-02, PERS-08
    - Change checkbox state from `[ ]` to `[x]` if it's the inline list format
    - Find the status table (per `grep "Phase 13" .planning/REQUIREMENTS.md`) — change `Pending` to `Closed` for the 7 IDs

    Update `.planning/STATE.md`:
    - Find the position marker (e.g., "Current Phase: 13") — advance to "Current Phase: 14 (next)"
    - Add a "Phase 13 closed" entry under "Recent decisions" or wherever phase-close history is tracked
    - If STATE.md is the multi-megabyte append-only doc, just append a new section per the existing pattern; don't try to rewrite

    Run final check:
    ```bash
    uv run pytest 2>&amp;1 | tail -5
    ```
    Expected: `==== N passed in X.XXs ====` (no failures, no errors, no xfails)
  </action>
  <verify>
    <automated>
      uv run pytest -x 2>&amp;1 | tail -3 | grep -qE '[0-9]+ passed' &amp;&amp;
      ! uv run pytest 2>&amp;1 | grep -qE 'failed|error|XPASS' &amp;&amp;
      test -f .planning/phases/13-property-ingestion/13-06-SUMMARY.md &amp;&amp;
      grep -q 'INGEST-01.*closed\|INGEST-01.*✅' .planning/phases/13-property-ingestion/13-06-SUMMARY.md &amp;&amp;
      grep -q 'PERS-08.*closed\|PERS-08.*✅' .planning/phases/13-property-ingestion/13-06-SUMMARY.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest` reports all tests passed; zero failed, zero errors, zero `xfailed`, zero `XPASS`
    - 13-06-SUMMARY.md exists and contains the requirement closure audit for all 7 IDs
    - 13-06-SUMMARY.md contains the D-13 lock audit (all 5 locks proven)
    - 13-06-SUMMARY.md contains the Q1-Q4 open-questions resolution audit
    - ROADMAP.md has Phase 13 checkbox flipped to `[x]`
    - REQUIREMENTS.md has all 7 Phase 13 requirement rows updated from Pending to Closed
    - STATE.md is updated to reflect Phase 13 completion
    - Realistic Sonnet cost noted (~$0.16/call; ~$5/mo) — corrects CONTEXT.md's outdated estimate
  </acceptance_criteria>
  <done>Phase 13 fully closed: 7 requirements green, 5 D-13 locks proven, 4 open questions resolved, full test suite green, ROADMAP + REQUIREMENTS + STATE updated. Ready for Phase 14 (Property Analysis Pipeline) to begin.</done>
</task>

</tasks>

<verification>
- Full `uv run pytest` reports all green (no failed, no error, no xfail, no XPASS)
- All 7 Phase 13 requirement IDs marked closed in REQUIREMENTS.md
- ROADMAP.md Phase 13 line shows `[x]`
- 3 HTML fixtures + 2 extracted JSON fixtures committed under tests/fixtures/zillow/
- CLI accepts MORTGAGE_OPS_MOCK_SONNET=1 env var for deterministic integration testing
- Integration test asserts full pipeline: URL → block-detect → Sonnet (mocked) → Pydantic → DuckDB write → DuckDB read
- Meta-tests guard against fixture drift (sha mismatch) and AI-attribution leakage
- No PII in committed fixtures (no real phone, no real email pattern)
- All 5 SC-1..SC-5 demonstrably satisfied via integration tests
</verification>

<success_criteria>
- Phase 13 COMPLETE: 7 requirements closed, 5 locks proven, 4 open questions resolved
- Seed fixture set committed (3 HTML + 2 JSON); Phase 17 can expand without inventing a recipe
- End-to-end pipeline test green: a Zillow URL goes in, a PropertyListing comes out, DuckDB round-trips
- Synthetic-only-in-CI policy maintained: no live Sonnet calls in CI; no PII in committed fixtures
- Cost realism documented: ~$5/mo at 30 listings/mo (Sonnet 4.6 pricing)
- ROADMAP + REQUIREMENTS + STATE all reflect Phase 13 closure
- Ready for Phase 14 (Property Analysis Pipeline) to depend on `PropertyListing` model
</success_criteria>

<output>
After completion, the following SUMMARY files exist:
- 13-00-SUMMARY.md (Wave 0 scaffolding)
- 13-01-SUMMARY.md (PropertyListing model)
- 13-02-SUMMARY.md (block detector)
- 13-03-SUMMARY.md (Sonnet extractor)
- 13-04-SUMMARY.md (CLI orchestrator)
- 13-05-SUMMARY.md (DuckDB persistence)
- 13-06-SUMMARY.md (fixtures + integration + phase closure audit) — created by this plan

Recommend next: `/gsd-discuss-phase 14` to begin Phase 14 (Property Analysis Pipeline). Phase 14 depends on `lib.property_listing.PropertyListing` (now stable) and will use the Phase 13 DuckDB read API to fetch listings for analysis.
</output>
