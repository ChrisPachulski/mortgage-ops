---
id: 15-03
title: "Wave 1 — .claude/skills/mortgage-ops/scripts/property_analyze.py orchestrator (JSON-in / markdown-file-out, always-exit-0)"
phase: 15
plan: 3
type: execute
wave: 1
depends_on: [15-01]
files_modified:
  - .claude/skills/mortgage-ops/scripts/property_analyze.py
  - config/household.example.yml
autonomous: true
requirements: [MODE-03]
must_haves:
  truths:
    - ".claude/skills/mortgage-ops/scripts/property_analyze.py CLI signature: --listing <path.json> --household <path.yml> --profile <path.yml> --output-dir <dir/>; lives at SKILL FOLDER (per Phase 13 property_fetch.py precedent + RESEARCH OQ2 RESOLVED). CONTEXT.md's shorthand `scripts/property_analyze.py` is interpreted as skill-relative."
    - "Always exits 0 (Phase 12 D-12-LIVE02-01); the ONLY documented exit-2 case is argparse parse error (Phase 12 WR-02). All exceptions caught at main() boundary and converted to error envelope."
    - "Success envelope: stdout JSON `{report_path: 'reports/{NNN}-property-{zpid}-{YYYY-MM-DD}.md', verdict: 'GO|WATCH|NO_GO', error: null}` (D-15-ORCH-03)."
    - "Error envelope: stdout JSON `{report_path: null, verdict: null, error: {code: '...', message: '...'}}`. Error codes: household_yaml_invalid, profile_yaml_invalid, listing_validation_failed, fred_cache_cold, missing_county_data, analyze_internal_error, output_dir_unwritable."
    - "Pydantic ValidationError on listing JSON emits 6-key envelope on stderr (WR-02 closure shape; mirror scripts/amortize.py lines 36-60 verbatim docstring)."
    - "Pure compute — NO WebFetch, NO HTTP, NO network calls (D-15-ORCH-01; Phase 12 contract). WebFetch + URL handling lives in modes/property.md (D-15-ORCH-02)."
    - "Reads config/household.yml (Phase 4 multi-applicant schema) and maps to Phase 14 flat Household via `_load_phase14_household_from_yaml()` (Pitfall 2 — aggregate income across applicants, min FICO, etc.)."
    - "Sidecar: copies validated listing JSON to `data/property-listings/{zpid}-{YYYY-MM-DD}.json` BEFORE writing the report and rewrites --listing argv to the sidecar path before passing argv to render() — makes citation footer reproducible (Pitfall 10 + A3)."
    - "NNN sequencer: scan reports/*.md for max 3-digit prefix; increment; same-day-same-zpid duplicate appends `-r2`/`-r3` suffix (D-15-ORCH-04 + Pitfall 6)."
    - "User-Layer DATA_CONTRACT preserved: orchestrator READS config/household.yml + config/profile.yml; NEVER writes them (ASVS V4)."
    - "Path-traversal hardening: --output-dir resolved + asserted to be under project root; otherwise error code output_dir_unwritable (ASVS V5)."
    - "config/household.example.yml extended with two new optional top-level fields: `liquid_reserves` (Decimal-string default '0.00') and `preferred_down_payment_pct` (Decimal-string default '0.200000') per OQ1/A1 — non-breaking addition to User-Layer example file."
  artifacts:
    - path: .claude/skills/mortgage-ops/scripts/property_analyze.py
      provides: "PropertyListing JSON → AnalysisReport → markdown report orchestrator; always-exit-0 envelope"
      exports: ["main"]
      min_lines: 250
    - path: config/household.example.yml
      provides: "Phase 4 example extended with liquid_reserves + preferred_down_payment_pct optional fields (OQ1/A1)"
  key_links:
    - from: .claude/skills/mortgage-ops/scripts/property_analyze.py
      to: lib.property_analysis.analyze
      via: "import + call after Pydantic validation"
      pattern: "from lib.property_analysis import analyze"
    - from: .claude/skills/mortgage-ops/scripts/property_analyze.py
      to: lib.property_report.render
      via: "import + call to produce markdown string"
      pattern: "from lib.property_report import render"
    - from: .claude/skills/mortgage-ops/scripts/property_analyze.py
      to: data/property-listings/{zpid}-{date}.json
      via: "shutil.copy or json.dump of validated listing"
      pattern: "data/property-listings/"
    - from: .claude/skills/mortgage-ops/scripts/property_analyze.py
      to: reports/{NNN}-property-{zpid}-{date}.md
      via: "Path.write_text after render()"
      pattern: "reports/.*-property-"
---

<objective>
Ship `.claude/skills/mortgage-ops/scripts/property_analyze.py` at the skill folder (per Phase 13 `property_fetch.py` precedent + RESEARCH OQ2 RESOLVED) — the always-exit-0 orchestrator that turns a validated PropertyListing JSON + config/household.yml + config/profile.yml into a markdown report under `reports/{NNN}-property-{zpid}-{YYYY-MM-DD}.md`. Closes MODE-03. Every MODE-03 test in tests/test_property_analyze_cli.py (Plan 15-01) transitions from RED to GREEN.

Purpose: Compose Phase 14 `analyze()` + Plan 15-02 `render()` behind a single CLI envelope that Claude (via modes/property.md) invokes from a Bash subshell. The orchestrator owns: argparse + lazy imports (D-18 fast --help path), YAML loading + Phase-4-to-Phase-14 Household mapping (Pitfall 2), Pydantic validation gate (6-key envelope on stderr), filename sequencer (Pitfall 6), sidecar listing JSON write (Pitfall 10), Phase 12 always-exit-0 envelope discipline (D-12-LIVE02-01).

Output: One Python module (~280–350 lines) at `.claude/skills/mortgage-ops/scripts/property_analyze.py` + a 2-line addition to config/household.example.yml. The module lives next to its sibling skill scripts (`property_fetch.py`, `amortize.py`, `affordability.py`, etc.).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-property-skill-mode-report-formatter/15-CONTEXT.md
@.planning/phases/15-property-skill-mode-report-formatter/15-RESEARCH.md
@.planning/phases/15-property-skill-mode-report-formatter/15-PATTERNS.md
@.planning/phases/15-property-skill-mode-report-formatter/15-VALIDATION.md
@.planning/phases/15-property-skill-mode-report-formatter/15-01-SUMMARY.md
@CLAUDE.md

# Source-of-truth (READ-ONLY)
@lib/property_analysis.py
@lib/property_listing.py
@lib/household.py
@lib/profile.py
@lib/property_report.py

# CLI analogs (verbatim patterns)
@.claude/skills/mortgage-ops/scripts/property_fetch.py
@.claude/skills/mortgage-ops/scripts/amortize.py
@.claude/skills/mortgage-ops/scripts/_cli_helpers.py
@lib/rules/_loader.py

# User-Layer schema baseline
@config/household.example.yml
@config/profile.example.yml

# Wave 0 RED bed (must transition to GREEN)
@tests/test_property_analyze_cli.py

<interfaces>
<!-- Phase 14 surface this orchestrator composes. See 15-01 plan's <interfaces> for full schema. -->

From lib.property_analysis (called by orchestrator step E):
```python
def analyze(
    listing: PropertyListing,
    household: Household,
    profile: Profile,
    fred_mortgage_30us: Decimal | None = None,
    fred_mortgage_15us: Decimal | None = None,
) -> AnalysisReport: ...  # raises if FRED cache cold and kwargs absent
```

From lib.property_report (called by orchestrator step F — Plan 15-02 ships this):
```python
def render(report: AnalysisReport, orchestrator_argv: list[str]) -> str: ...
```

From lib.property_listing (called by orchestrator step B):
```python
class PropertyListing(BaseModel):
    @classmethod
    def model_validate_json(cls, raw: str) -> Self: ...  # raises ValidationError
    zpid: str
    # ... (full fields per Phase 13)
```

Phase 4 household.yml multi-applicant schema (per config/household.example.yml current state):
```yaml
household:
  applicants:
    - name: <str>
      gross_monthly_income: <Decimal-string>
      credit_score: <int>
    - ...
  monthly_debts:
    auto: <Decimal-string>
    student_loans: <Decimal-string>
    credit_cards: <Decimal-string>
    other: <Decimal-string>
  location:
    state_fips: <str>
    county_fips: <str>
    county_name: <str>
  # NEW (this plan adds, OQ1/A1):
  liquid_reserves: <Decimal-string>          # optional, default "0.00"
  preferred_down_payment_pct: <Decimal-string>  # optional, default "0.200000"
```

Phase 4 profile.yml (read directly via Profile(**raw)):
```yaml
profile:
  va_eligible: <bool>
  first_time_buyer: <bool>
  military_status: <str>
  filing_status: <str>
  marginal_tax_rate: <Decimal-string>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend `config/household.example.yml` with `liquid_reserves` + `preferred_down_payment_pct` optional fields (OQ1/A1)</name>
  <read_first>
    - config/household.example.yml (current Phase 4 shape — applicants[], monthly_debts, location)
    - .planning/phases/15-property-skill-mode-report-formatter/15-RESEARCH.md §"Pitfall 2" lines 475-509 (Option A: extend example yaml)
    - .planning/phases/15-property-skill-mode-report-formatter/15-CONTEXT.md §"Open Q1/A1" (recommends Option A)
    - scripts/hooks/block-user-layer.py if it exists (Phase 1 FND-04 — confirm `*.example.yml` is allowlisted)
    - lib/household.py (target field types: liquid_reserves=Decimal, preferred_down_payment_pct=Decimal default 0.20)
  </read_first>
  <action>
    Append two new optional top-level fields to config/household.example.yml under the `household:` block. Field 1: `liquid_reserves: "0.00"` (Decimal-string per CLAUDE.md money discipline). Field 2: `preferred_down_payment_pct: "0.200000"` (six-decimal Decimal-string for rate-like precision per Phase 14 convention).
    
    Each field gets a one-line comment block ABOVE it explaining the Phase 14 contract:
    ```
    # Phase 15 addition (OQ1/A1): liquid post-closing reserves in dollars; used by
    # affordability stress paths. Defaults to $0.00 if omitted.
    liquid_reserves: "0.00"
    
    # Phase 15 addition (OQ1/A1): the down-payment percentage you'd actually use
    # if buying today (3-decimal precision, e.g., 0.200000 = 20%). Drives stress
    # block + the YOUR FIT preferred-DP column bolding. Defaults to 0.200000 if omitted.
    preferred_down_payment_pct: "0.200000"
    ```
    
    The fields are OPTIONAL — orchestrator defaults them when missing (Pitfall 2 + Task 2 below).
    
    Verify the User-Layer pre-commit hook does not reject `*.example.yml` edits. If `scripts/hooks/block-user-layer.py` exists, confirm its allowlist (likely already contains `.example.yml` per Phase 1 FND-04). If the hook blocks the edit, surface the path and add `.example.yml` to its allowlist as part of this task.
    
    Do NOT modify config/household.yml (the real user-layer file is gitignored and user-owned).
  </action>
  <verify>
    <automated>grep -c "^[[:space:]]*liquid_reserves:" config/household.example.yml | awk '{exit ($1 != 1)}' && grep -c "^[[:space:]]*preferred_down_payment_pct:" config/household.example.yml | awk '{exit ($1 != 1)}' && python -c "import yaml; raw=yaml.safe_load(open('config/household.example.yml').read())['household']; assert raw['liquid_reserves']=='0.00' and raw['preferred_down_payment_pct']=='0.200000'"</automated>
  </verify>
  <acceptance_criteria>
    - config/household.example.yml contains exactly one `liquid_reserves:` line (value "0.00") and exactly one `preferred_down_payment_pct:` line (value "0.200000"), each with a Phase 15 OQ1/A1 comment block above.
    - yaml.safe_load successfully parses the file and both new fields are accessible.
    - Decimal-string discipline preserved (values are JSON/YAML strings, not naked floats/ints).
    - Pre-commit hook (if present) does not reject the edit (`.example.yml` allowlisted).
  </acceptance_criteria>
  <done>
    config/household.example.yml has the two new fields. `yaml.safe_load` returns a dict with both keys. The file remains a valid Phase 4 schema (all existing fields preserved; only additive).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship `.claude/skills/mortgage-ops/scripts/property_analyze.py` orchestrator (argparse + lazy imports + 8-step pipeline + always-exit-0 envelope)</name>
  <read_first>
    - .claude/skills/mortgage-ops/scripts/property_fetch.py (verbatim analog — lines 1-40 module docstring; lines 173-225 argparse + lazy-import; lines 419-437 outer try/except always-exit-0)
    - .claude/skills/mortgage-ops/scripts/amortize.py (verbatim analog — lines 36-60 6-key Pydantic envelope contract docstring; lines 71-103 argparse + sys.path injection; lines 149-154 ValidationError envelope emission)
    - lib/property_analysis.py (analyze() signature + AnalysisReport contract — frozen)
    - lib/property_report.py (render() signature — Plan 15-02 ships)
    - lib/property_listing.py (PropertyListing.model_validate_json — input gate)
    - lib/household.py (Household constructor — flat Phase-14 shape; constraint-checks the mapping)
    - lib/profile.py (Profile constructor — direct (**raw) shape)
    - lib/rules/_loader.py line 70 (only existing yaml.safe_load call site — pattern reference)
    - .planning/phases/15-property-skill-mode-report-formatter/15-RESEARCH.md §"Pattern 2 Orchestrator Envelope" lines 262-296 + §"Pitfall 2/6/7/8/10" + §"Code Examples" lines 661-737
    - .planning/phases/15-property-skill-mode-report-formatter/15-PATTERNS.md §"property_analyze.py" lines 94-282 (verbatim implementation excerpts) + §"Always-exit-0 envelope" lines 818-836 + §"Lazy-import" lines 840-865 + §"Path-traversal hardening" lines 918-930
    - tests/test_property_analyze_cli.py (the RED bed — every test must pass after this task)
    - config/household.example.yml (NEW shape from Task 1 above)
    - .planning/phases/15-property-skill-mode-report-formatter/15-CONTEXT.md (D-15-ORCH-01..04 + D-15-CITATION-03 footer reproducibility)
    - CLAUDE.md §"Money discipline" + §"Data Contract" + §"Skill portability"
  </read_first>
  <behavior>
    Every test in tests/test_property_analyze_cli.py transitions to GREEN:
    
    - test_help_fast_no_heavy_imports: `python .claude/skills/mortgage-ops/scripts/property_analyze.py --help` returncode 0; elapsed < 0.3s (D-18 — heavy imports happen AFTER argparse).
    - test_argparse_error_exit_2: `python .claude/skills/mortgage-ops/scripts/property_analyze.py` (no args) → returncode 2 (Phase 12 WR-02 documented exit-2).
    - test_success_envelope_shape: against synthetic eval fixture + example yamls + tmp output-dir → returncode 0; stdout JSON `{report_path: <ends .md>, verdict: <in GO/WATCH/NO_GO>, error: null}`; report file exists on disk.
    - test_error_envelope_always_exit_0: bad listing `{}` → returncode 0 (KEY); stdout has error.code + error.message; report_path null; verdict null.
    - test_pydantic_validation_envelope_on_stderr: listing with float price → stderr is JSON list with 6-key entries (type, loc, msg, input, url); stdout has error envelope; returncode 0.
    - test_filename_format: success returns report_path matching regex `reports/\d{3}-property-\w+-\d{4}-\d{2}-\d{2}\.md$`.
    - test_same_day_zpid_suffix: two consecutive runs same fixture+date → second report_path ends with `-r2.md`.
    - test_household_yaml_mapping: orchestrator loads real config/household.example.yml (NEW Phase 4 + Phase 15 shape) → returncode 0; error is None; sum-aggregation of applicant income works.
    - test_output_dir_outside_project_rejected: `--output-dir /tmp/foo` → error envelope code "output_dir_unwritable".
    - test_user_layer_files_unmodified: config/household.yml + config/profile.yml mtime unchanged after run (DATA_CONTRACT read-only ASVS V4).
    - test_sidecar_listing_written: successful run produces `data/property-listings/{zpid}-{YYYY-MM-DD}.json` (Pitfall 10 + A3); citation footer cites this stable path, not the input tempfile path.
  </behavior>
  <files>.claude/skills/mortgage-ops/scripts/property_analyze.py</files>
  <rationale>
    **Task size note (WARNING 3 — scope_sanity):** This task ships a single ~280–350-line module via a single executor task. Splitting would force artificial cross-task imports (the 8-step pipeline + 3 helpers + 1 outer wrapper are tightly coupled by argparse state, the args namespace, and the project_root sys.path injection). Per planner authority limits + executor context budget: this is a coherent unit (one Python module = one task). Executor context budget is sufficient because the helpers (`_load_phase14_household_from_yaml`, `_resolve_filename`, `_emit_error_envelope`) are all pure functions with no cross-coupling — the executor reads them top-to-bottom without re-entry. Estimated context cost: ~25-30% (one new file, fixed shape, verbatim analogs in property_fetch.py and amortize.py).
  </rationale>
  <action>
    Create .claude/skills/mortgage-ops/scripts/property_analyze.py at the SKILL FOLDER (per Phase 13 `property_fetch.py` precedent + RESEARCH OQ2 RESOLVED). Follow PATTERNS L94-282 + PATTERNS L818-865 verbatim.
    
    File structure top-to-bottom:
    
    1. `#!/usr/bin/env python3` shebang.
    2. Module docstring per PATTERNS L102-135: name MODE-03, D-15-ORCH-01..04, envelope contract (success + error JSON shapes inline), error code enumeration (household_yaml_invalid, profile_yaml_invalid, listing_validation_failed, fred_cache_cold, missing_county_data, analyze_internal_error, output_dir_unwritable), 6-key Pydantic stderr envelope reference (verbatim from amortize.py L36-60), Usage block, Always-exit-0 note (Phase 12 inheritance), exit-2 exception (argparse only).
    3. `from __future__ import annotations`.
    4. **Top-level (pre-argparse) imports only**: argparse, json, sys, datetime (UTC, datetime, date), pathlib (Path), shutil. NO lazy-loaded deps imported here (D-18).
    5. `def main() -> int:` — argparse setup per PATTERNS L150-168. Args: --listing (required, type=Path), --household (required, type=Path), --profile (required, type=Path), --output-dir (required, type=Path). `parser.parse_args()` — `--help` exits here without loading heavy deps.
    6. **AFTER `args = parser.parse_args()` — `sys.path` injection + lazy imports**. Because this file lives at `.claude/skills/mortgage-ops/scripts/property_analyze.py`, the project root is 4 levels up: `project_root = Path(__file__).resolve().parents[4]` (scripts → mortgage-ops → skills → .claude → project_root). This mirrors the Phase 13 `property_fetch.py` `sys.path` injection pattern verbatim. Then `if str(project_root) not in sys.path: sys.path.insert(0, str(project_root))`. Then lazy import: `from lib.property_analysis import analyze`; `from lib.property_listing import PropertyListing`; `from lib.household import Household`; `from lib.profile import Profile`; `from lib.property_report import render`; `from pydantic import ValidationError`; `import yaml`; `from decimal import Decimal`.
    7. **Step A — validate --output-dir (ASVS V5 path-traversal hardening per PATTERNS L920-930)**: output_dir = args.output_dir.resolve(); `if not str(output_dir).startswith(str(project_root)) or not output_dir.is_dir() or not output_dir.parent.exists(): emit_error("output_dir_unwritable", f"output-dir must be under project root and exist; got {output_dir}"); return 0`. Also reject paths containing `..` segments.
    8. **Step B — load + validate listing JSON**: try `listing = PropertyListing.model_validate_json(args.listing.read_text())` except ValidationError as e: `print(e.json(), file=sys.stderr); _emit_error_envelope("listing_validation_failed", "PropertyListing failed Pydantic validation; see stderr"); return 0`. (Note: amortize.py returns 2 here; D-15-ORCH-03 supersedes — return 0 with envelope; the dual emission preserves both contracts.)
    9. **Step C — load household.yml + map to Phase 14 flat Household via `_load_phase14_household_from_yaml(args.household)` helper**. Defined inside main() or at module level (planner's call — at module level for testability). Implementation per PATTERNS L237-260:
       ```python
       def _load_phase14_household_from_yaml(path: Path) -> Household:
           raw = yaml.safe_load(path.read_text())["household"]
           monthly_income = sum(Decimal(a["gross_monthly_income"]) for a in raw["applicants"])
           monthly_obligations = sum(
               Decimal(raw["monthly_debts"][k])
               for k in ("auto", "student_loans", "credit_cards", "other")
           )
           fico = min(int(a["credit_score"]) for a in raw["applicants"])
           return Household(
               monthly_income=monthly_income.quantize(Decimal("0.01")),
               monthly_obligations=monthly_obligations.quantize(Decimal("0.01")),
               fico=fico,
               liquid_reserves=Decimal(raw.get("liquid_reserves", "0.00")),
               state_fips=raw["location"]["state_fips"],
               county_fips=raw["location"]["county_fips"],
               county_name=raw["location"]["county_name"],
               preferred_down_payment_pct=Decimal(raw.get("preferred_down_payment_pct", "0.200000")),
           )
       ```
       Wrap call in try/except (yaml.YAMLError, KeyError, ValueError, ValidationError) → error code "household_yaml_invalid".
    10. **Step D — load profile.yml**: `raw = yaml.safe_load(args.profile.read_text())["profile"]; profile = Profile(**raw)`. Wrap in try/except → error code "profile_yaml_invalid".
    11. **Step E — call analyze()**: `report = analyze(listing, household, profile)`. Wrap in try/except. The exact FRED-cache-cold exception is `ValueError` raised from `lib.property_analysis._todays_rate_per_program` (Phase 14, lib/property_analysis.py:540) with message format `"FRED cache cold for {series_id}; run scripts/fred_cli.py get {series_id} --latest to refresh"`. Catch shape: `except ValueError as exc: if "FRED cache cold" in str(exc): _emit_error_envelope("fred_cache_cold", str(exc)); return 0; else: raise` — substring check on the message because Phase 14 chose the in-stdlib `ValueError` over a bespoke `FredCacheColdError` (lib/fred_cache.py only exposes `FredCacheLockError` and `FredCacheSchemaError`; cold-cache is signaled by the `NotImplementedError` from `get_cached_or_fetch` which `_todays_rate_per_program` re-raises as `ValueError` with the documented message — see lib/property_analysis.py:539-543). For `MissingCountyDataError`: Phase 14 already catches it internally and surfaces a `"MissingCountyDataError"` string in `report.warnings` (lib/property_analysis.py:576-577) — there is no exception to catch at this boundary. So the only Phase-14 exception the orchestrator must specifically classify is the FRED `ValueError`. Catch generic `Exception` last → error code "analyze_internal_error". Note: if the user supplies `fred_mortgage_30us`/`fred_mortgage_15us` kwargs (test injection) the FRED path is bypassed entirely; the orchestrator doesn't pass those kwargs so the cache-cold path is live in production.
    12. **Step F — write sidecar listing JSON to `data/property-listings/{zpid}-{YYYY-MM-DD}.json` (Pitfall 10 + A3 reproducible footer)**:
        ```python
        today = datetime.now(UTC).date().isoformat()
        zpid = listing.zpid
        sidecar_dir = project_root / "data" / "property-listings"
        sidecar_dir.mkdir(parents=True, exist_ok=True)
        sidecar_path = sidecar_dir / f"{zpid}-{today}.json"
        sidecar_path.write_text(listing.model_dump_json(indent=2))
        ```
    13. **Step G — rewrite argv for citation footer**: build argv for render() that cites the sidecar path, not the original tempfile path. `footer_argv = ["--listing", str(sidecar_path.relative_to(project_root)), "--household", str(args.household), "--profile", str(args.profile), "--output-dir", str(output_dir.relative_to(project_root)) + "/"]`. This makes the footer a re-runnable copy-paste (D-15-CITATION-03 + Pitfall 10 mitigation). Note: `lib.property_report.render` prefixes the argv with the full skill-relative invocation `python .claude/skills/mortgage-ops/scripts/property_analyze.py` (per Plan 15-02 `_FOOTER_PREFIX`), so the rendered footer is the complete re-runnable command from project-root cwd.
    14. **Step H — call `render(report, footer_argv)`**: get markdown body str.
    15. **Step I — resolve filename via `_resolve_filename(output_dir, zpid, today)` per PATTERNS L264-279**:
        ```python
        def _resolve_filename(out_dir: Path, zpid: str, today: str) -> Path:
            pattern = re.compile(r"^(\d{3})-")
            existing_nnns = [int(m.group(1)) for f in out_dir.glob("*.md") if (m := pattern.match(f.name))]
            next_nnn = (max(existing_nnns) + 1) if existing_nnns else 1
            base = f"{next_nnn:03d}-property-{zpid}-{today}"
            dupes = list(out_dir.glob(f"*-property-{zpid}-{today}*.md"))
            if not dupes:
                return out_dir / f"{base}.md"
            return out_dir / f"{base}-r{len(dupes) + 1}.md"
        ```
        Add `import re` to lazy-import block.
    16. **Step J — write report**: `report_path.write_text(markdown_body)`.
    17. **Step K — emit success envelope on stdout**: `print(json.dumps({"report_path": str(report_path.relative_to(project_root)), "verdict": report.verdict.level, "error": None}))`.
    18. **Step L — return 0**.
    
    Helper `_emit_error_envelope(code, message) -> None`: prints `{"report_path": None, "verdict": None, "error": {"code": code, "message": message}}` to stdout via json.dumps.
    
    **Outer try/except always-exit-0 wrapper (verbatim from PATTERNS L827-836 / property_fetch.py L419-437)**:
    ```python
    if __name__ == "__main__":
        try:
            sys.exit(main())
        except SystemExit:
            raise  # argparse parse errors (exit 2) — the one documented non-zero exit
        except Exception as exc:
            print(json.dumps({
                "report_path": None,
                "verdict": None,
                "error": {"code": "analyze_internal_error",
                          "message": f"unexpected_failure: {exc!r}"},
            }))
            sys.exit(0)
    ```
    
    **DO NOT** call `node orchestration/db-write.mjs insert-analyzed-listing` or any DuckDB write (deferred to v1.2 per CONTEXT Deferred Ideas + Pitfall 12).
    
    **DO NOT** import Anthropic SDK, WebFetch, requests, urllib, or any network library (D-15-ORCH-01 + Pitfall 7).
    
    Type annotations on every function. mypy --strict compliant.
  </action>
  <verify>
    <automated>test -f .claude/skills/mortgage-ops/scripts/property_analyze.py && uv run pytest tests/test_property_analyze_cli.py -x -v 2>&1 | tail -40 && uv run mypy --strict .claude/skills/mortgage-ops/scripts/property_analyze.py 2>&1 | tail -5 && grep -cE "import requests|import urllib|import httpx|WebFetch|from anthropic" .claude/skills/mortgage-ops/scripts/property_analyze.py | awk '{exit ($1 != 0)}' && grep -c "^def _emit_error_envelope\|^def _load_phase14_household_from_yaml\|^def _resolve_filename\|^def main" .claude/skills/mortgage-ops/scripts/property_analyze.py | awk '{exit ($1 < 4)}'</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/mortgage-ops/scripts/property_analyze.py exists at the skill folder; executable as `python .claude/skills/mortgage-ops/scripts/property_analyze.py --help`.
    - `uv run pytest tests/test_property_analyze_cli.py -x` exits 0 — all ≥11 Wave 0 MODE-03 tests pass GREEN.
    - `uv run mypy --strict .claude/skills/mortgage-ops/scripts/property_analyze.py` exits 0.
    - Zero network imports: `grep -cE "import requests|import urllib|import httpx|WebFetch|from anthropic" .claude/skills/mortgage-ops/scripts/property_analyze.py` returns 0 (D-15-ORCH-01).
    - --help completes < 0.3s (D-18 lazy-import).
    - Always-exit-0 verified via `python .claude/skills/mortgage-ops/scripts/property_analyze.py --listing /nonexistent.json --household config/household.example.yml --profile config/profile.example.yml --output-dir /tmp; echo $?` returns 0.
    - Argparse error exits 2: `python .claude/skills/mortgage-ops/scripts/property_analyze.py; echo $?` returns 2.
    - Sidecar listing JSON appears at `data/property-listings/{zpid}-{date}.json` after successful run.
    - Citation footer in produced reports references `data/property-listings/...` (NOT a tempfile path) per Pitfall 10.
    - User-Layer files unmodified after run (mtime check in test_user_layer_files_unmodified).
  </acceptance_criteria>
  <done>
    All MODE-03 tests in tests/test_property_analyze_cli.py pass GREEN. mypy --strict green. `--help` is fast. Always-exit-0 holds for every failure mode (bad listing, bad yaml, output-dir outside project, fred cold). Sidecar listing exists.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-supplied `--listing <path>` JSON → PropertyListing | Untrusted JSON file (could be tampered, malformed, or attacker-controlled in adversarial scenarios). Validated via PropertyListing.model_validate_json (Pydantic strict; Phase 13 contract). 6-key envelope on validation failure. |
| User-supplied `--household <path>` + `--profile <path>` YAML → Household/Profile | Untrusted YAML files. Loaded via `yaml.safe_load` only (NEVER `yaml.load` — V5 ASVS). Mapping function constrained: only documented Phase 4 fields read. |
| User-supplied `--output-dir <path>` → filesystem write | Untrusted path. Resolved + validated to be under project root (V5 path-traversal hardening). Rejected otherwise with `output_dir_unwritable` error envelope. |
| Orchestrator → `data/property-listings/{zpid}-{date}.json` sidecar | Trusted write under project root; created via mkdir(parents=True, exist_ok=True); zpid validated as Pydantic-strict string in PropertyListing. |
| Orchestrator → `reports/{NNN}-property-{zpid}-{date}.md` | Trusted write under output_dir (validated above); filename components all sanitized (NNN is int, zpid is Pydantic-validated str, date is ISO format). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-V1 | Spoofing | `--listing` path arg | accept | Personal-use CLI; no auth boundary. User runs as themselves. |
| T-15-V2 | Tampering | YAML deserialization injection | mitigate | `yaml.safe_load` only — never `yaml.load`. Established convention via lib/rules/_loader.py:70. ASVS V5. |
| T-15-V3 | Tampering | Path traversal via `--output-dir` (e.g., `/etc`, `../../etc`) | mitigate | output_dir.resolve() + assert startswith(project_root); reject otherwise with error envelope. Path-traversal hardening per PATTERNS L920-930. |
| T-15-V4 | Tampering | YAML / JSON content fails Pydantic validation but bubbles a traceback | mitigate | Pydantic ValidationError → 6-key envelope on stderr; error envelope on stdout; exit 0. Outer try/except wraps ALL exceptions (Phase 12 always-exit-0 + Pitfall 7). |
| T-15-V5 | Tampering | Pydantic ValidationError emits 6-key envelope on stderr (WR-02 contract) | mitigate | Verbatim from amortize.py L36-60; tests in `test_pydantic_validation_envelope_on_stderr` assert the contract. |
| T-15-V6 | Information Disclosure | User-Layer files modified by orchestrator | mitigate | DATA_CONTRACT ASVS V4: orchestrator READS config/household.yml + config/profile.yml; NEVER writes. Test `test_user_layer_files_unmodified` asserts mtime unchanged. |
| T-15-V7 | Tampering | Citation footer cites tempfile path; user copy-paste fails to reproduce | mitigate | Sidecar `data/property-listings/{zpid}-{date}.json` write before render(); footer argv rewritten to cite sidecar path (Pitfall 10 + A3). |
| T-15-V8 | Denial of Service | NNN scan loops indefinitely on a corrupted reports/ directory | accept | reports/ is gitignored and project-local; corrupt-state recovery is manual. Pattern uses `glob` + `re.match` (bounded). |
| T-15-V9 | Information Disclosure | sidecar listing JSON contains gap-fill `provenance: user_provided` values | accept | Sidecar lives under project-local `data/property-listings/` (gitignored). Same trust boundary as config/household.yml. |
</threat_model>

<verification>
- All MODE-03 tests in tests/test_property_analyze_cli.py pass GREEN.
- `python .claude/skills/mortgage-ops/scripts/property_analyze.py --help` completes < 0.3s.
- `python .claude/skills/mortgage-ops/scripts/property_analyze.py` (no args) returns exit 2; any other failure returns exit 0 with error envelope.
- `grep -cE "import requests|import urllib|import httpx|WebFetch|from anthropic" .claude/skills/mortgage-ops/scripts/property_analyze.py` returns 0.
- yaml.safe_load only: `grep -cE "yaml\.load\b" .claude/skills/mortgage-ops/scripts/property_analyze.py` returns 0.
- Path-traversal hardening present: `grep -c "output_dir_unwritable" .claude/skills/mortgage-ops/scripts/property_analyze.py` returns ≥1.
- Sidecar listing path used in footer: after a successful run, `grep -c "data/property-listings/" reports/*-property-*.md` returns ≥6 (6 footers per report).
- config/household.example.yml carries new fields (Task 1 verify).
- User-Layer file mtimes unchanged after run (asserted by test_user_layer_files_unmodified).
</verification>

<success_criteria>
- MODE-03 closed: .claude/skills/mortgage-ops/scripts/property_analyze.py orchestrates ingest → analyze → format → persist → emit envelope per D-15-ORCH-01..04.
- All Phase 12 invariants honored: always-exit-0 (except argparse), 6-key Pydantic envelope on stderr, stdout-only sourcing of envelope.
- Citation footer is re-runnable copy-paste: cites stable sidecar path under `data/property-listings/`, not the input tempfile (Pitfall 10 + A3).
- config/household.example.yml extended with two new optional fields (OQ1/A1 closed); pre-commit hook does not reject the edit.
- Household yaml mapping (Pitfall 2) implemented as documented helper; mypy strict green.
- Path-traversal hardening (ASVS V5) prevents `--output-dir` escapes from project root.
- DATA_CONTRACT ASVS V4 preserved: User-Layer files unmodified after orchestrator runs.
</success_criteria>

<output>
After completion, create `.planning/phases/15-property-skill-mode-report-formatter/15-03-SUMMARY.md` documenting:
- Final argparse signature and all 8 documented error codes
- Verbatim citation-footer text produced for a sample successful run (showing sidecar path)
- _load_phase14_household_from_yaml() mapping table (Phase 4 source field → Phase 14 target field; aggregation rule)
- _resolve_filename() behavior: NNN counter logic + same-day-zpid suffix examples
- Pitfall mitigation matrix (Pitfalls 2, 6, 7, 8, 10, 12 — which step in the orchestrator addresses each)
- pyteest output snippet showing ≥11 GREEN MODE-03 tests
- mypy --strict green confirmation
- config/household.example.yml diff summary (two new lines + their comment blocks)
- Confirmation that the orchestrator NEVER calls node orchestration/db-write.mjs (DuckDB-write deferral, Pitfall 12)
</output>
