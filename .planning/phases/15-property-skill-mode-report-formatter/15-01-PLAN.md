---
id: 15-01
title: "Wave 0 — Test Scaffolding + Synthetic Fixtures + Oracle Stub"
phase: 15
plan: 1
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_property_report.py
  - tests/test_property_analyze_cli.py
  - tests/test_skill_routing.py
  - evals/fixtures/property/sfh_conforming_001.json
  - evals/fixtures/property/sfh_conforming_001.html
  - evals/expected/property-analysis-01.json
autonomous: true
requirements: [MODE-01, MODE-02, MODE-03, RPRT-01, RPRT-02]
must_haves:
  truths:
    - "Wave 0 tests fail RED (xfail or failing assertion) because target source modules do not yet exist — per Nyquist Dimension 8: tests precede implementation."
    - "Synthetic `evals/fixtures/property/sfh_conforming_001.json` carries hand-calculated PropertyListing + Household + Profile + FRED rates + expected_response per Phase 14 fixture shape (mirror `tests/fixtures/property_analysis/sfh_conforming_king_county.json`)."
    - "Oracle `evals/expected/property-analysis-01.json` pins verdict.level via `expected_route_keywords` (per RESEARCH A4 + D-15-EVAL-03) and 3 numerics (Conv30 preferred-DP PITI, first_year_interest_conv30, verdict_reasons_count) via `expected_numbers` with decimal-point form."
    - "Subprocess CLI test stubs target `.claude/skills/mortgage-ops/scripts/property_analyze.py` (skill folder, per Phase 13 `property_fetch.py` precedent + RESEARCH OQ2 RESOLVED). CONTEXT.md's shorthand `scripts/property_analyze.py` is interpreted as the skill-relative path."
    - "Per D-15-EVAL-01: fixture lives at `evals/fixtures/property/`, NOT `tests/fixtures/zillow/` (those are Phase 17). Synthetic, no PII (Phase 11 D-02 inheritance)."
  artifacts:
    - path: tests/test_property_report.py
      provides: "Wave 0 unit-test stubs for `lib.property_report.render()` — RPRT-01 + RPRT-02 coverage"
      contains: "import pytest"
    - path: tests/test_property_analyze_cli.py
      provides: "Wave 0 subprocess-test stubs for `.claude/skills/mortgage-ops/scripts/property_analyze.py` — MODE-03 coverage"
      contains: "SCRIPT_PATH ="
    - path: tests/test_skill_routing.py
      provides: "Wave 0 filesystem-introspection stubs — MODE-01, MODE-02 coverage (tiktoken budget + Row 0 presence)"
      contains: "count_tokens"
    - path: evals/fixtures/property/sfh_conforming_001.json
      provides: "Synthetic PropertyListing + Household + Profile + fred_rates + expected_response (Phase 14 fixture shape)"
    - path: evals/fixtures/property/sfh_conforming_001.html
      provides: "2KB synthetic Zillow HTML stub with `<script id=\"__NEXT_DATA__\">` block matching the JSON fixture"
    - path: evals/expected/property-analysis-01.json
      provides: "Eval oracle: expected_route_keywords + 3 expected_numbers (per D-15-EVAL-03)"
  key_links:
    - from: tests/test_property_report.py
      to: lib.property_report.render
      via: "import lib.property_report"
      pattern: "from lib.property_report import render"
    - from: tests/test_property_analyze_cli.py
      to: .claude/skills/mortgage-ops/scripts/property_analyze.py
      via: "subprocess.run([sys.executable, SCRIPT_PATH, ...])"
      pattern: "SCRIPT_PATH.*property_analyze\\.py"
    - from: evals/expected/property-analysis-01.json
      to: evals/fixtures/property/sfh_conforming_001.json
      via: "oracle numerics derived from fixture's expected_response block"
      pattern: "conv30_preferred_dp_piti"
---

<objective>
Ship the Wave 0 RED bed for Phase 15: pytest stubs that target unbuilt source modules (formatter, orchestrator, mode-file, SKILL.md Row 0), plus the synthetic fixtures and oracle stub the rest of the phase consumes. This wave unblocks Plans 15-02 and 15-03 (Wave 1) by establishing every automated `<verify>` command those plans will reference.

Purpose: Enforce Nyquist Dimension 8 (tests precede implementation) and de-risk Pitfall 2 (household.yml mapping) + Pitfall 3 (AnalysisReport field drift) + Pitfall 6 (NNN sequencer) + Pitfall 1 (SKILL.md token budget) by encoding their expected behavior as failing tests before any production code ships.

Output: 3 test files + 2 fixture files + 1 oracle file. Quick-run command `uv run pytest tests/test_property_report.py tests/test_property_analyze_cli.py tests/test_skill_routing.py -x` returns non-zero (RED) — every test is either xfail-marked or has an assertion that production code must satisfy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-property-skill-mode-report-formatter/15-CONTEXT.md
@.planning/phases/15-property-skill-mode-report-formatter/15-RESEARCH.md
@.planning/phases/15-property-skill-mode-report-formatter/15-PATTERNS.md
@.planning/phases/15-property-skill-mode-report-formatter/15-VALIDATION.md

# Analog files (Wave 0 stubs mirror these)
@tests/fixtures/property_analysis/sfh_conforming_king_county.json
@tests/fixtures/zillow/sfh_conforming_happy_path.html
@evals/expected/amortize-01.json
@evals/expected/live-rate-injection-01.json
@evals/metrics.py

<interfaces>
<!-- Interfaces that Wave 0 stubs depend on. Wave 1 plans will implement these. -->
<!-- From lib/property_analysis.py (Phase 14, READ-ONLY, frozen). Used by formatter + orchestrator. -->

From lib/property_analysis.py:
```python
class AnalysisReport(BaseModel):  # ConfigDict(strict=True, frozen=True, extra="forbid")
    listing_snapshot: PropertyListing
    household_snapshot_hash: str
    fetched_at: datetime
    fred_mortgage_30us: Rate
    fred_mortgage_15us: Rate
    matrix: DownPaymentMatrix
    stress: StressBlock
    refi: RefiBlock
    points: PointsBlock
    tax: TaxBlock
    verdict: Verdict
    warnings: list[str]

class DownPaymentMatrix(BaseModel):
    cells: list[ProgramResult]
    programs_present: list[str]  # ["Conv30", "Conv15", "FHA30", ...]
    down_payment_pcts: list[Decimal]  # [0.03, 0.05, 0.10, 0.15, 0.20, 0.25]

class ProgramResult(BaseModel):
    program: str
    down_payment_pct: Decimal
    loan_amount: Decimal
    monthly_pi: Decimal
    monthly_tax: Decimal
    monthly_insurance: Decimal
    monthly_hoa: Decimal
    monthly_mi: Decimal
    piti: Decimal
    cash_to_close: Decimal
    dti_back: Decimal
    ltv: Decimal
    eligible: bool
    blocker_reasons: list[str]
    eligible_reasons: list[str]
    closing_costs_estimated: bool

class StressRow(BaseModel):
    program: str
    stress_kind: Literal["rate_shock", "income_shock", "arm_reset"]
    baseline_piti: Decimal
    stressed_piti: Decimal | None
    stressed_dti_back: Decimal
    breaches_dti_ceiling: bool
    blocker_reasons: list[str]

class RefiRow(BaseModel):
    program: str
    target_rate: Decimal
    scenario_label: Literal["minus_100bps", "fred_times_0_85"]
    monthly_savings: Decimal  # CAN be negative
    breakeven_months: int | None
    npv_60mo: Decimal  # CAN be negative

class PointsRow(BaseModel):
    program: str
    points_purchased: Literal[1, 2]
    rate_drop: Decimal
    simple_breakeven_months: int | None
    npv_breakeven_months: int | None
    note: str | None

class TaxBlock(BaseModel):
    first_year_interest_per_program: dict[str, Decimal]
    over_750k_cap_per_program: dict[str, bool]
    qualified_loan_limit: Decimal
    filing_status: Literal["single", "mfj", "mfs", "hoh"]

class Verdict(BaseModel):
    level: Literal["GO", "WATCH", "NO_GO"]
    headline_reason: str
    reasons: list[VerdictReason]

class VerdictReason(BaseModel):
    predicate_code: str
    computed_value: str
    program: str | None
    dp_pct: Decimal | None

def analyze(
    listing: PropertyListing,
    household: Household,
    profile: Profile,
    fred_mortgage_30us: Decimal | None = None,  # test injection
    fred_mortgage_15us: Decimal | None = None,
) -> AnalysisReport: ...
```

From lib/household.py (Phase 14):
```python
class Household(BaseModel):  # flat snapshot
    monthly_income: Decimal
    monthly_obligations: Decimal
    fico: int
    liquid_reserves: Decimal
    state_fips: str
    county_fips: str
    county_name: str
    preferred_down_payment_pct: Decimal
```

From lib/profile.py (Phase 14):
```python
class Profile(BaseModel):
    va_eligible: bool
    first_time_buyer: bool
    military_status: str
    filing_status: Literal["single", "mfj", "mfs", "hoh"]
    marginal_tax_rate: Decimal
```

From evals/metrics.py:
```python
NUMBER_REGEX = r"\$?(\d{1,3}(?:,\d{3})*|\d+)\.\d{1,4}\b"  # requires decimal point
def score_route_match(transcript: str, expected_keywords: list[str]) -> float: ...
def score_numeric_match(expected_numbers: list[dict], transcript: str) -> Literal["pass", "fail", "skip"]: ...
SC4_GATE_THRESHOLD = 0.95
```

From tests/_skill_helpers.py:
```python
def count_tokens(text: str) -> int  # tiktoken cl100k_base
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author synthetic fixture (JSON + HTML stub) + eval oracle stub</name>
  <read_first>
    - tests/fixtures/property_analysis/sfh_conforming_king_county.json (canonical AnalysisReport fixture shape — mirror its sections verbatim: listing, household, profile, fred_rates, expected_response, _meta)
    - tests/fixtures/zillow/sfh_conforming_happy_path.html (synthetic Zillow HTML precedent; copy `<script id="__NEXT_DATA__">` block shape; trim body to ≤2KB per D-15-EVAL-01)
    - evals/expected/amortize-01.json (oracle frontmatter shape)
    - evals/expected/live-rate-injection-01.json (route-keyword string-pin precedent for A4)
    - .planning/phases/15-property-skill-mode-report-formatter/15-CONTEXT.md (D-15-EVAL-01..03)
    - .planning/phases/15-property-skill-mode-report-formatter/15-RESEARCH.md §"Eval prompt frontmatter" lines 741-773 + §"Code Examples"
    - .planning/phases/15-property-skill-mode-report-formatter/15-PATTERNS.md (fixture key-table at lines 500-509)
  </read_first>
  <files>
    evals/fixtures/property/sfh_conforming_001.json,
    evals/fixtures/property/sfh_conforming_001.html,
    evals/expected/property-analysis-01.json
  </files>
  <action>
    Create three synthetic fixtures. The JSON fixture mirrors the Phase 14 sfh_conforming_king_county.json shape exactly — copy the file as a starting point, then rewrite:
    - listing.zpid="1", listing.source_url="https://www.zillow.com/homedetails/synthetic/1_zpid/", listing.fetched_at="2026-05-20T14:32:11Z"
    - listing.price=ProvenancedMoney("625000.00", provenance="scraped"), listing.zip="98101", listing.property_type="SFH", listing.tax_annual=ProvenancedMoney("6000.00", provenance="scraped"), listing.hoa_monthly=ProvenancedMoney("0.00", provenance="scraped"), listing.insurance_estimate_annual=ProvenancedMoney("1200.00", provenance="scraped")
    - household: flat Phase-14 shape monthly_income="14000.00", monthly_obligations="800.00", fico=760, liquid_reserves="80000.00", state_fips="53", county_fips="033", county_name="King", preferred_down_payment_pct="0.200000"
    - profile: va_eligible=false, first_time_buyer=false, military_status="none", filing_status="mfj", marginal_tax_rate="0.24"
    - fred_rates: MORTGAGE30US="0.065000", MORTGAGE15US="0.058000"
    - expected_response: include verdict.level, verdict.headline_reason, verdict.reasons[] (with predicate_code + computed_value), matrix.programs_present, matrix.down_payment_pcts, matrix.preferred_dp_cells[] (one ProgramResult per program at 20% DP), tax.qualified_loan_limit="750000.00", tax.over_750k_cap_per_program (per-program bool dict), tax.first_year_interest_per_program (Decimal-string dict; key Conv30 ≈ "32335.43" per RESEARCH line 765 hand-calc anchor), tax.filing_status="mfj", warnings: []
    - _meta: citation="Phase 15 D-15-EVAL-01 synthetic fixture; hand-calc anchors per RESEARCH lines 760-767", engine_version="v1.1", requirements: ["MODE-01", "MODE-02", "MODE-03", "RPRT-01", "RPRT-02"]
    - All money/rate values are JSON strings per CLAUDE.md money discipline.
    - Hand-calc anchor for conv30_preferred_dp_piti: $625k × 0.80 = $500k loan; P&I @ 6.50%/30 = $3,160.34; + tax/12 = $500.00; + ins/12 = $100.00; + HOA = $0.00; + PMI = $0.00 (LTV 80%); = $3,760.34. Use this exact value.
    
    The HTML stub: copy tests/fixtures/zillow/sfh_conforming_happy_path.html structure but TRIM to ≤2KB total. Keep only `<html><head><title>Synthetic Listing 1 - Zillow</title></head><body><script id="__NEXT_DATA__" type="application/json">{...}</script></body></html>`. The `__NEXT_DATA__` payload must transcribe the JSON fixture's listing block to Zillow wire-field names (zpid="1" as string, price=625000 as JSON number, zipcode="98101", propertyTypeDimension="SingleFamily", taxAnnualAmount=6000, hoaFee=0, zestimate=612400, yearBuilt=1995, livingArea=2200, bedrooms=4, bathrooms=2.5, daysOnZillow=14, listingDate="2026-05-06"). No PII; no real address.
    
    The oracle: mirror evals/expected/amortize-01.json shape but populate per D-15-EVAL-03 + RESEARCH A4:
    - schema_version: 1, id: "property-analysis-01", mode: "property", numeric_status: "anchored", v1_frozen_at: "2026-05-20"
    - expected_route_keywords: ["property", "property_analyze.py", "GO"]  # "GO" pins verdict.level via route-keyword path (A4)
    - expected_scripts: [{"script": "property_analyze.py", "args_must_include": ["--listing", "--household", "--profile"]}]
    - expected_numbers: 3 entries with decimal-point form per evals.metrics.NUMBER_REGEX:
      - {label: "conv30_preferred_dp_piti", value: "3760.34", tolerance: "0.50", source_script: "property_analyze.py", "numeric_status": "anchored", provenance: "stdout"}
      - {label: "first_year_interest_conv30", value: "32335.43", tolerance: "0.50", source_script: "property_analyze.py", "numeric_status": "anchored", provenance: "stdout"}
      - {label: "verdict_reasons_count", value: "1.0", tolerance: "0.0", source_script: "property_analyze.py", "numeric_status": "anchored", provenance: "stdout"}  # decimal-point form per RESEARCH line 775
    
    Note: every entry carries `numeric_status: "anchored"` (per WARNING 5 reconciliation — Wave 0 stub and Wave 2 final must agree on this field's presence). Oracle numerics may be revised in Plan 15-05 after the orchestrator actually runs against this fixture; this Wave 0 stub uses the RESEARCH hand-calc anchors and may need ±0.50 tolerance widening if analyze() produces slightly different totals.
  </action>
  <verify>
    <automated>test -f evals/fixtures/property/sfh_conforming_001.json && test -f evals/fixtures/property/sfh_conforming_001.html && test -f evals/expected/property-analysis-01.json && python -c "import json; d=json.load(open('evals/fixtures/property/sfh_conforming_001.json')); assert d['listing']['zpid']=='1' and d['listing']['property_type']=='SFH' and d['household']['preferred_down_payment_pct']=='0.200000'" && python -c "import json; d=json.load(open('evals/expected/property-analysis-01.json')); assert 'GO' in d['expected_route_keywords'] and len(d['expected_numbers'])==3 and any(n['label']=='conv30_preferred_dp_piti' for n in d['expected_numbers'])" && test $(wc -c < evals/fixtures/property/sfh_conforming_001.html) -le 2048</automated>
  </verify>
  <acceptance_criteria>
    - evals/fixtures/property/sfh_conforming_001.json validates as JSON; carries listing/household/profile/fred_rates/expected_response/_meta sections; all money fields are JSON strings.
    - evals/fixtures/property/sfh_conforming_001.html is ≤2KB; contains `<script id="__NEXT_DATA__"` block; HTML body is well-formed.
    - evals/expected/property-analysis-01.json contains expected_route_keywords with "GO" substring (A4); expected_numbers has exactly 3 entries with decimal-point values; numeric_status="anchored".
    - No PII or real addresses appear in any of the three files.
  </acceptance_criteria>
  <done>
    Three fixture/oracle files committed. `grep -l "synthetic\|123 Synthetic" evals/fixtures/property/` returns at least one file (synthetic markers present). The fixture is consumable by `evals/runner.py` (frontmatter parser can locate it via the prompt's pinned-fixture pointer, which Plan 15-05 ships).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author `tests/test_property_report.py` Wave 0 RED stubs (RPRT-01, RPRT-02)</name>
  <read_first>
    - tests/test_property_analysis.py (Phase 14 fixture-driven test pattern; analyze() invocation + AnalysisReport assertions)
    - .planning/phases/15-property-skill-mode-report-formatter/15-PATTERNS.md §"tests/test_property_report.py" lines 538-619 (verbatim test stubs)
    - .planning/phases/15-property-skill-mode-report-formatter/15-VALIDATION.md (per-task verification map RPRT-01/RPRT-02 rows)
    - .planning/phases/15-property-skill-mode-report-formatter/15-CONTEXT.md (D-15-MATRIX-01..04, D-15-CITATION-01..03)
    - tests/fixtures/property_analysis/sfh_conforming_king_county.json (real Phase 14 fixture — sample_report fixture loads from here, not from the synthetic eval fixture)
    - lib/property_analysis.py (AnalysisReport contract — frozen surface the renderer consumes)
  </read_first>
  <behavior>
    Stub tests fail RED because `lib.property_report` does not yet exist. Each test imports `from lib.property_report import render`, which raises `ModuleNotFoundError`. Mark every test with `pytest.importorskip("lib.property_report")` OR wrap import in `try/except ImportError: pytestmark = pytest.mark.xfail(reason="Wave 1 — lib/property_report.py not yet shipped", strict=False)` so the suite collects without crashing.
    
    Test coverage required (one test function per row, parametrized where useful):
    - test_render_emits_six_sections — assert ## YOUR FIT, ## RATE STRESS, ## POINTS BREAKEVEN, ## REFI OPPORTUNITY, ## TAX, ## VERDICT all appear in render() output
    - test_six_citation_footers — assert md.count("*Computed by: python .claude/skills/mortgage-ops/scripts/property_analyze.py") == 6 (per D-15-CITATION-01 + RESEARCH OQ2 RESOLVED)
    - test_footer_is_full_invocation — render with orchestrator_argv=["--listing", "data/property-listings/1-2026-05-20.json", "--household", "config/household.yml", "--profile", "config/profile.yml", "--output-dir", "reports/"]; assert "--listing data/property-listings/1-2026-05-20.json" appears in output (D-15-CITATION-03)
    - test_matrix_renders_all_cells — count "/mo" occurrences in YOUR FIT section ≥ len(report.matrix.cells); D-15-MATRIX-03 every cell appears
    - test_cell_eligibility_marks — count "✓" in YOUR FIT == number of eligible cells; D-15-MATRIX-02
    - test_preferred_dp_column_bolded — "*(your DP)*" appears in YOUR FIT section (D-15-MATRIX-04)
    - test_signed_money_negative_format — when refi.rows includes a negative monthly_savings, render outputs "-$" not "$-" (Pitfall 4)
    - test_blocker_code_truncation — ineligible cell renders blocker code stripped of parenthetical / colon-suffix per D-15-MATRIX-02 + RESEARCH line 720
    - test_arm_reset_row_under_conv30 — RATE STRESS section sorts stress.rows by (program, stress_kind) with arm_reset appearing ONLY for Conv30 (Pitfall 5)
    - test_tax_over_cap_see_cpa_callout — when tax.over_750k_cap_per_program[program]==True, "see CPA" or equivalent non-numeric callout appears in TAX section (A8); when False, no such callout
  </behavior>
  <files>tests/test_property_report.py</files>
  <action>
    Create tests/test_property_report.py with the test stubs listed in `<behavior>`. Top of file:
    
    `from __future__ import annotations` ; standard imports (json, decimal.Decimal, pathlib.Path, pytest) ; import lib.property_analysis (AnalysisReport, analyze) + lib.property_listing.PropertyListing + lib.household.Household + lib.profile.Profile.
    
    Module-level guard for the unbuilt module:
    ```python
    try:
        from lib.property_report import render  # noqa: F401
        HAS_RENDER = True
    except ImportError:
        HAS_RENDER = False
        pytestmark = pytest.mark.xfail(reason="Wave 1 — lib/property_report.py not yet shipped", strict=False)
    ```
    
    `sample_report` pytest fixture: load tests/fixtures/property_analysis/sfh_conforming_king_county.json (the Phase 14 fixture, NOT the eval fixture — the Phase 14 fixture is the canonical AnalysisReport-anchor for unit tests per PATTERNS.md L607-619). Build PropertyListing + Household + Profile from the fixture's blocks. Call analyze(...) with fred_mortgage_30us / fred_mortgage_15us kwargs from fred_rates block. Return the AnalysisReport.
    
    `sample_report_with_negative_refi` pytest fixture: mutate analyze() output to insert a synthetic RefiRow with monthly_savings=Decimal("-250.00") and npv_60mo=Decimal("-5000.00") (use model_copy(update=...) — AnalysisReport is frozen). Used by test_signed_money_negative_format.
    
    Orchestrator argv constant at module level:
    ```python
    DEFAULT_ARGV = [
        "--listing", "data/property-listings/1-2026-05-20.json",
        "--household", "config/household.yml",
        "--profile", "config/profile.yml",
        "--output-dir", "reports/",
    ]
    ```
    
    Every test function declares its requirement: `"""RPRT-01 + D-15-MATRIX-XX: <one-line behavior>"""` docstring. Use pytest parametrize for the 6-section assertion (one section name per parameter).
    
    Do NOT implement render() or any source code in this task — tests fail RED until Plan 15-02.
  </action>
  <verify>
    <automated>test -f tests/test_property_report.py && uv run pytest tests/test_property_report.py --collect-only -q 2>&1 | grep -E "test_render_emits_six_sections|test_six_citation_footers|test_matrix_renders_all_cells|test_cell_eligibility_marks|test_preferred_dp_column_bolded|test_footer_is_full_invocation|test_signed_money_negative_format|test_blocker_code_truncation|test_arm_reset_row_under_conv30|test_tax_over_cap_see_cpa_callout" | wc -l | awk '{exit ($1 < 10)}'</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_property_report.py exists; pytest collects ≥10 test functions without crashing (xfail-marked due to missing lib.property_report).
    - Every test function has a docstring naming the requirement + decision ID (RPRT-01/RPRT-02 + D-15-MATRIX-/CITATION-).
    - File imports `from lib.property_report import render` (wrapped in try/except for Wave 0 RED state).
    - `sample_report` fixture loads tests/fixtures/property_analysis/sfh_conforming_king_county.json and calls analyze() with fred_mortgage_*us kwargs.
    - `uv run pytest tests/test_property_report.py -x` exits non-zero or reports xfail (RED — passes when Plan 15-02 ships render()).
  </acceptance_criteria>
  <done>
    `grep -c "def test_" tests/test_property_report.py` returns ≥10. `grep -c "^# RPRT-\|RPRT-01\|RPRT-02" tests/test_property_report.py` returns ≥10 (every test cites a requirement). `uv run pytest tests/test_property_report.py --collect-only -q` succeeds.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author `tests/test_property_analyze_cli.py` + `tests/test_skill_routing.py` Wave 0 RED stubs (MODE-01, MODE-02, MODE-03)</name>
  <read_first>
    - tests/test_property_fetch.py (verbatim SCRIPT_PATH + _run_cli + envelope-shape assertions — MODE-03 mirrors this exactly)
    - tests/test_skill.py (token-budget + mode-file presence + grep assertions — MODE-01/MODE-02 mirrors this)
    - tests/_skill_helpers.py (count_tokens helper for tiktoken budget assertion)
    - .planning/phases/15-property-skill-mode-report-formatter/15-PATTERNS.md §"tests/test_property_analyze_cli.py" lines 622-730 + §"tests/test_skill_routing.py" lines 733-781 (verbatim test stubs)
    - .planning/phases/15-property-skill-mode-report-formatter/15-CONTEXT.md (D-15-ORCH-01..04 + D-15-ROUTE-01..03)
    - .planning/phases/15-property-skill-mode-report-formatter/15-VALIDATION.md (per-task verification map MODE-01/02/03 rows)
    - .planning/phases/15-property-skill-mode-report-formatter/15-RESEARCH.md §"Pitfall 6 NNN sequencer" lines 580-591 + §"Pitfall 1 SKILL.md budget" lines 459-473
    - config/household.example.yml (multi-applicant Phase 4 shape — household_yaml_mapping test reads this)
  </read_first>
  <behavior>
    Two separate test files. Each fails RED because target source files do not yet exist.
    
    tests/test_property_analyze_cli.py — subprocess CLI tests targeting `.claude/skills/mortgage-ops/scripts/property_analyze.py` (per Phase 13 `property_fetch.py` precedent + RESEARCH OQ2 RESOLVED):
    - SCRIPT_PATH = Path(__file__).resolve().parent.parent / ".claude" / "skills" / "mortgage-ops" / "scripts" / "property_analyze.py"
    - test_help_fast_no_heavy_imports — `--help` returncode 0 AND elapsed <0.3s (D-18 lazy-import)
    - test_argparse_error_exit_2 — no args → returncode 2 (Phase 12 WR-02 documented exit-2 exception)
    - test_success_envelope_shape — happy path against eval fixture + config/household.yml + config/profile.yml + tmp_path output-dir → returncode 0, stdout JSON has report_path (str ending .md, file exists), verdict in {GO, WATCH, NO_GO}, error is None (D-15-ORCH-03)
    - test_error_envelope_always_exit_0 — bad listing JSON (`{}`) → returncode 0 (KEY assertion: always-exit-0), stdout envelope has error.code and error.message, report_path is None, verdict is None
    - test_pydantic_validation_envelope_on_stderr — listing JSON with float price (Decimal violation) → returncode 0, stderr is JSON list with 6-key entries (type/loc/msg/input/url + optionally ctx)
    - test_filename_format — successful run produces report_path matching regex `reports/\d{3}-property-\w+-\d{4}-\d{2}-\d{2}\.md$` (D-15-ORCH-04 + RPRT-01)
    - test_same_day_zpid_suffix — two consecutive runs with same fixture+date → second report_path ends with `-r2.md` (Pitfall 6)
    - test_household_yaml_mapping — orchestrator loads real config/household.example.yml (multi-applicant Phase 4 shape) without error → returncode 0, error is None (Pitfall 2)
    - test_output_dir_outside_project_rejected — `--output-dir /tmp/foo` returns error envelope with code="output_dir_unwritable" (ASVS V5 path-traversal hardening per PATTERNS L920-930)
    - test_user_layer_files_unmodified — capture mtime of config/household.yml + config/profile.yml before orchestrator run; assert unchanged after (DATA_CONTRACT read-only)
    - test_sidecar_listing_written — successful run creates `data/property-listings/{zpid}-{YYYY-MM-DD}.json` (Pitfall 10 + A3 reproducible-footer sidecar)
    
    tests/test_skill_routing.py — filesystem-introspection tests targeting `.claude/skills/mortgage-ops/SKILL.md` + `.claude/skills/mortgage-ops/modes/property.md`:
    - skill_root pytest fixture: returns Path(".claude/skills/mortgage-ops")
    - test_skill_md_token_budget — count_tokens(SKILL.md) ≤ 4500 (MODE-02 + Pitfall 1)
    - test_property_mode_row0_present — first 200 lines of SKILL.md contain "zillow.com" + "analyze listing" + "property" + "property_analyze.py" (D-15-ROUTE-01 + MODE-01)
    - test_property_mode_file_exists — modes/property.md is a file (MODE-01)
    - test_skill_md_cross_references_property_mode — SKILL.md contains "modes/property.md" OR "property" routing reference (MODE-02)
    - test_property_mode_contains_extractor_prompt — modes/property.md contains "__NEXT_DATA__" + "WebFetch" strings (MODE-01 — extractor prompt embedded verbatim per RESEARCH Pattern 1)
    - test_property_mode_loads_shared_first — modes/property.md mentions "modes/_shared.md" + "FIRST" (per Phase 10 D-10 convention)
    - test_property_mode_dispatches_to_orchestrator — modes/property.md contains "python .claude/skills/mortgage-ops/scripts/property_analyze.py" invocation example (full skill-relative path per RESEARCH OQ2 RESOLVED)
    - test_property_mode_documents_envelope_codes — modes/property.md enumerates orchestrator error codes (household_yaml_invalid, profile_yaml_invalid, listing_validation_failed, fred_cache_cold, output_dir_unwritable) per PATTERNS L79 edge-cases convention
    
    Both files: module-level `pytestmark = pytest.mark.xfail(reason="Wave 1/2 — target source not yet shipped", strict=False)` OR per-test xfail markers. Tests fail RED until Plans 15-03 + 15-04 ship the target sources.
  </behavior>
  <files>tests/test_property_analyze_cli.py, tests/test_skill_routing.py</files>
  <action>
    Create both test files. Mirror tests/test_property_fetch.py for SCRIPT_PATH constant + _run_cli helper (PATTERNS L630-649). Mirror tests/test_skill.py for skill_root + count_tokens (PATTERNS L744-781).
    
    tests/test_property_analyze_cli.py:
    - SCRIPT_PATH = Path(__file__).resolve().parent.parent / ".claude" / "skills" / "mortgage-ops" / "scripts" / "property_analyze.py" (skill folder per Phase 13 precedent + RESEARCH OQ2 RESOLVED)
    - _run_cli(*args, env=None, timeout=15) — subprocess.run([sys.executable, str(SCRIPT_PATH), *args], capture_output=True, text=True, timeout=timeout, env=merged_env, check=False)
    - Pytest fixtures: tmp_reports (tmp_path / "reports"), golden_listing (returns Path to evals/fixtures/property/sfh_conforming_001.json), household_yml (Path to config/household.example.yml), profile_yml (Path to config/profile.example.yml)
    - All 11 test functions from `<behavior>`; each with docstring naming requirement + decision ID
    - Citation-footer prefix assertion (tests for the orchestrator's rendered report): footers begin with `*Computed by: python .claude/skills/mortgage-ops/scripts/property_analyze.py --listing data/property-listings/{zpid}-{YYYY-MM-DD}.json --household config/household.yml --profile config/profile.yml --output-dir reports/*` (full re-runnable invocation from project-root cwd per D-15-CITATION-03 + RESEARCH OQ2 RESOLVED). Assertions in test_footer_is_full_invocation use this exact prefix.
    - Module-level guard: if not SCRIPT_PATH.exists(), pytestmark = pytest.mark.xfail(reason="Wave 1 — .claude/skills/mortgage-ops/scripts/property_analyze.py not yet shipped", strict=False)
    
    tests/test_skill_routing.py:
    - from tests._skill_helpers import count_tokens (if file exists; if not, inline `tiktoken.get_encoding("cl100k_base").encode(text)` length count)
    - skill_root pytest fixture returns Path(__file__).resolve().parent.parent / ".claude" / "skills" / "mortgage-ops"
    - All 8 test functions from `<behavior>`; each with docstring naming requirement + decision ID
    - For test_property_mode_file_exists and tests touching modes/property.md: pytestmark guard `if not (skill_root / "modes" / "property.md").exists(): xfail`
    
    Do NOT create .claude/skills/mortgage-ops/scripts/property_analyze.py or modes/property.md in this task.
  </action>
  <verify>
    <automated>test -f tests/test_property_analyze_cli.py && test -f tests/test_skill_routing.py && uv run pytest tests/test_property_analyze_cli.py tests/test_skill_routing.py --collect-only -q 2>&1 | grep -cE "test_help_fast_no_heavy_imports|test_argparse_error_exit_2|test_success_envelope_shape|test_error_envelope_always_exit_0|test_pydantic_validation_envelope_on_stderr|test_filename_format|test_same_day_zpid_suffix|test_household_yaml_mapping|test_output_dir_outside_project_rejected|test_user_layer_files_unmodified|test_sidecar_listing_written|test_skill_md_token_budget|test_property_mode_row0_present|test_property_mode_file_exists|test_skill_md_cross_references_property_mode|test_property_mode_contains_extractor_prompt|test_property_mode_loads_shared_first|test_property_mode_dispatches_to_orchestrator|test_property_mode_documents_envelope_codes" | awk '{exit ($1 < 19)}'</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_property_analyze_cli.py exists; ≥11 test functions collected; SCRIPT_PATH points to .claude/skills/mortgage-ops/scripts/property_analyze.py (skill folder per RESEARCH OQ2 RESOLVED).
    - tests/test_skill_routing.py exists; ≥8 test functions collected; skill_root fixture points to .claude/skills/mortgage-ops/.
    - Every test function has a docstring naming the requirement (MODE-01/02/03) + decision ID (D-15-ROUTE-* / D-15-ORCH-* / Pitfall N).
    - Both files import without crashing (xfail-marked where source is unbuilt).
    - `uv run pytest tests/test_property_analyze_cli.py tests/test_skill_routing.py -x` exits non-zero OR all marked xfail (RED — passes when Plans 15-03 + 15-04 ship).
  </acceptance_criteria>
  <done>
    `grep -c "def test_" tests/test_property_analyze_cli.py` returns ≥11. `grep -c "def test_" tests/test_skill_routing.py` returns ≥8. `pytest --collect-only` does not crash. Subprocess test stubs use `SCRIPT_PATH` constant pointing at `.claude/skills/mortgage-ops/scripts/property_analyze.py` (skill folder per Phase 13 precedent + RESEARCH OQ2 RESOLVED).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture file → test process | Synthetic JSON/HTML fixtures are static; no remote fetch. Trust = full (committed to repo). |
| Eval oracle → metrics scorer | Oracle JSON is static; scorer trust depends on its own input validation (Phase 12). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | evals/fixtures/property/sfh_conforming_001.json | mitigate | Phase 11 D-02 synthetic-only rule: synthetic address ("123 Synthetic St" or no real address), no agent contact, synthetic zpid="1", synthetic source_url. Verified by grep `grep -i "real\|actual\|@gmail\|@yahoo\|phone:" evals/fixtures/property/*.json` returns empty. |
| T-15-02 | Tampering | tests/test_property_analyze_cli.py (subprocess CLI invocation) | accept | Tests run subprocesses under user's own credentials in dev/CI; no privilege escalation surface. Test isolation via tmp_path. |
| T-15-03 | Repudiation | Oracle hand-calc numerics (e.g., 3760.34) | mitigate | Oracle file includes `v1_frozen_at: "2026-05-20"` timestamp + `_meta.citation` field naming the RESEARCH line that derives the value. Auditable lineage. |
</threat_model>

<verification>
- All three test files exist and pytest --collect-only succeeds without import errors.
- Test functions collectively reference every Phase 15 requirement ID: `grep -E "MODE-01|MODE-02|MODE-03|RPRT-01|RPRT-02" tests/test_property_report.py tests/test_property_analyze_cli.py tests/test_skill_routing.py` returns ≥10 matches.
- Synthetic fixtures contain no PII or real listing data: `grep -iE "real|actual|@gmail|@yahoo|@hotmail|phone:|cell:" evals/fixtures/property/*.json evals/fixtures/property/*.html` returns empty.
- Oracle file passes JSON validation: `python -c "import json; d=json.load(open('evals/expected/property-analysis-01.json')); assert d['schema_version']==1 and d['numeric_status']=='anchored' and len(d['expected_numbers'])==3"` exits 0.
- Tests fail RED for unbuilt sources (Wave 0 invariant): `uv run pytest tests/test_property_report.py tests/test_property_analyze_cli.py tests/test_skill_routing.py 2>&1 | grep -E "failed|xfailed|error" | head -5` shows non-pass outcomes.
- HTML fixture size budget: `test $(wc -c < evals/fixtures/property/sfh_conforming_001.html) -le 2048` per D-15-EVAL-01.
</verification>

<success_criteria>
- `tests/test_property_report.py`, `tests/test_property_analyze_cli.py`, `tests/test_skill_routing.py` all collect ≥10/≥11/≥8 test functions respectively.
- `evals/fixtures/property/sfh_conforming_001.json` + `.html` + `evals/expected/property-analysis-01.json` all exist; JSON files validate.
- Every Phase 15 requirement ID (MODE-01, MODE-02, MODE-03, RPRT-01, RPRT-02) is referenced in a test docstring.
- Oracle anchors verdict.level via `expected_route_keywords: ["GO"]` (per A4) and 3 numerics via `expected_numbers` with decimal-point form.
- Hand-calc anchor for Conv30 preferred-DP PITI = "3760.34" recorded in oracle (re-derivable from fixture: $500k loan × 6.50%/30yr P&I = $3,160.34 + $500 tax + $100 ins = $3,760.34).
- Wave 0 RED gate confirmed: production code does not yet exist; tests fail or xfail.
</success_criteria>

<output>
After completion, create `.planning/phases/15-property-skill-mode-report-formatter/15-01-SUMMARY.md` documenting:
- Final fixture content (zpid, listing snapshot, household snapshot, profile, fred_rates, expected_response cells)
- Hand-calc derivation for conv30_preferred_dp_piti = 3760.34 (loan amount, rate, P&I, tax/insurance pickup)
- Oracle frontmatter shape (route_keywords, scripts, numbers)
- Test function count per file + requirement coverage table
- Wave 0 RED state confirmation (which tests xfail vs fail vs missing-import)
</output>
