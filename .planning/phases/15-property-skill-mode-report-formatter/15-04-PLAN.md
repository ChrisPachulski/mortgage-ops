---
id: 15-04
title: "Wave 2 — modes/property.md + SKILL.md Row 0 (URL-pin routing; ≤4500 token budget preserved)"
phase: 15
plan: 4
type: execute
wave: 2
depends_on: [15-01, 15-03]
files_modified:
  - .claude/skills/mortgage-ops/modes/property.md
  - .claude/skills/mortgage-ops/SKILL.md
autonomous: true
requirements: [MODE-01, MODE-02]
must_haves:
  truths:
    - "`.claude/skills/mortgage-ops/modes/property.md` exists and loads modes/_shared.md FIRST per Phase 10 D-10 convention."
    - "modes/property.md body contains the verbatim Pattern 1 `__NEXT_DATA__` extractor prompt (research §Pattern 1 lines 100-138 in .planning/research/v1.1-property-analysis.md)."
    - "modes/property.md instructs Claude to invoke `python .claude/skills/mortgage-ops/scripts/property_analyze.py --listing /tmp/listing-{uuid}.json --household config/household.yml --profile config/profile.yml --output-dir reports/` after WebFetch + gap-fill (per D-15-ORCH-02 + Plan 15-03 CLI signature; full skill-relative path per RESEARCH OQ2 RESOLVED — runs from project-root cwd)."
    - "modes/property.md documents recovery for each orchestrator error code (household_yaml_invalid, profile_yaml_invalid, listing_validation_failed, fred_cache_cold, output_dir_unwritable) per PATTERNS L79 edge-cases convention."
    - "modes/property.md explicitly notes 'Save Report flow skipped' (Phase 15 deviation from _shared.md D-13-01..05; DuckDB write deferred to v1.2 per PATTERNS L933-939)."
    - "SKILL.md routing table inserts Row 0 ABOVE existing 7 rows: `| Zillow URL substring (zillow.com) OR phrase 'analyze listing' | property | scripts/property_analyze.py |` (D-15-ROUTE-01 + D-15-ROUTE-02)."
    - "SKILL.md precedence list inserts Row 0 ABOVE existing 9 rows with HIGHEST precedence note; existing rows renumber by +1 (D-15-ROUTE-03)."
    - "SKILL.md ≤ 4500 cl100k tokens after Row 0 insertion (MODE-02 hard limit; Pitfall 1). Token budget assertion test in tests/test_skill_routing.py passes."
    - "SKILL.md cross-references `modes/property.md` in the existing 'Loading Additional Context' / references section (MODE-02 + Phase 10 D-09 progressive disclosure)."
  artifacts:
    - path: .claude/skills/mortgage-ops/modes/property.md
      provides: "URL-pin mode body: WebFetch + __NEXT_DATA__ extractor + gap-fill + orchestrator dispatch + error recovery"
      min_lines: 120
    - path: .claude/skills/mortgage-ops/SKILL.md
      provides: "Routing table Row 0 + precedence Row 0 inserted; cross-reference to modes/property.md added"
      contains: "zillow.com"
  key_links:
    - from: .claude/skills/mortgage-ops/SKILL.md
      to: .claude/skills/mortgage-ops/modes/property.md
      via: "routing table row + cross-reference in references section"
      pattern: "modes/property.md"
    - from: .claude/skills/mortgage-ops/modes/property.md
      to: .claude/skills/mortgage-ops/scripts/property_analyze.py
      via: "Bash invocation in 'Orchestrator dispatch' section"
      pattern: "python \\.claude/skills/mortgage-ops/scripts/property_analyze.py"
    - from: .claude/skills/mortgage-ops/modes/property.md
      to: .claude/skills/mortgage-ops/modes/_shared.md
      via: "explicit load-first instruction"
      pattern: "modes/_shared.md.*FIRST"
---

<objective>
Ship `.claude/skills/mortgage-ops/modes/property.md` and edit `.claude/skills/mortgage-ops/SKILL.md` to insert Row 0 of the routing table + precedence list. Closes MODE-01 + MODE-02. Every MODE-01/MODE-02 test in tests/test_skill_routing.py (Plan 15-01) transitions from RED to GREEN.

Purpose: Establish the user-facing entry point. A Zillow URL or the literal phrase `analyze listing` in user input MUST dispatch to property mode (D-15-ROUTE-01 — URL pin overrides everything, including explicit slash-commands). Mode body owns the WebFetch + gap-fill + Bash-dispatch flow (D-15-ORCH-02 separation: Claude does I/O, the script does compute).

Output: One new mode file (~120–180 lines) + one SKILL.md edit (insert ~3-4 new lines total: 1 routing row, 1 precedence row, optionally 1 cross-reference row). Token budget headroom: Pitfall 1 estimates ~120 cl100k tokens for the additions; hard cap is 4500.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-property-skill-mode-report-formatter/15-CONTEXT.md
@.planning/phases/15-property-skill-mode-report-formatter/15-RESEARCH.md
@.planning/phases/15-property-skill-mode-report-formatter/15-PATTERNS.md
@.planning/phases/15-property-skill-mode-report-formatter/15-VALIDATION.md
@.planning/phases/15-property-skill-mode-report-formatter/15-03-SUMMARY.md
@CLAUDE.md

# Source-of-truth (the orchestrator interface this mode body invokes)
@.claude/skills/mortgage-ops/scripts/property_analyze.py

# Mode-file analog (verbatim shape model)
@.claude/skills/mortgage-ops/modes/evaluate.md
@.claude/skills/mortgage-ops/modes/_shared.md

# SKILL.md edit target (insertion site)
@.claude/skills/mortgage-ops/SKILL.md

# Pattern 1 extractor prompt (embed verbatim)
@.planning/research/v1.1-property-analysis.md

# Wave 0 RED bed (must transition to GREEN)
@tests/test_skill_routing.py

# Token-budget helper
@tests/_skill_helpers.py

<interfaces>
<!-- Orchestrator CLI (Plan 15-03 ships this — mode body invokes it as a subshell). -->

CLI invocation (from modes/property.md "Orchestrator dispatch" section; full skill-relative path per RESEARCH OQ2 RESOLVED):
```bash
python .claude/skills/mortgage-ops/scripts/property_analyze.py \
  --listing /tmp/listing-{uuid}.json \
  --household config/household.yml \
  --profile config/profile.yml \
  --output-dir reports/
```

Stdout envelope (mode body parses this):
- success: `{"report_path": "reports/{NNN}-property-{zpid}-{YYYY-MM-DD}.md", "verdict": "GO|WATCH|NO_GO", "error": null}`
- error: `{"report_path": null, "verdict": null, "error": {"code": "<one of 7 codes>", "message": "<human>"}}`

7 error codes the mode body must enumerate recovery for:
1. listing_validation_failed — Pydantic round-trip failed; surface 6-key stderr envelope to user; offer manual paste recovery
2. household_yaml_invalid — point user to config/household.example.yml; do not auto-edit
3. profile_yaml_invalid — same; point user to config/profile.example.yml
4. fred_cache_cold — narrate: run `python .claude/skills/mortgage-ops/scripts/fred_cli.py get MORTGAGE30US --latest` and retry
5. output_dir_unwritable — should never trigger with default `--output-dir reports/` invocation; document as defensive
6. missing_county_data — listing.zip not in data/reference/conforming-limits-2026.yml; user should provide county/conforming-limit context manually
7. analyze_internal_error — catch-all; narrate error.message; ask user to copy stderr/stdout for triage
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create `.claude/skills/mortgage-ops/modes/property.md` (URL-pin mode body)</name>
  <read_first>
    - .claude/skills/mortgage-ops/modes/evaluate.md (canonical multi-step mode body — header shape, "When to invoke", "What scripts to call", "Edge cases", "RELATED REFERENCES" sections; mirror its structure verbatim)
    - .claude/skills/mortgage-ops/modes/_shared.md (load-first convention; D-13-01..05 save-report flow — property.md SKIPS this per CONTEXT Deferred Ideas + PATTERNS L933-939)
    - .planning/research/v1.1-property-analysis.md §"Pattern 1: Zillow HTML extraction" (the verbatim `__NEXT_DATA__` extractor prompt — copy verbatim into modes/property.md)
    - .planning/phases/15-property-skill-mode-report-formatter/15-RESEARCH.md §"Pattern 1: Mode Body" lines 182-260 (verbatim mode-body template)
    - .planning/phases/15-property-skill-mode-report-formatter/15-PATTERNS.md §"modes/property.md" lines 27-91 (verbatim section excerpts)
    - .planning/phases/15-property-skill-mode-report-formatter/15-CONTEXT.md (D-15-ROUTE-01..03 + D-15-ORCH-02 + Deferred Ideas line 153 — Save-Report skip)
    - .planning/phases/13-property-ingestion/13-CONTEXT.md (PropertyListing 3 MUST-HAVE fields: price, zip, property_type; gap-fill UX)
    - .claude/skills/mortgage-ops/scripts/property_analyze.py (CLI signature + error codes — Plan 15-03 ships)
    - tests/test_skill_routing.py (test stubs that must pass — exact assertion strings drive content requirements)
  </read_first>
  <action>
    Create .claude/skills/mortgage-ops/modes/property.md with structure mirroring evaluate.md (PATTERNS L33-91). Section ordering:
    
    1. **Title + load-first instruction** (mirror evaluate.md L1-4):
       ```markdown
       # Mode: property — Zillow listing → underwriting workup
       
       Loaded by SKILL.md routing per the dispatch table (Row 0: URL-pin). Read
       `modes/_shared.md` FIRST (per D-10), then this file.
       ```
    
    2. **## When to invoke** section (mirror evaluate.md L5-22):
       Route here when: (a) user message contains a `zillow.com` substring, OR (b) user said `analyze listing` (literal phrase). The URL-pin wins over refi/afford/stress/arm/amortize/evaluate/compare verbs (D-15-ROUTE-01). Example triggers (3-4 examples).
       
       Do NOT route here if: No URL AND no "analyze listing" phrase → existing precedence applies.
       
       Special case: "analyze listing" with no URL → ask "Sure — paste the Zillow URL." Do NOT auto-route to evaluate/compare/etc.
    
    3. **## Ingestion subroutine** (mirror PATTERNS L60-89 + RESEARCH L207-225):
       
       Step 1. WebFetch the user-supplied URL with the extractor prompt (next subsection).
       
       Step 2. Parse the WebFetch response as JSON. Check sentinel keys:
       - `_block_detected: true` → narrate captcha/block; switch to manual paste
       - `_truncated: true` → narrate WebFetch truncation; switch to manual paste
       - `_no_next_data: true` → same fallback
       
       Step 3. Validate via Pydantic round-trip using `python -c "from lib.property_listing import PropertyListing; PropertyListing.model_validate_json(open('/tmp/listing.json').read())"`. On error, surface the 6-key envelope; ask user to fix or paste manually.
       
       Step 4. **Interactive gap-fill for MUST-HAVE fields** (price, zip, property_type) when any are null. ONE question per missing field; user types value; merge with `provenance: user_provided` tagging per Phase 13 D-13-MUSTHAVE-01.
       
       Step 5. Write the validated listing JSON to `/tmp/listing-{uuid}.json`.
    
    4. **### Pattern 1 __NEXT_DATA__ extractor prompt (embedded verbatim)** — Copy the entire prompt block from `.planning/research/v1.1-property-analysis.md §Pattern 1 lines 100-138` verbatim. This block instructs WebFetch's Haiku sub-prompt to extract canonical PropertyListing fields from the `<script id="__NEXT_DATA__">` JSON blob. MUST contain literal strings "__NEXT_DATA__" + "WebFetch" + the JSON output schema. Test `test_property_mode_contains_extractor_prompt` asserts both strings appear.
    
    5. **## Orchestrator dispatch** (mirror PATTERNS L60-76):
       ```markdown
       Run `python .claude/skills/mortgage-ops/scripts/property_analyze.py --help` first if you have not invoked
       it this session (CLAUDE.md skill portability: read --help before calling).
       
       After WebFetch → gap-fill → tempfile write, invoke:
       
       \`\`\`bash
       python .claude/skills/mortgage-ops/scripts/property_analyze.py \
         --listing /tmp/listing-{uuid}.json \
         --household config/household.yml \
         --profile config/profile.yml \
         --output-dir reports/
       \`\`\`
       
       Parse stdout JSON envelope `{report_path, verdict, error}`. On `error != null`,
       narrate the error code + message per `_shared.md` § Error Narration Template
       AND consult the "Edge cases" section below for code-specific recovery.
       ```
    
    6. **## Result narration** — On success, narrate to user:
       > Saved underwriting report to **{report_path}**.
       > Verdict: **{verdict}** — see the file for matrix, stress, refi, points, tax, and verdict sections.
       > *(Computed by .claude/skills/mortgage-ops/scripts/property_analyze.py at {fetched_at}; citations embedded in every section.)*
    
    7. **## Edge cases** (mirror evaluate.md L119-133 — bulleted recovery list, one bullet per orchestrator error code). Enumerate ALL 7 codes from `<interfaces>`:
       - `listing_validation_failed` → 6-key envelope on stderr; offer paste-manual fallback
       - `household_yaml_invalid` → point user to config/household.example.yml; do NOT auto-edit User Layer
       - `profile_yaml_invalid` → point user to config/profile.example.yml; same
       - `fred_cache_cold` → narrate: `python .claude/skills/mortgage-ops/scripts/fred_cli.py get MORTGAGE30US --latest` then retry
       - `missing_county_data` → listing.zip absent from data/reference/conforming-limits-2026.yml; ask user for county/conforming limit
       - `output_dir_unwritable` → defensive; default invocation uses `reports/` so this should never trigger; ask user to inspect filesystem
       - `analyze_internal_error` → catch-all; narrate exc repr; ask user for copy-paste of stderr+stdout
       
       Also enumerate ingestion-side edge cases (WebFetch 403 / captcha → manual paste; no `__NEXT_DATA__` → manual paste; Pydantic validation fails after gap-fill → 6-key envelope narration).
    
    8. **## Save Report — SKIPPED** (Phase 15 deviation; PATTERNS L933-939). Explicit subsection:
       ```markdown
       ## Save Report — SKIPPED in property mode
       
       Unlike `evaluate`/`compare`/`refinance`/`affordability`/`stress`/`amortize`/`arm`
       modes (which invoke `node orchestration/db-write.mjs --insert-report` after
       writing the .md file), property mode SKIPS the DuckDB persistence step.
       The orchestrator writes the markdown report to `reports/` but does NOT call
       `orchestration/db-write.mjs`. DuckDB persistence of `analyzed_listings` rows
       is deferred to v1.2 watchlist mode (CONTEXT §Deferred Ideas).
       ```
    
    9. **## RELATED REFERENCES** footer (mirror evaluate.md L135-143):
       - `references/property-analysis.md` (Phase 18 — ships ≥250 lines doc)
       - `.planning/research/v1.1-property-analysis.md` (Pattern 1 verbatim extractor prompt source; 12 pitfalls; 8 open questions)
       
       (Load on demand only — D-09 progressive disclosure.)
    
    10. **## Worked Example** (REQUIRED — CONTEXT §Specific Ideas line 146; mirrors `modes/evaluate.md` worked-example precedent). Show end-to-end flow: (a) user pastes Zillow URL, (b) WebFetch with the embedded `__NEXT_DATA__` extractor prompt returns JSON, (c) Pydantic round-trip validates `PropertyListing`, (d) interactive gap-fill question for any missing MUST-HAVE field (price/zip/property_type), (e) orchestrator invocation `python .claude/skills/mortgage-ops/scripts/property_analyze.py --listing /tmp/listing-{uuid}.json --household config/household.yml --profile config/profile.yml --output-dir reports/`, (f) report file path emitted in stdout envelope (e.g., `reports/001-property-12345-2026-05-20.md`), (g) user-facing summary narration (verdict + report file pointer + citation cue). Keep concise (≤30 lines).
    
    File ends without trailing blank lines. Target ~120-180 lines total.
    
    No mention of `node orchestration/db-write.mjs` outside the explicit "Save Report SKIPPED" subsection (which mentions it only to disclaim).
  </action>
  <verify>
    <automated>test -f .claude/skills/mortgage-ops/modes/property.md && uv run pytest tests/test_skill_routing.py::test_property_mode_file_exists tests/test_skill_routing.py::test_property_mode_contains_extractor_prompt tests/test_skill_routing.py::test_property_mode_loads_shared_first tests/test_skill_routing.py::test_property_mode_dispatches_to_orchestrator tests/test_skill_routing.py::test_property_mode_documents_envelope_codes -x 2>&1 | tail -15 && grep -c "__NEXT_DATA__" .claude/skills/mortgage-ops/modes/property.md | awk '{exit ($1 < 1)}' && grep -c "modes/_shared.md" .claude/skills/mortgage-ops/modes/property.md | awk '{exit ($1 < 1)}' && grep -c "python \.claude/skills/mortgage-ops/scripts/property_analyze.py" .claude/skills/mortgage-ops/modes/property.md | awk '{exit ($1 < 1)}' && grep -cE "household_yaml_invalid|profile_yaml_invalid|listing_validation_failed|fred_cache_cold|output_dir_unwritable" .claude/skills/mortgage-ops/modes/property.md | awk '{exit ($1 < 5)}' && grep -c "## Worked Example" .claude/skills/mortgage-ops/modes/property.md | awk '{exit ($1 < 1)}'</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/mortgage-ops/modes/property.md exists; 5 routing-related tests in tests/test_skill_routing.py pass GREEN: test_property_mode_file_exists, test_property_mode_contains_extractor_prompt, test_property_mode_loads_shared_first, test_property_mode_dispatches_to_orchestrator, test_property_mode_documents_envelope_codes.
    - File contains literal "__NEXT_DATA__" + "WebFetch" + "modes/_shared.md" + "python .claude/skills/mortgage-ops/scripts/property_analyze.py" + at least 5 of the 7 orchestrator error codes.
    - File explicitly documents "Save Report SKIPPED" subsection citing v1.2 watchlist deferral.
    - File does NOT contain `node orchestration/db-write.mjs` except in the disclaim subsection.
    - File length 120-200 lines (concise but complete; not bloated).
    - Worked-example section present (WARNING 4 closure): `grep -c "## Worked Example" .claude/skills/mortgage-ops/modes/property.md` returns ≥1 (mirrors `modes/evaluate.md` precedent; CONTEXT §Specific Ideas worked-example recommendation is part of the mode-body deliverable).
  </acceptance_criteria>
  <done>
    modes/property.md committed. 5 mode-routing tests GREEN. Pattern 1 extractor prompt is embedded verbatim (the synthetic HTML stub from Plan 15-01 must be parseable by the prompt — sanity check by inspection).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Insert Row 0 into SKILL.md routing table + precedence list (D-15-ROUTE-01..03), preserve ≤4500 cl100k token budget</name>
  <read_first>
    - .claude/skills/mortgage-ops/SKILL.md (current 9-row precedence + 7-row routing table — Phase 10/12 baseline; line 23-31 routing table, line 37-47 precedence list per RESEARCH L380-417)
    - .planning/phases/15-property-skill-mode-report-formatter/15-RESEARCH.md §"Pattern 4 SKILL.md Row 0 Insertion" lines 379-417 + §"Pitfall 1 token budget" lines 459-473
    - .planning/phases/15-property-skill-mode-report-formatter/15-PATTERNS.md §"SKILL.md edit" lines 785-813
    - .planning/phases/15-property-skill-mode-report-formatter/15-CONTEXT.md (D-15-ROUTE-01..03)
    - tests/test_skill_routing.py (test_skill_md_token_budget + test_property_mode_row0_present + test_skill_md_cross_references_property_mode)
    - tests/_skill_helpers.py (count_tokens — tiktoken cl100k_base wrapper)
    - .planning/phases/10-claude-skill/10-CONTEXT.md (D-09 progressive disclosure — references-table cross-reference pattern)
  </read_first>
  <action>
    Edit `.claude/skills/mortgage-ops/SKILL.md` in three minimal places:
    
    **Edit A: Routing table — Insert Row 0 ABOVE existing 7 rows** (per RESEARCH L380-407):
    
    Locate the routing table (around line 23-31, starting with `| Input pattern | Mode | Script |`). Insert this new row immediately after the table header + separator (so it becomes the FIRST data row, above "Single loan + payment question"):
    
    ```
    | Zillow URL substring (`zillow.com`) OR phrase "analyze listing" | `property` | `scripts/property_analyze.py` |
    ```
    
    **Path note (RESEARCH OQ2 RESOLVED):** The Script column shows `scripts/property_analyze.py` — this is the **skill-relative path**. SKILL.md itself lives at `.claude/skills/mortgage-ops/SKILL.md`, so paths in its routing table are relative to that location. From the project-root cwd that Claude operates in, this resolves to `.claude/skills/mortgage-ops/scripts/property_analyze.py` (which is where Plan 15-03 ships the file). The skill-relative form matches the convention used by every other row in this routing table (`scripts/amortize.py`, `scripts/property_fetch.py`, etc.) — DO NOT use the project-root form here, only in the mode body's worked example and citation footers.
    
    No other rows are reordered or rewritten. The existing 7 rows shift down visually (same content, just below the new Row 0).
    
    **Edit B: Precedence list — Insert Row 0 ABOVE existing 9 rows** (per RESEARCH L411-417 + D-15-ROUTE-03):
    
    Locate the precedence list (around line 37-47, starting with `1. Explicit sub-command           → ...`). The intent of CONTEXT D-15-ROUTE-03 ("existing rows shift down by one") is **sequential renumbering with NO gap**: the new table is Row 0 (URL-pin) followed by Rows 1..9 (the existing 9 rows, renumbered from their current 1..9 positions but with their content unchanged). The CONTEXT phrasing "explicit slash-command becomes Row 2, refi verb becomes Row 3" is interpreted as: explicit slash-command moves from old-Row-1 to new-Row-2 (because URL-pin took Row 0 AND the new ordering starts at 0; visually the slash-command is now the 3rd row counting from Row 0). The locked, executor-actionable interpretation is sequential numbering Row 0..9, no gap.
    
    Final precedence table after the edit (10 rows total, contiguous numbering):
    
    ```
    0. URL pin: `zillow.com` substring OR phrase "analyze listing"
                                      → `property` (HIGHEST — overrides ALL verbs and explicit slash-commands)
    1. Explicit sub-command           → `/mortgage-ops {mode}`
    2. "refinance" / "refi" verb      → `refinance` (overrides arm/amortize/stress vocabulary)
    3. "afford" / "borrow" verb       → `affordability` (overrides amortize)
    4. (existing Row 3 content — Phase 10 baseline; copy verbatim, renumber from 3 → 4)
    5. (existing Row 4 content — Phase 10 baseline; copy verbatim, renumber from 4 → 5)
    6. (existing Row 5 content — Phase 10 baseline; copy verbatim, renumber from 5 → 6)
    7. (existing Row 6 content — Phase 10 baseline; copy verbatim, renumber from 6 → 7)
    8. (existing Row 7 content — Phase 10 baseline; copy verbatim, renumber from 7 → 8)
    9. (existing Row 8 content — Phase 10 baseline; copy verbatim, renumber from 8 → 9)
    ```
    
    The 9th existing row (current Row 9) becomes Row 9 in the new table only if it exists; if Phase 10's table has exactly 9 numbered rows (Rows 1-9), the new table has exactly 10 rows (Rows 0-9). Sequential, no gap, no skip. Per CONTEXT D-15-ROUTE-03 the existing content is copied verbatim — only the leading row-number digit changes. No "Row 1 + Row 3 + ... no Row 2" pattern. No "Row 0 + Row 2 + ..." pattern.
    
    **Edit C: Cross-reference in "Loading Additional Context" or references section** (per D-09 progressive disclosure):
    
    Locate the references / "Loading Additional Context" section (Phase 10 reference table). Add a single 1-line row OR a single bullet:
    
    ```
    - `modes/property.md` — URL-pin dispatch for Zillow listing analysis (Phase 15)
    ```
    
    Keep this addition to ONE line to preserve token budget. If the existing references section uses a markdown table format, add ONE table row.
    
    **Edit D: Token budget verification** (Pitfall 1; MODE-02 hard limit):
    
    After all three edits, verify SKILL.md is ≤ 4500 cl100k tokens via:
    ```bash
    python -c "import tiktoken; enc=tiktoken.get_encoding('cl100k_base'); print(len(enc.encode(open('.claude/skills/mortgage-ops/SKILL.md').read())))"
    ```
    
    If the count exceeds 4500: apply trim per RESEARCH L470-472 — compact the references table to 1 line per ref, or remove a less-critical row. Do NOT trim the new Row 0 or the new precedence Row 0 — those are MODE-01 load-bearing.
    
    Run `uv run pytest tests/test_skill_routing.py -x` after the edit. All Row-0/budget/cross-reference tests must pass GREEN.
    
    Total new content estimate: ~3 lines (one in routing table) + ~2 lines (one in precedence; multi-line continuation) + ~1 line (cross-reference) = ~6 lines net add. Per Assumption A7 (~120 cl100k tokens), this stays under budget.
  </action>
  <verify>
    <automated>uv run pytest tests/test_skill_routing.py::test_skill_md_token_budget tests/test_skill_routing.py::test_property_mode_row0_present tests/test_skill_routing.py::test_skill_md_cross_references_property_mode -x 2>&1 | tail -10 && python -c "import tiktoken; n=len(tiktoken.get_encoding('cl100k_base').encode(open('.claude/skills/mortgage-ops/SKILL.md').read())); assert n<=4500, f'{n} > 4500'" && grep -c "zillow.com" .claude/skills/mortgage-ops/SKILL.md | awk '{exit ($1 < 1)}' && grep -c "analyze listing" .claude/skills/mortgage-ops/SKILL.md | awk '{exit ($1 < 1)}' && grep -c "scripts/property_analyze.py" .claude/skills/mortgage-ops/SKILL.md | awk '{exit ($1 < 1)}'</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_skill_routing.py::test_skill_md_token_budget passes (≤ 4500 cl100k tokens).
    - tests/test_skill_routing.py::test_property_mode_row0_present passes (first 200 lines of SKILL.md contain zillow.com + analyze listing + property + property_analyze.py).
    - tests/test_skill_routing.py::test_skill_md_cross_references_property_mode passes.
    - SKILL.md routing table has Row 0 inserted ABOVE existing 7 rows.
    - SKILL.md precedence list has Row 0 inserted ABOVE existing 9 rows with HIGHEST precedence note (D-15-ROUTE-01 verbatim language).
    - SKILL.md cross-references modes/property.md exactly once in references/loading-additional-context section.
    - No existing routing row content is altered (D-15-ROUTE-03 — only Row 0 is new).
    - All 8 tests in tests/test_skill_routing.py pass GREEN.
  </acceptance_criteria>
  <done>
    SKILL.md has the three minimal edits (Routing Row 0, Precedence Row 0, references cross-ref). Token budget under 4500. All MODE-01/MODE-02 tests in tests/test_skill_routing.py pass GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User prompt → SKILL.md routing (Row 0 dispatcher) | Untrusted text. URL substring detection ("zillow.com") + phrase detection ("analyze listing") drive dispatch. No code execution at this stage. |
| modes/property.md → WebFetch invocation (Claude tool) | Claude's WebFetch tool boundary; not under repo control. WebFetch returns text from external URL. |
| WebFetch output → Pydantic gate | Untrusted JSON text passed through PropertyListing.model_validate_json (Phase 13 contract); validation failure → 6-key envelope; no code execution. |
| modes/property.md → Bash invocation of .claude/skills/mortgage-ops/scripts/property_analyze.py | Trusted local subprocess (Plan 15-03 always-exit-0 envelope; no network). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-R1 | Spoofing | URL-pin routing trigger | accept | `zillow.com` substring is the heuristic. Adversarial input ("the website is zillow.com but here's a different URL") is a documented residual risk; v1.2 may add tighter URL extraction. Personal-use scope. |
| T-15-R2 | Tampering | Mode file body altered to inject malicious bash | mitigate | modes/property.md is committed source under repo control; alterations require a commit; Phase 15 test `test_property_mode_dispatches_to_orchestrator` asserts the literal `python .claude/skills/mortgage-ops/scripts/property_analyze.py` invocation is the dispatched command. |
| T-15-R3 | Information Disclosure | SKILL.md routing table reveals project structure | accept | SKILL.md is committed; project structure is already public if repo is public. Routing table only references modes/ and scripts/ paths. |
| T-15-R4 | Denial of Service | Token budget overrun in SKILL.md blocks skill loading | mitigate | tests/test_skill_routing.py::test_skill_md_token_budget enforces 4500 cl100k token cap (MODE-02 hard limit; Pitfall 1). Plan trims references table compactly if needed. |
</threat_model>

<verification>
- All 8 tests in tests/test_skill_routing.py pass GREEN.
- SKILL.md token budget asserted ≤ 4500 cl100k (test + manual `python -c "import tiktoken; ..."` cross-check).
- `grep -c "zillow.com" .claude/skills/mortgage-ops/SKILL.md` returns ≥1.
- `grep -c "modes/property.md" .claude/skills/mortgage-ops/SKILL.md` returns ≥1 (cross-reference present).
- `grep -c "__NEXT_DATA__" .claude/skills/mortgage-ops/modes/property.md` returns ≥1 (extractor prompt embedded).
- `grep -c "Save Report.*SKIPPED" .claude/skills/mortgage-ops/modes/property.md` returns ≥1 (v1.2 deferral documented).
- `wc -l .claude/skills/mortgage-ops/modes/property.md` returns between 120 and 220 (right-sized; not bloated).
- No existing SKILL.md content altered (diff shows only additive insertions): `git diff .claude/skills/mortgage-ops/SKILL.md` shows +X/-0 lines (additive only).
</verification>

<success_criteria>
- MODE-01 closed: modes/property.md exists; URL-pin routing triggers documented; Pattern 1 extractor prompt embedded; orchestrator dispatch documented; 7 error-code recoveries enumerated.
- MODE-02 closed: SKILL.md Row 0 inserted (table + precedence); cross-reference to modes/property.md added; ≤4500 cl100k token budget preserved.
- All 8 tests in tests/test_skill_routing.py pass GREEN.
- modes/property.md explicitly documents Save Report SKIPPED (Phase 15 deviation from _shared.md; v1.2 watchlist deferral).
- D-15-ROUTE-01..03 honored verbatim: URL-pin is HIGHEST precedence; existing rows shifted by +1 per CONTEXT wording; no rewriting of existing routing content.
- Phase 15 token-budget Assumption A7 (~120 cl100k headroom) holds in practice.
</success_criteria>

<output>
After completion, create `.planning/phases/15-property-skill-mode-report-formatter/15-04-SUMMARY.md` documenting:
- Exact byte diff for SKILL.md (3 edits: routing Row 0 + precedence Row 0 + references cross-ref)
- Final SKILL.md tiktoken count vs 4500 budget
- modes/property.md section inventory (When to invoke / Ingestion subroutine / __NEXT_DATA__ extractor prompt / Orchestrator dispatch / Result narration / Edge cases / Save Report SKIPPED / RELATED REFERENCES)
- modes/property.md line count
- All 8 tests in tests/test_skill_routing.py GREEN evidence (pytest output snippet)
- Confirmation that Row 0 is HIGHEST precedence (overrides explicit slash-command) per D-15-ROUTE-01
- Confirmation that no existing routing-table row content was altered (D-15-ROUTE-03)
- Save Report SKIPPED rationale documented (v1.2 watchlist deferral; Pitfall 12 mitigation)
</output>
