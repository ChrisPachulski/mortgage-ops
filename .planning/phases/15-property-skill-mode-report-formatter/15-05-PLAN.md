---
id: 15-05
title: "Wave 2 — evals/prompts/property-analysis-01.md + oracle reconciliation + evals.runner smoke"
phase: 15
plan: 5
type: execute
wave: 2
depends_on: [15-01, 15-02, 15-03]
files_modified:
  - evals/prompts/property-analysis-01.md
  - evals/expected/property-analysis-01.json
autonomous: true
requirements: [MODE-03, RPRT-01, RPRT-02]
must_haves:
  truths:
    - "evals/prompts/property-analysis-01.md exists; frontmatter mirrors amortize-01.md shape (id, mode, description, expected_route_keywords, expected_scripts, expected_numbers) per D-15-EVAL-02 + RESEARCH §Eval prompt frontmatter."
    - "Oracle anchors verdict.level via `expected_route_keywords: ['property', 'property_analyze.py', 'GO']` (A4 — route-keyword path; NUMBER_REGEX requires decimal point so 'GO' string cannot be a numeric oracle) per D-15-EVAL-03 #1."
    - "Oracle anchors 3 numerics via `expected_numbers` with decimal-point form (per evals.metrics.NUMBER_REGEX): conv30_preferred_dp_piti, first_year_interest_conv30, verdict_reasons_count (D-15-EVAL-03 #2/#3/#4)."
    - "Oracle numerics are RECONCILED — derived by actually running .claude/skills/mortgage-ops/scripts/property_analyze.py against the Plan 15-01 fixture and capturing the orchestrator's emitted values; tolerances widened to ±0.50 only if hand-calc anchor diverges from analyze() output."
    - "`uv run python -m evals.runner evals/prompts/property-analysis-01.md` exits 0 with route_match + numeric_match ≥ 0.95 (ROADMAP SC-6) — eval gate green."
    - "Eval runs against the synthetic fixture from Plan 15-01 (evals/fixtures/property/sfh_conforming_001.json); NO live WebFetch in CI per Phase 11 D-02 + Phase 12 contract."
    - "Full pytest suite remains green after Phase 15 lands: `uv run pytest -x` exits 0 (Phase 14 reference: 644+ tests; Phase 15 adds ≥29 new tests across 3 files)."
  artifacts:
    - path: evals/prompts/property-analysis-01.md
      provides: "Eval prompt with frontmatter (route_keywords + scripts + numbers) + body (Zillow URL trigger)"
      contains: "expected_route_keywords"
    - path: evals/expected/property-analysis-01.json
      provides: "Eval oracle with RECONCILED numerics (re-derived from actual orchestrator output, not hand-calc estimate)"
      contains: "v1_frozen_at"
  key_links:
    - from: evals/prompts/property-analysis-01.md
      to: evals/expected/property-analysis-01.json
      via: "id field match (property-analysis-01)"
      pattern: "id: property-analysis-01"
    - from: evals/expected/property-analysis-01.json
      to: .claude/skills/mortgage-ops/scripts/property_analyze.py
      via: "expected_scripts.script: property_analyze.py (basename per evals.metrics convention)"
      pattern: "property_analyze.py"
    - from: evals/expected/property-analysis-01.json
      to: evals/fixtures/property/sfh_conforming_001.json
      via: "oracle numerics derived from fixture's expected_response cells"
      pattern: "conv30_preferred_dp_piti"
---

<objective>
Ship the v1.1 eval prompt + reconcile the Plan 15-01 oracle stub with the actual orchestrator output. Closes ROADMAP SC-6: "Eval: a new prompt `evals/prompts/property-analysis-01.md` exercises the full property mode against a pinned Zillow HTML fixture; oracle pins the expected verdict + 3 numeric fields. `python -m evals.runner` still exits 0 (route_match + numeric_match ≥ 0.95)."

Purpose: This is Phase 15's regression gate. Future SKILL.md / modes/property.md / .claude/skills/mortgage-ops/scripts/property_analyze.py / lib/property_report.py edits get caught when route_match or numeric_match falls below 0.95. The fixture is the canonical "happy-path SFH conforming King County" scenario; oracle numbers are re-derivable from the fixture via the orchestrator subprocess.

Wave 2 ordering rationale: this plan depends on Plan 15-02 (formatter must exist for the report to render) AND Plan 15-03 (orchestrator must exist to produce stdout envelope). It runs parallel to Plan 15-04 (no file overlap — Plan 15-04 owns SKILL.md + modes/property.md; Plan 15-05 owns evals/).

Output: 1 new eval prompt file (~30 lines, mostly frontmatter) + revisions to the Wave 0 oracle stub (numerics reconciled).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-property-skill-mode-report-formatter/15-CONTEXT.md
@.planning/phases/15-property-skill-mode-report-formatter/15-RESEARCH.md
@.planning/phases/15-property-skill-mode-report-formatter/15-PATTERNS.md
@.planning/phases/15-property-skill-mode-report-formatter/15-VALIDATION.md
@.planning/phases/15-property-skill-mode-report-formatter/15-01-SUMMARY.md
@.planning/phases/15-property-skill-mode-report-formatter/15-02-SUMMARY.md
@.planning/phases/15-property-skill-mode-report-formatter/15-03-SUMMARY.md

# Eval analogs (verbatim frontmatter shape)
@evals/prompts/amortize-01.md
@evals/prompts/live-rate-injection-01.md
@evals/expected/amortize-01.json
@evals/expected/live-rate-injection-01.json
@evals/runner.py
@evals/metrics.py

# Wave 0 stub + Wave 1 outputs
@evals/fixtures/property/sfh_conforming_001.json
@evals/expected/property-analysis-01.json
@.claude/skills/mortgage-ops/scripts/property_analyze.py
@lib/property_report.py

<interfaces>
<!-- evals.runner expected behavior (from Phase 12 — frozen). -->

```python
# evals/runner.py
SC4_GATE_THRESHOLD = 0.95
def run_replay_stub(prompt_path: Path, expected_path: Path) -> dict: ...  # used in CI replay mode
def synthesize_stub_transcript(prompt_meta, expected_meta) -> str: ...    # stitches expected_scripts + expected_numbers into a fake transcript
# Aggregates: numeric_pass / (numeric_pass + numeric_fail) ≥ 0.95 AND route_match ≥ 0.95
```

```python
# evals/metrics.py
NUMBER_REGEX = r"\$?(\d{1,3}(?:,\d{3})*|\d+)\.\d{1,4}\b"  # requires decimal point
def score_route_match(transcript: str, expected_keywords: list[str]) -> float
def score_numeric_match(expected_numbers: list[dict], transcript: str) -> Literal["pass", "fail", "skip"]
def _sourced_via_stdout(label: str, transcript: str) -> bool  # D-12-SC3-01 stdout-only sourcing gate
```

Frontmatter schema (mirror amortize-01.md verbatim shape):
```yaml
---
id: property-analysis-01
mode: property
description: <one-line>
expected_route_keywords: [<list of substrings the transcript must contain>]
expected_scripts:
  - script: <basename>
    args_must_include: [<list of CLI flags that must appear in the transcript>]
expected_numbers:
  - label: <stable_key>
    value: "<Decimal-string with mandatory decimal point per NUMBER_REGEX>"
    tolerance: "<Decimal-string>"
    source_script: <basename>
    provenance: stdout
---
<prompt body — the user-facing prompt that triggers the mode>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author `evals/prompts/property-analysis-01.md` + reconcile `evals/expected/property-analysis-01.json` against live orchestrator output</name>
  <read_first>
    - evals/prompts/amortize-01.md (canonical frontmatter shape — id, mode, description, expected_route_keywords, expected_scripts, expected_numbers — mirror verbatim)
    - evals/prompts/live-rate-injection-01.md (route-keyword string-pin precedent — line 6 pins "6.50" via expected_route_keywords; same pattern for "GO" verdict pinning per A4)
    - evals/expected/amortize-01.json (canonical oracle shape — mirror verbatim)
    - evals/runner.py (run_replay_stub + synthesize_stub_transcript flow — understand how oracle numbers map to scored transcript)
    - evals/metrics.py (NUMBER_REGEX + score_route_match + score_numeric_match + _sourced_via_stdout — score_numeric_match requires decimal point AND stdout sourcing per D-12-SC3-01)
    - evals/fixtures/property/sfh_conforming_001.json (Plan 15-01 fixture — contains expected_response.matrix.preferred_dp_cells + expected_response.tax + expected_response.verdict that determine oracle anchors)
    - evals/expected/property-analysis-01.json (Plan 15-01 stub — Wave 0 placeholder values that Task 1 RECONCILES with actual orchestrator output)
    - .claude/skills/mortgage-ops/scripts/property_analyze.py (Plan 15-03 — runs against the fixture; orchestrator stdout determines the actual numerics oracle must pin)
    - lib/property_report.py (Plan 15-02 — produces the markdown; the markdown text feeds the transcript that score_numeric_match scans)
    - .planning/phases/15-property-skill-mode-report-formatter/15-RESEARCH.md §"Eval prompt frontmatter" lines 741-775 + §"Open Question 4" lines 844-847
    - .planning/phases/15-property-skill-mode-report-formatter/15-PATTERNS.md §"property-analysis-01.md" lines 418-460 + §"property-analysis-01.json" lines 464-490
    - .planning/phases/15-property-skill-mode-report-formatter/15-CONTEXT.md (D-15-EVAL-01..03)
    - .planning/phases/12-fred-eval/12-CONTEXT.md (D-12-SC4-01 three-bucket numeric scoring; D-12-SC3-01 stdout-only sourcing — both gates the oracle must pass)
  </read_first>
  <behavior>
    Two artifacts. Task includes a RECONCILIATION step: re-derive the oracle numerics from actual orchestrator output (not hand-calc anchor), because tiny rounding differences in analyze()'s P&I + tax pickup arithmetic can shift the numerics by a few cents from the Wave 0 hand-calc.
    
    Artifact 1: `evals/prompts/property-analysis-01.md` (new file). Mirror amortize-01.md frontmatter shape exactly. The prompt body is one paragraph that simulates the user pasting a Zillow URL.
    
    Artifact 2: `evals/expected/property-analysis-01.json` (existing Wave 0 stub from Plan 15-01 — UPDATE in place). Reconcile numerics with actual orchestrator output captured by running .claude/skills/mortgage-ops/scripts/property_analyze.py against the fixture (substitute the fixture for the listing arg).
    
    Expected behavior after this task:
    - `uv run python -m evals.runner evals/prompts/property-analysis-01.md` exits 0
    - route_match for prompt-01 = 1.0 (all 3 keywords "property", "property_analyze.py", "GO" present in the synthesized transcript)
    - numeric_match for prompt-01 = pass (all 3 numerics within tolerance, all sourced via stdout per D-12-SC3-01)
    - Phase 12's SC4_GATE_THRESHOLD=0.95 holds: across all existing prompts + this new one, numeric_pass / (numeric_pass + numeric_fail) ≥ 0.95
    - Full pytest suite stays green: `uv run pytest -x` exits 0
  </behavior>
  <files>evals/prompts/property-analysis-01.md, evals/expected/property-analysis-01.json</files>
  <action>
    **Step 1 — Capture actual orchestrator output for reconciliation:**
    
    Run the orchestrator (at `.claude/skills/mortgage-ops/scripts/property_analyze.py` per RESEARCH OQ2 RESOLVED) against the Plan 15-01 fixture to capture the actual numerics that will appear in stdout + the rendered markdown:
    
    ```bash
    # Use a temp listing JSON that strips the eval-fixture-only fields (expected_response, _meta, fred_rates, household, profile) and keeps only the listing block as a standalone PropertyListing.
    mkdir -p /tmp/eval-recon
    uv run python -c "
    import json
    raw = json.load(open('evals/fixtures/property/sfh_conforming_001.json'))
    listing_only = raw['listing']
    open('/tmp/eval-recon/listing.json', 'w').write(json.dumps(listing_only))
    "
    uv run python .claude/skills/mortgage-ops/scripts/property_analyze.py \
      --listing /tmp/eval-recon/listing.json \
      --household config/household.example.yml \
      --profile config/profile.example.yml \
      --output-dir /tmp/eval-recon/ \
      2>/tmp/eval-recon/stderr.json \
      | tee /tmp/eval-recon/stdout.json
    cat /tmp/eval-recon/stdout.json
    ```
    
    From the captured stdout + the produced markdown report:
    - Extract the actual Conv30 preferred-DP PITI displayed in the markdown YOUR FIT matrix (search for `$X,XXX/mo`-style strings on the Conv30 row + 20% DP cell)
    - Extract the actual `first_year_interest_per_program["Conv30"]` value from the markdown TAX section
    - Count the actual `verdict.reasons` entries from the markdown VERDICT section
    
    These are the ACTUAL numerics that must populate the oracle.
    
    **Step 2 — Create `evals/prompts/property-analysis-01.md`:**
    
    Frontmatter (mirror amortize-01.md shape + D-15-EVAL-02/03 + RESEARCH L741-773):
    
    ```yaml
    ---
    id: property-analysis-01
    mode: property
    description: Full property analysis end-to-end — SFH conforming King County WA; closes ROADMAP SC-6. Synthetic JSON fixture per Phase 11 D-02 (no live WebFetch in CI).
    expected_route_keywords:
      - property
      - property_analyze.py
      - "GO"           # D-15-EVAL-03 #1: verdict.level via route-keyword (A4 — NUMBER_REGEX requires decimal point so non-numeric verdict strings cannot be expected_numbers)
    expected_scripts:
      - script: property_analyze.py
        args_must_include:
          - "--listing"
          - "--household"
          - "--profile"
          - "--output-dir"
    expected_numbers:
      - label: conv30_preferred_dp_piti
        value: "<RECONCILED — paste actual orchestrator-emitted Conv30@20%DP PITI from Step 1>"
        tolerance: "0.50"
        source_script: property_analyze.py
        numeric_status: anchored        # WARNING 5 closure: agrees with Plan 15-01 Wave 0 stub
        provenance: stdout
      - label: first_year_interest_conv30
        value: "<RECONCILED — paste actual orchestrator-emitted Conv30 first-year interest from Step 1>"
        tolerance: "0.50"
        source_script: property_analyze.py
        numeric_status: anchored        # WARNING 5 closure: agrees with Plan 15-01 Wave 0 stub
        provenance: stdout
      - label: verdict_reasons_count
        value: "<RECONCILED — actual count of verdict.reasons entries, formatted as `X.0` with mandatory decimal point per NUMBER_REGEX>"
        tolerance: "0.0"
        source_script: property_analyze.py
        numeric_status: anchored        # WARNING 5 closure: agrees with Plan 15-01 Wave 0 stub
        provenance: stdout
    ---
    
    Analyze this Zillow listing for me: https://www.zillow.com/homedetails/synthetic/1_zpid/
    ```
    
    Important details:
    - All three numerics MUST have a decimal point (`.`) — NUMBER_REGEX in evals/metrics.py requires it. So `verdict_reasons_count: "1.0"` not `"1"`.
    - `provenance: stdout` — Phase 12 D-12-SC3-01 stdout-only sourcing. The values must appear in the rendered markdown report (which the eval transcript includes via stdout capture).
    - `source_script: property_analyze.py` — must match the orchestrator's basename for the stdout-sourcing gate.
    - Verdict pinning via expected_route_keywords (A4 + L775) — `"GO"` is a string, not a numeric, so it goes in route_keywords not expected_numbers.
    - The prompt body is a single user-facing line that triggers Row 0 dispatch (zillow.com URL). The evals.runner replay-stub mode does NOT actually invoke WebFetch — it stitches the expected scripts + numbers into a synthesized transcript per `synthesize_stub_transcript`.
    
    **Step 3 — Update `evals/expected/property-analysis-01.json` (in place, reconciling Plan 15-01 stub):**
    
    Mirror amortize-01.json shape verbatim (PATTERNS L468-490):
    
    ```json
    {
      "schema_version": 1,
      "id": "property-analysis-01",
      "mode": "property",
      "numeric_status": "anchored",
      "expected_scripts": [
        {
          "script": "property_analyze.py",
          "args_must_include": ["--listing", "--household", "--profile", "--output-dir"]
        }
      ],
      "expected_numbers": [
        {"label": "conv30_preferred_dp_piti", "value": "<RECONCILED>", "tolerance": "0.50", "source_script": "property_analyze.py", "numeric_status": "anchored", "provenance": "stdout"},
        {"label": "first_year_interest_conv30", "value": "<RECONCILED>", "tolerance": "0.50", "source_script": "property_analyze.py", "numeric_status": "anchored", "provenance": "stdout"},
        {"label": "verdict_reasons_count", "value": "<RECONCILED with .0 suffix>", "tolerance": "0.0", "source_script": "property_analyze.py", "numeric_status": "anchored", "provenance": "stdout"}
      ],
      "expected_route_keywords": ["property", "property_analyze.py", "GO"],
      "v1_frozen_at": "2026-05-21"
    }
    ```
    
    `v1_frozen_at`: today's date — the date the oracle's reconciliation occurred. Future regressions either pass the gate (no drift) or fail (drift detected) — in either case, this date marks the frozen baseline.
    
    **Step 4 — Run evals.runner to verify the gate:**
    
    ```bash
    uv run python -m evals.runner evals/prompts/property-analysis-01.md
    # Verify exit 0; verify route_match = 1.0; verify numeric_match = pass
    ```
    
    **Step 5 — Run full evals.runner suite to verify SC4 gate holds across all prompts:**
    
    ```bash
    uv run python -m evals.runner
    # Verify exit 0; verify aggregate numeric_pass / (numeric_pass + numeric_fail) ≥ 0.95
    ```
    
    **Step 6 — Run full pytest suite:**
    
    ```bash
    uv run pytest -x
    # Verify exit 0 across all 644+ existing tests + Phase 15's new ≥29 tests
    ```
    
    If any of Step 4/5/6 fails:
    - If the failure is "numeric N within tolerance" — widen tolerance from "0.50" to "1.00" with a comment noting the actual divergence between hand-calc and orchestrator output (document in 15-05-SUMMARY.md)
    - If the failure is "stdout-sourced label not found" — verify the markdown report's rendered text actually contains the numeric in the expected format (e.g., `$3,760.34` for PITI; `$32,335.43` for first-year interest); adjust the formatter (Plan 15-02) if a display-format bug surfaces
    - If the failure is "route_keyword not found" — verify the transcript-stub includes "GO" via the verdict rendering (\_render\_verdict outputs `## VERDICT — **GO**` per Plan 15-02 — confirm "GO" substring matches case-sensitively)
    
    Document each tolerance widening + reconciliation step in 15-05-SUMMARY.md.
  </action>
  <verify>
    <automated>test -f evals/prompts/property-analysis-01.md && test -f evals/expected/property-analysis-01.json && uv run python -m evals.runner evals/prompts/property-analysis-01.md 2>&1 | tail -10 && uv run python -m evals.runner 2>&1 | tail -10 && uv run pytest -x 2>&1 | tail -5 && python -c "import json,frontmatter; pmpt=frontmatter.load('evals/prompts/property-analysis-01.md'); orc=json.load(open('evals/expected/property-analysis-01.json')); assert pmpt.metadata['id']==orc['id']=='property-analysis-01' and 'GO' in pmpt.metadata['expected_route_keywords'] and len(pmpt.metadata['expected_numbers'])==3 and orc['numeric_status']=='anchored'"</automated>
  </verify>
  <acceptance_criteria>
    - evals/prompts/property-analysis-01.md exists; frontmatter parses via python-frontmatter; id="property-analysis-01"; mode="property".
    - evals/expected/property-analysis-01.json exists; matches the prompt's id; numeric_status="anchored"; v1_frozen_at="2026-05-21".
    - expected_route_keywords contains "property", "property_analyze.py", and "GO" (A4 verdict-level pin).
    - expected_numbers has exactly 3 entries: conv30_preferred_dp_piti, first_year_interest_conv30, verdict_reasons_count — all with decimal-point form per NUMBER_REGEX.
    - All 3 numerics are RECONCILED with actual orchestrator output (not hand-calc estimates) — Wave 0 placeholder values from Plan 15-01 are overwritten with the captured values.
    - `uv run python -m evals.runner evals/prompts/property-analysis-01.md` exits 0; route_match=1.0; numeric_match=pass.
    - `uv run python -m evals.runner` (full suite) exits 0; aggregate numeric_pass / (numeric_pass + numeric_fail) ≥ 0.95.
    - `uv run pytest -x` exits 0 — Phase 14 reference 644+ tests + Phase 15's ≥29 new tests all green.
    - All money values in oracle are Decimal-strings (CLAUDE.md money discipline) — no naked floats or ints.
  </acceptance_criteria>
  <done>
    Eval prompt + oracle both committed; reconciled with actual orchestrator output; SC4 gate (≥0.95) holds; full pytest suite green; ROADMAP SC-6 closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Synthetic fixture (Plan 15-01) → orchestrator subprocess (reconciliation step) | Synthetic JSON; committed to repo; trust = full. |
| Orchestrator stdout → oracle numeric pinning | Subprocess output captured during reconciliation; oracle anchors are derived from this exact run. Trust = subprocess-isolation. |
| evals/runner.py replay-stub transcript synthesis → score_numeric_match | Existing Phase 12 harness; trust = full (frozen surface). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-E1 | Tampering | Oracle drift over time (orchestrator behavior change shifts numerics) | mitigate | `v1_frozen_at: "2026-05-21"` timestamp + `numeric_status: "anchored"` flag. Future drift triggers gate failure (route_match or numeric_match < 0.95), surfacing the regression. Repo-tracked oracle becomes the regression baseline. |
| T-15-E2 | Information Disclosure | Synthetic fixture leaks via committed eval prompt | mitigate | Phase 11 D-02 + Plan 15-01 acceptance criteria: synthetic source_url, zpid="1", no PII, no real address. Verified via `grep -i "@gmail|@yahoo|phone:|real" evals/fixtures/property/*.json` returning empty. |
| T-15-E3 | Repudiation | Oracle numerics lack derivation lineage | mitigate | 15-05-SUMMARY.md documents Step 1 reconciliation: which orchestrator invocation produced which numeric; any tolerance widening + reason. |
</threat_model>

<verification>
- `evals/prompts/property-analysis-01.md` parses via python-frontmatter; frontmatter has all required fields (id, mode, description, expected_route_keywords, expected_scripts, expected_numbers).
- `evals/expected/property-analysis-01.json` parses as JSON; schema_version=1; numeric_status="anchored".
- Oracle id matches prompt id: both "property-analysis-01".
- `expected_route_keywords` contains all 3 of: "property", "property_analyze.py", "GO".
- `expected_numbers` has exactly 3 entries; every value matches NUMBER_REGEX (`\$?(\d{1,3}(?:,\d{3})*|\d+)\.\d{1,4}\b`).
- `uv run python -m evals.runner evals/prompts/property-analysis-01.md` exits 0.
- `uv run python -m evals.runner` exits 0 (full suite gate holds at ≥0.95).
- `uv run pytest -x` exits 0 (full pytest suite green).
- Reconciled numerics are derived from actual orchestrator output (cross-check: re-run the orchestrator and verify the oracle numerics still match within their declared tolerances).
- No PII / real listing data in evals/fixtures/property/ or evals/prompts/property-analysis-01.md.
</verification>

<success_criteria>
- ROADMAP SC-6 closed: new eval prompt exercises full property mode against pinned fixture; runner exits 0; route_match + numeric_match ≥ 0.95.
- D-15-EVAL-01 honored: fixture at evals/fixtures/property/ (synthetic; Plan 15-01).
- D-15-EVAL-02 honored: single eval prompt for v1.1; no live WebFetch in CI.
- D-15-EVAL-03 honored: oracle pins (1) verdict.level via route-keyword "GO" (A4), (2) Conv30 preferred-DP PITI, (3) first_year_interest_conv30, (4) verdict_reasons_count.
- Phase 12 inheritance verified: numeric_status="anchored" (not "skip"); provenance="stdout" (D-12-SC3-01); source_script="property_analyze.py" (D-12-SC4-01 three-bucket).
- Full pytest suite green: Phase 15 adds ≥29 new tests (10 in test_property_report + 11 in test_property_analyze_cli + 8 in test_skill_routing) without breaking any of the 644+ Phase 14 baseline tests.
- Oracle numerics are RECONCILED — Wave 0 hand-calc estimates have been overwritten with actual orchestrator subprocess output captured during this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/15-property-skill-mode-report-formatter/15-05-SUMMARY.md` documenting:
- Final eval prompt frontmatter (id, mode, description, expected_route_keywords, expected_scripts, expected_numbers)
- Final oracle JSON (with v1_frozen_at timestamp)
- Reconciliation diary: each numeric's Wave 0 hand-calc value vs Wave 2 reconciled value; tolerance adjustments if any
- evals.runner output: route_match score + numeric_match status (pass/fail/skip) + aggregate gate value
- Full pytest output: 644+N test count; all green
- Confirmation that ROADMAP SC-6 is closed
- Confirmation that no Phase 12 baseline test or other phase's eval was disturbed (regression invariant)
- Phase 15 closure summary: all 5 requirements (MODE-01, MODE-02, MODE-03, RPRT-01, RPRT-02) + all 6 ROADMAP SCs traced to passing tests
</output>
